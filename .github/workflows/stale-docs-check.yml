# Weekly check: file an issue if version markers, deps table, Dockerfile ARGs,
# or website license files are out of sync with versions.env.
name: Stale Docs Check

on:
  schedule:
    - cron: "0 6 * * 1"  # every Monday 06:00 UTC
  workflow_dispatch:       # manual trigger

# The github-script step files issues; the default token can be repo-configured
# read-only, which would 403 the issue creation.
permissions:
  contents: read
  issues: write

jobs:
  stale-docs:
    name: Check for stale docs
    runs-on: ubuntu-26.04
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 1

      - name: Install Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.14"

      # Subset of the canonical gate list in linux/scripts/preflight.sh —
      # version snapshot (also covers inline markers, deps table, Dockerfile
      # ARGs, website license files), ARG consistency, mirror consistency.
      # continue-on-error is load-bearing for the issue-filing design below -
      # the next step reads steps.check.outcome, which only exists because this
      # step is allowed to conclude non-success. It is NOT a licence for the job
      # to go green over stale docs: the explicit re-raise at the end of this
      # job restores that signal. Removing the re-raise would leave a weekly
      # gate that cannot fail.
      - name: Check version/docs consistency
        id: check
        continue-on-error: true
        run: PREFLIGHT_ONLY=version-snapshot,arg-consistency,mirror-consistency,doc-links,doc-dupes,sbom bash linux/scripts/preflight.sh

      - name: Create issue if stale
        if: steps.check.outcome != 'success'
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          script: |
            const title = '📋 Stale docs detected — run sync_versions.py --write';
            const body = [
              'Weekly stale-doc check found out-of-date documentation.',
              '',
              'Run locally to fix:',
              '```bash',
              'python3 docs/scripts/sync_versions.py --write',
              'python3 docs/scripts/generate-website-licenses.py --write',
              '```',
              '',
              'Then commit and push the changes.',
              '',
              '---',
              '_Generated automatically by the Stale Docs Check workflow._',
            ].join('\n');
            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'documentation',
            });
            const alreadyOpen = issues.find(i =>
              i.title.includes('Stale docs detected')
            );
            if (alreadyOpen) {
              console.log('Issue already exists, skipping.');
              return;
            }
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: title,
              body: body,
              labels: ['documentation'],
            });

      # The gate's verdict, restored. Without this the weekly run reports SUCCESS
      # over provably stale docs and the only signal is an issue nobody is paged
      # for - a gate that cannot fail. The issue is still filed first, so this
      # costs the alerting design nothing.
      - name: Fail the run when the docs are stale
        if: steps.check.outcome != 'success'
        run: |
          echo "::error::version/docs consistency check failed - see the 'Check version/docs consistency' step"
          exit 1
