# code-dupes.allow -- deliberate twins, with a budget and a reason.
# Format: fileA | fileB | budget | reason
# Generated by --baseline; every entry below is PRE-EXISTING duplication
# frozen so the gate only reports NEW or GROWING copies. The budget must
# EQUAL the measurement: a copy that shrank fails until its row says so.

linux/scripts/03-media/build/ffmpeg/ffmpeg-dnn-backends.sh | linux/scripts/03-media/build/opencv/install-deps.sh | 12 | reviewed 2026-09-01: structurally similar multi-line file-presence gates (TF-SDK cache stamp vs riscv64 harfbuzz dev files) over DIFFERENT domains — not a dedup target; budget pinned at current size so growth still trips
linux/scripts/build-runtime-artifacts.sh | linux/scripts/build-runtime-manifest.sh | 28 | reviewed 2026-09-02: was 199 — the 17 option lines both documented identically now live in runtime_shared_usage_options (01-core/runtime-build-fns.sh), beside the runtime_shared_usage_env_overrides this library already owned; those flags are handled BY that library, so that is where they belong. --help output verified unchanged in content (58/58 and 64/64 lines; only --help and --image-prefix swap places, and --help last reads better). The 28 left are the orchestrator preamble and a case tail — boilerplate every such script has, not a dedup target
linux/scripts/03-media/build/iree/android/build-android.sh | linux/scripts/03-media/build/litert/android/build-android.sh | 12 | reviewed 2026-09-02: was 179 — the duplicated resolve_host_compiler (source-or-fallback, ~25 lines) moved into android-build-preamble.sh, which both scripts already source and which every android stage shares. The iree copy's own comment said "aligned with the litert copy", so the duplication was known and just had no owner. Source order verified in both (preamble before first use). The 12 left are the Android CMake toolchain flags, shared with the opencv android script too — a three-file family, not a pair, so it wants its own pass
linux/scripts/lint-shell.sh | linux/scripts/lint-workflows.sh | 82 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/06-packaging/smoke-runtime-image.sh | linux/scripts/06-packaging/smoke-runtime-image.sh | 90 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-tvm-cmake-args.sh | linux/scripts/tests/test-tvm-cmake-args.sh | 88 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 23 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/05-frameworks/torch/build-app-wheelhouse.sh | linux/scripts/05-frameworks/torch/build-app-wheelhouse.sh | 85 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 9 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/01-core/context-management.sh | linux/scripts/01-core/cross-stage-build.sh | 82 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/Dockerfile.media | linux/Dockerfile.toolchain | 81 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-smoke-arch-parity.sh | linux/scripts/tests/test-smoke-arch-parity.sh | 81 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 8 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/01-core/compiler-resolution.sh | linux/scripts/03-media/build/ffmpeg/ffmpeg-probe-framework.sh | 79 | 80 -> 79 on 2026-09-05 (android ABI knob): reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned F3 VERDICT 2026-09-07, per-CONSUMER like the gstreamer-env/libcamera-env pair: the fallback in ffmpeg-probe-framework.sh is reachable and the one in android-build-preamble.sh is not. build-ffmpeg.sh reaches resolve_host_compiler_for_lang only through media_common_init -> `source_module compiler-resolution.sh || true`, which TOLERATES an absent module, so the inline ladder is what runs on a host checkout -- LIVE. Dockerfile.android COPYs 01-core/compiler-resolution.sh to /opt/scripts/core (Dockerfile.android:96), so the preamble's `[ -f ]` branch always wins in the image -- its fallback is DEAD there and live only on a host checkout, which is the same shape the gstreamer-env pair was KEPT for. KEEP both: deleting a fallback that stands in for the module it duplicates is a build-closure edit no gate can prove, and the branch costs one [ -f ] per script start.
linux/scripts/06-packaging/validate-compilers.sh | linux/scripts/06-packaging/validate-compilers.sh | 71 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 9 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/host-config/prune-safe.sh | linux/host-config/prune-safe.sh | 69 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 1 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned F3 VERDICT 2026-09-07: the OTHER prune-safe overlap -- with 01-core/disk-guard.sh over the filtered `buildctl prune` command and the GB->MB convention -- has NO code owner available and cannot get one: prune-safe.sh runs main on load, so it cannot be sourced, and extracting the rest means restructuring a host-config script that operators run by hand. The x1000 half is no longer a guess: `buildctl prune --help` documents --keep-storage in MB, so keep_gb * 1000 really is ~120 GB. Recorded, not owned.
linux/scripts/01-core/build-helpers.sh | linux/scripts/02-toolchain/bootstrap.sh | 68 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 9 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/build-cross-chain.sh | linux/scripts/build-cross-stage.sh | 17 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/lib/code-quality.sh | linux/scripts/lib/coverage.sh | 13 | 59 -> 13 on 2026-09-08: the require_tools/has_tool pair the previous note called "NOT shared because code-quality.sh needs has_tool and coverage.sh does not" now IS shared - 01-core/tool-checks.sh owns both, and declares each only when the caller has not, so the per-project override the two inline fallbacks existed for still works. What is left at 13 is the load guard plus the log-bootstrap source line. PRIOR: 61 -> 59 on 2026-09-05 (post-merge): reviewed 2026-09-02 by MEASUREMENT: longest shared run 12 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows. F3 2026-09-04: 65→61, the four the shared logging bootstrap took with it (lib/log-bootstrap.sh); the rest is the require_tools/has_tool fallback pair, which is NOT shared because code-quality.sh needs has_tool and coverage.sh does not
linux/scripts/tests/test-parallel-loop.sh | linux/scripts/tests/test-parallel-loop.sh | 65 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/01-core/common.sh | linux/scripts/01-core/compiler-cache.sh | 28 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 8 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows 2026-09-05: 55 -> 28 (and the self-pair 39 -> 24) because the sccache server-address block that common.sh and the bootstrap path each carried now has ONE owner, sccache_export_server_address; the budget follows the measurement down so the shrink cannot be spent again
linux/scripts/05-frameworks/tvm-detect.sh | linux/scripts/05-frameworks/tvm-detect.sh | 52 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 12 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/04-runtime/gstreamer-env.sh | linux/scripts/04-runtime/libcamera-env.sh | 50 | reviewed 2026-09-02, READ: the 27-line run is a source-or-fallback for 01-core/path-helpers.sh — the BOOTSTRAP PARADOX (a fallback must duplicate what it stands in for). Same shape as the compiler-resolution family. Not a dedup target. REACHABILITY ANSWERED 2026-09-04, per-RUN as the KEEP decision demands (backlog archive 2026-09-03 F3): libcamera-env.sh has exactly ONE consumer, 04-runtime/entrypoint.sh, in the torch image, whose base carries Dockerfile.package:285 `COPY linux/scripts/01-core/ /opt/scripts/core/` — path-helpers.sh IS there, so that fallback is DEAD. gstreamer-env.sh has four: the same entrypoint (dead), Dockerfile.media:913 and :965, both of which bind-mount 01-core ENTIRE (dead), and setup-gstreamer.sh:420 / build-libcamera.sh:86, which source it by repo-relative path on a host checkout where /opt/scripts does not exist — LIVE. KEEP both anyway: they are one 27-line block, deleting half of it is a build-closure edit no gate can prove, and the branch costs one [ -f ] per container start Re-measured 51 -> 50 on 2026-09-04: unchanged files -- a new tests/*.sh pushed one more shingle past the >6-owner idiom suppression, the same bookkeeping the crlf-guard/doc-links row already carries.
linux/scripts/05-frameworks/tvm-python.sh | linux/scripts/05-frameworks/tvm.sh | 51 | reviewed 2026-09-02, READ: 13 lines are the same CLI invocation (--build-type/--cc/--cxx/--llvm-cmake-value) assembled in two places. A real but small target: one arg-builder would own it. Budget pinned
linux/scripts/verify-critical-fixes.sh | linux/scripts/verify-critical-fixes.sh | 47 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 7 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/01-core/cli-parsers.sh | linux/scripts/01-core/cli-parsers.sh | 44 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 6 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/lib/cmake-build.sh | linux/scripts/lib/ctest-run.sh | 44 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 11 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/01-core/common.sh | linux/scripts/03-media/build/gstreamer/common/build-gstreamer-monorepo.sh | 45 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 10 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch -- budget pinned so growth trips the gate. CL7 2026-09-05: 42 -> 45 with BOTH files unchanged. The cross_build_is_active fallback had THREE owners and the third was the unreachable 03-media/core/common.sh clone; deleting it dropped the family below the multi-owner idiom cutoff, which unsuppressed three shingles HERE. Three owners remain and none is redundant: 01-core/common.sh owns the documented fallback, _gst_monorepo_env_setup keeps a live superset because it runs in stage contexts where 01-core is never sourced, and install-deps-preamble.sh defines the trivial native stubs. test-cross-fallback-parity.sh asserts their behaviour matches in four scenarios
linux/scripts/03-media/build/onnxruntime/build/30-build-native-amd.sh | linux/scripts/03-media/build/onnxruntime/build/30-build-native-nvidia.sh | 43 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 9 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/03-media/build/ffmpeg/ffmpeg-probe-framework.sh | linux/scripts/03-media/build/ffmpeg/ffmpeg-probe-framework.sh | 42 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 10 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/02-toolchain/build-clang.sh | linux/scripts/02-toolchain/llvm-cross.sh | 42 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/02-toolchain/gcc.sh | linux/scripts/02-toolchain/gcc.sh | 42 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 10 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/tests/test-cross-oci-handoff.sh | linux/scripts/tests/test-cross-oci-handoff.sh | 41 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 10 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/03-media/build/iree/android/build-android.sh | linux/scripts/05-frameworks/torch/build-app-wheelhouse.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/03-media/android-build-preamble.sh | linux/scripts/03-media/build/gstreamer/android/build-android-from-source.sh | 34 | 39 -> 34 on 2026-09-07: media_jobs takes [cap_mb] now, so the lane calls it with ANDROID_GSTREAMER_PER_JOB_MB instead of re-implementing the on-demand load for one number. What is left is the android preamble idiom the row already described. reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned. F3 2026-09-04, FAMILY READ (android-build-preamble.sh:77 / build-android-from-source.sh:236 / build-app-wheelhouse.sh:76, 26 shingles): NOT a shared idiom — it is media_jobs, and the name already has TWO definitions. 03-media/core/common.sh:221 assumes media_common_init pre-loaded parallelism.sh; android-build-preamble.sh:77 is a strict superset that sources it on demand; both hardcode a 2000 MB cap. build-android-from-source.sh SOURCES the preamble at line 6 and then re-implements the function inline anyway (its comment says "Mirrors media_jobs() but keeps the configurable per-job cap"), and build-app-wheelhouse.sh is a fourth copy at 4096 MB via compute_cpp_heavy_jobs. The fix is one media_jobs [cap_mb] defaulting to 2000 — behaviour-neutral for all 14 existing callers — but the two same-named definitions mean whichever is sourced LAST wins wherever both are in scope, and NOTHING covers media_jobs today. Build closure, needs a chain and a characterisation suite first
linux/scripts/tests/test-compiler-cache.sh | linux/scripts/tests/test-compiler-cache.sh | 24 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 9 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows 2026-09-05: 39 -> 24, same cause as the common.sh pair — the duplicated server-address block moved behind sccache_export_server_address, and the new cases assert that one function instead of re-typing its arms
linux/scripts/05-frameworks/torch/build-app-wheelhouse.sh | linux/scripts/05-frameworks/tvm-python.sh | 38 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/03-media/build/gstreamer/common/build-gst-plugins-rs.sh | linux/scripts/03-media/build/gstreamer/common/setup-gstreamer.sh | 37 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/01-core/common.sh | linux/scripts/06-packaging/smoke-common.sh | 12 | F3 2026-09-05, NEW row and read in full: both files carry a cross_build_is_active FALLBACK for when cross-env.sh is not sourced, and the shared text is the two comment lines that say so plus the guard shape -- longest identical run 2 line(s). It cannot have one owner: smoke-common.sh exists precisely to be self-sufficient in an image where 01-core may be absent, which is the same reason its platform.sh row above is KEEP. It surfaced now, with BOTH files unchanged, because dropping the two ffmpeg/pyav copies of the launcher block took shared shingles under the >6-owner idiom cutoff; recorded, not re-suppressed
linux/scripts/01-core/platform.sh | linux/scripts/06-packaging/smoke-common.sh | 34 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned. 36 -> 34 on 2026-09-05 with BOTH files unchanged: the new 06-packaging/smoke-critical-fixes.sh is a SEVENTH owner of two arch-map shingles, which puts them over the >6-owner idiom cutoff and SUPPRESSES them here. The 2026-09-04 log-bootstrap note records this effect in the other direction; adding an owner moves the number too, and the honest response is the same — re-read and record, never widen MAX_OWNERS
linux/scripts/03-media/build/ffmpeg/build-ffmpeg.sh | linux/scripts/03-media/build/ffmpeg/ffmpeg-probe-framework.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it. F3 2026-09-05: the guarded launcher-resolution block left build-ffmpeg.sh for media_compiler_launcher in 03-media/core/common.sh, and this pair fell with it. Recorded at the new measurement
linux/scripts/01-core/compiler-cache.sh | linux/scripts/02-toolchain/build-gcc.sh | 37 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned. F3 2026-09-05: 36 -> 38 with BOTH files unchanged. Extracting report_onnx_build_output dropped three copies of the wheel-listing and capped library-listing shingles below the >6-owner idiom cutoff, which unsuppressed them here. Re-read, not re-suppressed: longest identical run is still 4 line(s) of tolerant redirect-and-continue guard idiom, not a copied block. 38 -> 37 on 2026-09-05 when the sccache setup line gained its [server=] field (YB): one shingle of the shared 'log the cache dir and cap' idiom stopped being identical, which is the budget doing its job
linux/scripts/06-packaging/setup-package-image.sh | linux/scripts/06-packaging/validate-compilers.sh | 36 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/runtime-build-fns.sh | linux/scripts/01-core/runtime-build-fns.sh | 36 | reviewed 2026-09-04 by MEASUREMENT: longest shared run 10 line(s); +1 shingle when both call sites gained the per-arch build-arg argument. Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/02-toolchain/vulkan.sh | linux/scripts/02-toolchain/vulkan.sh | 26 | 31 -> 26 on 2026-09-07: the three dedicated builders' failure arms record the component name through ONE _vk_note_failure instead of a two-line copy each. 35 -> 31 on 2026-09-05: the six new target-SDK components share ONE _vulkan_target_install_component instead of a copy of the configure/build/log block each. reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in the same file, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/gstreamer/common/build-gstreamer-monorepo.sh | linux/scripts/03-media/build/gstreamer/common/build-gstreamer-stage.sh | 35 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 7 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/03-media/build/onnxruntime/build/30-build-native.sh | linux/scripts/03-media/build/onnxruntime/build/60-build-genai.sh | 18 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned. F3 2026-09-05: 34 -> 18, the shared end-of-stage summary (headline + wheel listing + capped .so listing) now has ONE owner, report_onnx_build_output in lib/common.sh; the four copies had already drifted (the -amd one listed libraries with sed -n '1,20p' where the rest used head -20)
linux/scripts/01-core/base-image.sh | linux/scripts/02-toolchain/setup-dependencies.sh | 34 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 10 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/01-core/install-tensorrt.sh | linux/scripts/03-media/build/onnxruntime/build/30-build-native-nvidia.sh | 34 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/onnxruntime/build/30-build-native-nvidia.sh | linux/scripts/03-media/build/onnxruntime/build/30-build-native.sh | 13 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 8 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows F3 2026-09-05: 34 -> 13, the shared end-of-stage summary (headline + wheel listing + capped .so listing) now has ONE owner, report_onnx_build_output in lib/common.sh; the four copies had already drifted (the -amd one listed libraries with sed -n '1,20p' where the rest used head -20)
linux/scripts/lint-dockerfiles.sh | linux/scripts/lint-shell.sh | 34 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/03-media/build/ffmpeg/build-ffmpeg.sh | linux/scripts/03-media/build/onnxruntime/build/lib/common.sh | 14 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it. F3 2026-09-05: the guarded launcher-resolution block left build-ffmpeg.sh for media_compiler_launcher in 03-media/core/common.sh, and this pair fell with it. Recorded at the new measurement
linux/scripts/01-core/verify.sh | linux/scripts/02-toolchain/llvm-validate.sh | 33 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/lint-dockerfiles.sh | linux/scripts/lint-workflows.sh | 33 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/01-core/verify-arg-consistency.sh | linux/scripts/01-core/verify-arg-consistency.sh | 32 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 8 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/06-packaging/copy-media-payloads.sh | linux/scripts/06-packaging/validate-compilers.sh | 29 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned. 32 -> 29 on 2026-09-05: copy_media_payloads gained /usr/local/include/absl (707 of the 1322 LiteRT headers it copies include absl/), which breaks three 12-token windows that used to span the insertion point. Both lists still name the same payload tree on purpose — validate-compilers only NOTEs what is missing
linux/scripts/03-media/build/onnxruntime/build/30-build-native-amd.sh | linux/scripts/03-media/build/onnxruntime/build/30-build-native.sh | 20 | reviewed 2026-09-02, READ: 13 lines of shared BUILD_ARGS cmake defines. The -amd variant deliberately mirrors the base build's flags; diverging them silently is the greater risk. Budget pinned so drift is visible F3 2026-09-05: 31 -> 20, the shared end-of-stage summary (headline + wheel listing + capped .so listing) now has ONE owner, report_onnx_build_output in lib/common.sh; the four copies had already drifted (the -amd one listed libraries with sed -n '1,20p' where the rest used head -20)
linux/scripts/03-media/build/ffmpeg/ffmpeg-probe-framework.sh | linux/scripts/03-media/build/onnxruntime/build/lib/common.sh | 31 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/path-helpers.sh | linux/scripts/04-runtime/gstreamer-env.sh | 30 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 12 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/01-core/path-helpers.sh | linux/scripts/04-runtime/libcamera-env.sh | 30 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 12 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/01-core/downloads.sh | linux/scripts/05-frameworks/tvm.sh | 30 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/build-helpers.sh | linux/scripts/02-toolchain/build-gcc.sh | 29 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it. F3 2026-09-04, FAMILY READ (build-helpers.sh:160 / bootstrap.sh:41 / build-gcc.sh:825, 27 shingles): the pairwise measurement stands, but the family is the find/file/awk/xargs ELF strip, and it already HAS an owner — strip_elf_tree, whose own comment says it "Centralizes the find/file/awk/xargs strip pattern duplicated in 02-toolchain". bootstrap.sh's copy is the legitimate source-or-fallback half. build-gcc.sh:833 is the one caller that never converted, and it is NOT equivalent: it filters `-type f -executable` + awk on ELF executable-or-shared-object, the owner takes `-type f` + /ELF/. Calling the owner would strip a WIDER set in the shipped toolchain, so this needs either a filter argument on strip_elf_tree or an accepted behaviour change — build closure, needs a chain
linux/scripts/02-toolchain/bootstrap.sh | linux/scripts/02-toolchain/build-gcc.sh | 29 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/android-build-preamble.sh | linux/scripts/05-frameworks/torch/build-app-wheelhouse.sh | 40 | reviewed 2026-09-02: grew 29 -> 40 when the iree+litert host-compiler fallbacks were consolidated INTO the preamble. Total duplication in the tree went DOWN (two copies became one); this pair's number went up because the surviving copy now sits in a file that already overlapped here. Fourth site of the same family
linux/scripts/03-media/build/onnxruntime/build/60-build-genai.sh | linux/scripts/03-media/build/onnxruntime/build/lib/common.sh | 28 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 11 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/tests/test-codec-so-map-convergence.sh | linux/scripts/tests/test-codec-so-map-convergence.sh | 28 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/01-core/tag-naming.sh | linux/scripts/01-core/tag-naming.sh | 27 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 6 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/06-packaging/smoke-media.sh | linux/scripts/06-packaging/smoke-media.sh | 27 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/01-core/common.sh | linux/scripts/02-toolchain/probe-sccache.sh | 27 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/06-packaging/smoke-android.sh | linux/scripts/06-packaging/smoke-common.sh | 27 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/onnxruntime/build/50-build-js.sh | linux/scripts/03-media/build/onnxruntime/build/50-build-js.sh | 26 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/05-frameworks/tvm.sh | linux/scripts/05-frameworks/tvm.sh | 26 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 6 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/02-toolchain/android-sdk.sh | linux/scripts/02-toolchain/android-sdk.sh | 23 | 25 -> 23 on 2026-09-05 (post-merge): reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/build-runtime-artifacts.sh | linux/scripts/build-sdk-artifacts.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/lib/code-quality.sh | linux/scripts/lib/code-quality.sh | 24 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/01-core/platform.sh | linux/scripts/01-core/platform.sh | 24 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 8 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/01-core/runtime-build-fns.sh | linux/scripts/build-sdk-artifacts.sh | 24 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/gstreamer/common/setup-gstreamer.sh | linux/scripts/03-media/build/opencv/build-opencv.sh | 24 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 7 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/03-media/runtime/validate-media-runtime.sh | linux/scripts/06-packaging/setup-torch-venv.sh | 24 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/build-cross-chain.sh | linux/scripts/verify-cross-chain.sh | 24 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/runtime/verify-media-artifacts.sh | linux/scripts/03-media/runtime/verify-media-artifacts.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 6 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned; re-measured 23 -> 12 on 2026-09-04 when verify_shared_lib_optional was deleted as dead.
linux/webserver/scripts/flutter_integration_smoke_test.sh | linux/webserver/scripts/flutter_integration_smoke_test.sh | 23 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in the same file, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/02-toolchain/build-gcc.sh | linux/scripts/02-toolchain/build-gcc.sh | 20 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 14 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/01-core/cmake-cache-linker.sh | linux/scripts/01-core/compiler-cache.sh | 22 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 6 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/01-core/cross-python.sh | linux/scripts/03-media/build/gstreamer/android/build-android-from-source.sh | 22 | reviewed 2026-09-02, READ: 16-line host-python resolution, same source-or-fallback family as path-helpers and compiler-resolution. Third instance of that shape — the family, not this pair, is the unit of work
linux/scripts/02-toolchain/bootstrap.sh | linux/scripts/02-toolchain/bootstrap.sh | 22 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/06-packaging/verify-parity.sh | linux/scripts/stop-cross-chain.sh | 22 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/tests/test-genai-smoke-payload.sh | linux/scripts/tests/test-genai-smoke-payload.sh | 22 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/lib/agentic-loop.sh | linux/scripts/lib/agentic-loop.sh | 38 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 6 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in the same file, not a copied block. Budget pinned so a real copy still trips it. F2 2026-09-05: 22 -> 38 with the surviving code UNCHANGED -- splitting the engine adapters out to lib/agentic-engines.sh took the section/log "Command:"/DRY_RUN preamble from >6 owners down to 3 (invoke_build, invoke_tests, invoke_quality) and unsuppressed shingles that had been idiom. Third time in three waves; read and recorded, not re-suppressed. The named seam if it ever earns the work: one _agentic_phase_run <banner> <cmd> <noun> -- deliberately NOT taken, because signalling the dry-run short-circuit back to three callers with different verdict vocabularies (BUILD PASSED/FAILED, TESTS PASSED/FAILED, quality tolerates any rc) needs an out-of-band return code, and nothing covers those three functions
linux/scripts/lib/code-quality.sh | linux/scripts/lib/docs-build.sh | 25 | 26 -> 25 on 2026-09-07 (MEASURED, not chosen): code_quality_find_dart_files is now a one-line call into code_quality_find_tracked_files -- the single owner of the build/vendored exclusion set, shared with flutter_checks.sh's _find_pubspecs, which used to restate it and omitted rust_builder/. The wrapper that is left is below the gate's minimum unit size, so the one shingle it shared with docs-build.sh went with it. Nothing about the pair itself changed. 22 -> 26 on 2026-09-06: code_quality_ensure_cmake_format resolves Scripts/activate for Windows (Git Bash) venvs; docs-build keeps the plain bin/activate with its loud missing-venv err. Reviewed 2026-09-02 by MEASUREMENT: longest shared run 11 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/tests/test-compiler-cache-launcher.sh | linux/scripts/tests/test-compiler-cache-launcher.sh | 22 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/01-core/cross-stage-build.sh | linux/scripts/01-core/disk-guard.sh | 15 | REVIEWED 2026-08-31 KEEP: the two disk helpers share only a local-decl + df-read preamble (longest run 2 lines); nothing extractable 2026-09-07: budget lowered with the DISK3 image lever in the corpus -- more owners for the df/threshold preamble pushed shingles under the idiom cutoff; re-read, not re-suppressed. RE-MEASURED 2026-09-07 after DISK3: extracting _disk_guard_lever_ready and _disk_guard_lever_needed took this block from >6 owners to fewer, which UNSUPPRESSED shingles that were never findings -- the same effect the log-bootstrap and ORT-summary extractions recorded. Read, not re-suppressed: longest identical run is 2-3 lines of local decls plus the `case ''|*[!0-9]*` numeric guard. 2026-09-07 (2nd pass, same cascade): the parallelism/gst-rs extractions shifted the corpus again; measured DOWN once more and recorded, never re-suppressed.
linux/scripts/01-core/logging.sh | linux/scripts/01-core/logging.sh | 21 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in the same file, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/02-toolchain/build-clang.sh | linux/scripts/02-toolchain/build-gcc.sh | 21 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 7 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/01-core/cross-stage-build.sh | linux/scripts/01-core/cross-stage-build.sh | 20 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/06-packaging/setup-torch-venv.sh | linux/scripts/06-packaging/setup-torch-venv.sh | 20 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in the same file, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/python_uv.sh | linux/scripts/02-toolchain/install-rust.sh | 20 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/lib/app-runner.sh | linux/scripts/lib/ctest-run.sh | 20 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 11 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/06-packaging/verify-parity.sh | linux/scripts/06-packaging/verify-parity.sh | 18 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/Dockerfile.android | linux/Dockerfile.sdk | 18 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 8 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows Re-measured 2026-09-04: -1 when Dockerfile.android's PATH left the ENV block it could not read (docs/code-quality-tooling.md#env-instruction-ordering-dockerfile-lint).
linux/host-config/install-nerdctl-full.sh | linux/scripts/02-toolchain/bootstrap.sh | 15 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/onnxruntime/build/30-build-native.sh | linux/scripts/03-media/runtime/repair-wheels.sh | 19 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/ancestry.sh | linux/scripts/01-core/ancestry.sh | 19 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/03-media/build/ffmpeg/build-ffmpeg.sh | linux/scripts/03-media/build/ffmpeg/build-ffmpeg.sh | 19 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in the same file, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/gstreamer/android/build-gstreamer.sh | linux/scripts/05-frameworks/flutter/setup-flutter.sh | 19 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/gstreamer/common/pre-setup.sh | linux/scripts/03-media/build/gstreamer/install-deps.sh | 19 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/03-media/build/iree/android/build-android.sh | linux/scripts/03-media/build/iree/android/build-android.sh | 19 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/03-media/build/litert/install-litert-web.sh | linux/scripts/03-media/build/litert/install-litert-web.sh | 19 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 1 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/03-media/build/onnxruntime/build/40-build-wasm.sh | linux/scripts/03-media/build/onnxruntime/build/40-build-wasm.sh | 19 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 1 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in the same file, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/tests/test-logging-err-trap.sh | linux/scripts/tests/test-logging-err-trap.sh | 19 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 8 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/Dockerfile.amd | linux/Dockerfile.nvidia | 18 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 8 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/01-core/platform.sh | linux/scripts/tests/test-parallel-loop.sh | 18 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/build-helpers.sh | linux/scripts/01-core/digest-pinning.sh | 18 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 8 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/lint-shell.sh | linux/scripts/lint-shell.sh | 18 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in the same file, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/runtime/assemble-torch-app.sh | linux/scripts/03-media/runtime/assemble-torch-app.sh | 17 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 8 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/tests/test-ancestry.sh | linux/scripts/tests/test-ancestry.sh | 17 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-lib-smoke.sh | linux/scripts/tests/test-lib-smoke.sh | 17 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-tvm-wheel-diagnostics.sh | linux/scripts/tests/test-tvm-wheel-diagnostics.sh | 17 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/03-media/build/gstreamer/common/pre-setup.sh | linux/scripts/03-media/build/gstreamer/common/pre-setup.sh | 16 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 9 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/01-core/base-image.sh | linux/scripts/01-core/use-fast-ubuntu-mirror.sh | 16 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 9 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/01-core/install-deps-preamble.sh | linux/scripts/03-media/build/gstreamer/common/build-gstreamer-monorepo.sh | 18 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it CL7 2026-09-05: budget moved with BOTH files unchanged. Deleting the unreachable cross_build_is_active clone in 03-media/core/common.sh took the guarded-fallback family from four owners to three, which dropped it below the multi-owner idiom cutoff and unsuppressed shingles here. The remaining three are deliberate and NOT interchangeable: 01-core/common.sh owns the documented approximation, _gst_monorepo_env_setup keeps a live superset for stage contexts where 01-core is never sourced, and install-deps-preamble.sh defines the trivial native `false` stubs an install-deps script needs when no cross module exists at all
linux/scripts/03-media/build/litert/build-litert.sh | linux/scripts/05-frameworks/torch/build-app-wheelhouse.sh | 16 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/onnxruntime/build/20-fetch.sh | linux/scripts/03-media/build/onnxruntime/build/20-fetch.sh | 16 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/01-core/cross-python.sh | linux/scripts/01-core/cross-python.sh | 16 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 7 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/01-core/cross-stage-build.sh | linux/scripts/01-core/stage-defs.sh | 16 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/02-toolchain/gcc.sh | linux/scripts/02-toolchain/vulkan.sh | 16 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/03-media/build/gstreamer/common/install-rice-proto.sh | linux/scripts/03-media/build/gstreamer/common/setup-gstreamer.sh | 16 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/lib-orchestrator.sh | linux/scripts/lib-orchestrator.sh | 16 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 10 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/01-core/common.sh | linux/scripts/01-core/install-deps-preamble.sh | 17 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it CL7 2026-09-05: budget moved with BOTH files unchanged. Deleting the unreachable cross_build_is_active clone in 03-media/core/common.sh took the guarded-fallback family from four owners to three, which dropped it below the multi-owner idiom cutoff and unsuppressed shingles here. The remaining three are deliberate and NOT interchangeable: 01-core/common.sh owns the documented approximation, _gst_monorepo_env_setup keeps a live superset for stage contexts where 01-core is never sourced, and install-deps-preamble.sh defines the trivial native `false` stubs an install-deps script needs when no cross module exists at all
linux/scripts/01-core/setup-rocm-repo.sh | linux/scripts/01-core/setup-rocm-repo.sh | 15 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/01-core/verify-ubuntu-mirror-consistency.sh | linux/scripts/01-core/verify-ubuntu-mirror-consistency.sh | 15 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/03-media/build/armnn/build-arm-compute-library.sh | linux/scripts/03-media/build/armnn/build-armnn.sh | 15 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/03-media/runtime/repair-wheels.sh | linux/scripts/06-packaging/smoke-media.sh | 15 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/gstreamer/install-deps.sh | linux/scripts/03-media/build/litert/install-deps.sh | 15 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/03-media/build/litert/build-litert.sh | linux/scripts/03-media/build/litert/build-litert.sh | 15 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 6 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-ffmpeg-dnn-contract.sh | linux/scripts/tests/test-lib-smoke.sh | 15 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/host-config/ghcr-delete-tags.sh | linux/host-config/ghcr-delete-tags.sh | 14 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 0 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in the same file, not a copied block. Budget pinned so a real copy still trips it
linux/host-config/install-nerdctl-full.sh | linux/host-config/install-nerdctl-full.sh | 14 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in the same file, not a copied block. Budget pinned so a real copy still trips it
linux/host-config/install-nerdctl-full.sh | linux/scripts/build-runtime-manifest.sh | 14 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/abseil-headers.sh | linux/scripts/05-frameworks/torch/build-app-wheelhouse.sh | 25 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned. F3 2026-09-04: measurement rose from 14 without an edit to either file — the lib/*.sh logging preamble moved to lib/log-bootstrap.sh, so its shingles dropped from 9+ owners to under MAX_OWNERS=6 and stopped being suppressed as idiom. Re-measured 2026-09-04, READ: this is the DEFENSIVE-LOGGER family, not the lib/ one — `command -v` not `declare -F`, bare [INFO]/[WARN] with no ANSI, no err(), and no attempt to load logging.sh, because the module chain (artifact-common/common.sh) normally supplies them. It cannot adopt lib/log-bootstrap.sh: lib/ is deliberately in no Dockerfile, and these three files are all inside the build closure. KEEP
linux/scripts/02-toolchain/android-sdk.sh | linux/scripts/02-toolchain/vulkan.sh | 14 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/common.sh | linux/scripts/02-toolchain/bootstrap.sh | 14 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/01-core/platform.sh | linux/scripts/tests/test-ffmpeg-dnn-contract.sh | 14 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 1 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/tests/test-ffmpeg-dnn-contract.sh | linux/scripts/tests/test-parallel-loop.sh | 14 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/03-media/runtime/validate-media-runtime.sh | linux/scripts/03-media/runtime/validate-media-runtime.sh | 14 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 7 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/02-toolchain/build-clang.sh | linux/scripts/06-packaging/verify-parity.sh | 14 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/02-toolchain/build-clang.sh | linux/scripts/stop-cross-chain.sh | 14 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/armnn/build-armnn.sh | linux/scripts/03-media/build/armnn/install-deps.sh | 14 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/06-packaging/smoke-vulkan.sh | linux/scripts/06-packaging/smoke-vulkan.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in the same file, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/gstreamer/install-deps.sh | linux/scripts/03-media/build/opencv/install-deps.sh | 14 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/03-media/build/litert/install-deps.sh | linux/scripts/03-media/build/opencv/install-deps.sh | 14 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-vulkan-target-decomposition.sh | linux/scripts/tests/test-vulkan-target-decomposition.sh | 13 | 14 -> 13 on 2026-09-08 by MEASUREMENT, with THIS file unchanged and nothing de-duplicated inside it: the fake-shellcheck fixture this batch added to test-workflow-lint.sh (mkdir -p / printf > / chmod +x, the unit at :156) became the SEVENTH holder of the shingle `mkdir - p "S" printf "S" > "S" chmod + x "S"` — test-compiler-cache-launcher.sh:63, test-cross-apt.sh:224, test-mutation-gate.sh:132, test-setup-package-image.sh:135, test-vulkan-target-decomposition.sh:175 and :190, test-workflow-lint.sh:156 — so MAX_OWNERS=6 now reads it as idiom and stops counting it for this self-pair. Proven, not inferred: re-indexing the corpus with HEAD’s test-workflow-lint.sh in place of the working copy puts the pair back at exactly 14. Nothing here shrank by being fixed, so the budget follows the measurement DOWN and the shrink cannot be spent on a real copy later. PRIOR: reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-crlf-guard.sh | linux/scripts/tests/test-doc-links.sh | 12 | reviewed 2026-09-03 by MEASUREMENT: longest shared run 4 line(s) — the standard suite preamble (set -u, TESTS_DIR, source test-harness.sh, REPO_ROOT) every file in linux/scripts/tests carries. A shared idiom, not a copy. Budget pinned; re-measured 13 -> 12 on 2026-09-04 when new tests/*.sh pushed one shingle past the >6-owner idiom suppression.
.github/actions/README.md | .github/actions/README.md | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 1 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in the same file, not a copied block. Budget pinned so a real copy still trips it. Re-measured 13 -> 16 on 2026-09-07: the prepare-windows-container-host section grew to document the two inputs it gained (submodules, measure-data-root) in the same words its Linux-twin section already uses -- twin actions described in twin prose, longest identical run still 0 line(s). Re-measured 16 -> 11 on 2026-09-08: the four `image` input lines stopped being four copies of "`image` (required)" and now each point at the one "The two images" section, which is exactly the duplication this budget was measuring
linux/scripts/03-media/build/onnxruntime/build/30-build-native.sh | linux/scripts/03-media/build/onnxruntime/build/lib/common.sh | 13 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/cross-gcc.sh | linux/scripts/01-core/cross-meson.sh | 13 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/02-toolchain/packaging-deps.sh | linux/scripts/02-toolchain/packaging-deps.sh | 13 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 6 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/01-core/cross-apt.sh | linux/scripts/03-media/build/gstreamer/common/pre-setup.sh | 13 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/core/common.sh | linux/scripts/03-media/runtime/install-deps.sh | 13 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/02-toolchain/build-clang.sh | linux/scripts/02-toolchain/build-clang.sh | 13 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/02-toolchain/build-gcc.sh | linux/scripts/02-toolchain/gcc.sh | 13 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/armnn/build-armnn.sh | linux/scripts/03-media/build/opencv/build-opencv.sh | 13 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned. F3 2026-09-05: 13 -> 14 with BOTH files unchanged. Extracting report_onnx_build_output dropped three copies of the wheel-listing and capped library-listing shingles below the >6-owner idiom cutoff, which unsuppressed them here. Re-read, not re-suppressed: longest identical run is still 1 line(s) of tolerant redirect-and-continue guard idiom, not a copied block. 14 -> 13 on 2026-09-05 with BOTH files unchanged, same cause as the platform.sh <-> smoke-common.sh row: a new file became a seventh owner of one shingle and suppressed it
linux/scripts/03-media/build/gstreamer/common/build-gstreamer-monorepo.sh | linux/scripts/03-media/build/libcamera/build-libcamera.sh | 13 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/litert/build-litert.sh | linux/scripts/03-media/runtime/repair-wheels.sh | 13 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/03-media/runtime/verify-wheels.sh | linux/scripts/03-media/runtime/verify-wheels.sh | 17 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned. F3 2026-09-04: measurement rose from 13 without an edit to either file — the lib/*.sh logging preamble moved to lib/log-bootstrap.sh, so its shingles dropped from 9+ owners to under MAX_OWNERS=6 and stopped being suppressed as idiom. Re-measured: still 6 lines at most, both ends a wheel-tag loop over ${WHEELS_DIR}; verdict unchanged
linux/scripts/build-cross-chain.sh | linux/scripts/build-cross-compiler.sh | 13 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/Dockerfile.android | linux/Dockerfile.media | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 10 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows Re-measured 2026-09-04: -1 when Dockerfile.android's PATH left the ENV block it could not read (docs/code-quality-tooling.md#env-instruction-ordering-dockerfile-lint).
linux/Dockerfile.media | linux/Dockerfile.sdk | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/Dockerfile.torch | linux/llm-stack/Dockerfile | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 0 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/common.sh | linux/scripts/01-core/cross-python.sh | 11 | 12 -> 11 on 2026-09-08 by MEASUREMENT, with BOTH files unchanged and nothing de-duplicated between them: the new shared/config/sync-shared-config.sh became the SEVENTH holder of the shingle `"S" return N fi if [ - n "S" ] ; then` — common.sh:269, common.sh:601, cross-python.sh:97, 01-core/platform.sh:301, 06-packaging/smoke-toolchain.sh:153, lib/agentic-engines.sh:98 and now shared/config/sync-shared-config.sh:328 — so MAX_OWNERS=6 now reads it as idiom and stops counting it for this pair. Proven, not inferred: re-indexing the corpus with that one file dropped puts the pair back at exactly 12. This is the F3 2026-09-05 effect recorded below running the other way, and the budget follows the measurement DOWN either way so the shrink cannot be spent on a real copy later. PRIOR: reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it. F3 2026-09-05: 11 -> 12 with BOTH files unchanged. Dropping the two ffmpeg/pyav copies of the launcher block took shared shingles from >6 owners to fewer, unsuppressing them here -- the fourth instance of that effect in three waves. Re-read, not re-suppressed: llvm_wanted_major and cross_target_python_major_minor are both `first non-empty env var wins, then a default` ladders over DIFFERENT variables with different defaults, so the long identical run is the shape of the ladder and nothing else
linux/host-config/ghcr-delete-tags.sh | linux/host-config/ghcr-prune-package.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/host-config/ghcr-prune-package.sh | linux/scripts/03-media/build/litert/install-litert-web.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/host-config/install-nerdctl-full.sh | linux/scripts/03-media/build/litert/install-litert-web.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/tests/test-tvm-cmake-args.sh | linux/scripts/tests/test-tvm-wheel-diagnostics.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/01-core/compiler-resolution.sh | linux/scripts/03-media/build/gstreamer/common/build-gst-plugins-rs.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/02-toolchain/android-sdk.sh | linux/scripts/06-packaging/setup-package-image.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/01-core/install-tensorrt.sh | linux/scripts/03-media/build/litert/build-litert.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/litert/build-litert.sh | linux/scripts/03-media/build/onnxruntime/build/30-build-native-nvidia.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/python_uv.sh | linux/scripts/02-toolchain/python/ci-common.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/setup-cuda-repo.sh | linux/scripts/01-core/setup-rocm-repo.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 1 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/06-packaging/setup-package-image.sh | linux/scripts/06-packaging/setup-torch-venv.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/02-toolchain/build-gcc.sh | linux/scripts/02-toolchain/rust/version_util.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/02-toolchain/gcc.sh | linux/scripts/02-toolchain/rust/version_util.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/build-cross-chain.sh | linux/scripts/stop-cross-chain.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/armnn/install-deps.sh | linux/scripts/03-media/build/ffmpeg/install-deps.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/06-packaging/setup-torch-venv.sh | linux/scripts/06-packaging/swap-native-gcc.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/06-packaging/smoke-runtime-image.sh | linux/scripts/tests/test-genai-smoke-payload.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/build-cross-chain.sh | linux/scripts/build-sdk-artifacts.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/build-cross-compiler.sh | linux/scripts/build-cross-stage.sh | 12 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 8 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/Dockerfile.sdk | linux/Dockerfile.toolchain | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 9 line(s). Small enough that a helper would cost more indirection than it saves, large enough to watch — budget pinned so growth trips the gate. NOT read line-by-line; revisit if it grows
linux/scripts/01-core/apply-patch.sh | linux/scripts/04-runtime/libcamera-env.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/02-toolchain/llvm.sh | linux/scripts/03-media/core/common.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/cross-gcc.sh | linux/scripts/03-media/build/gstreamer/common/build-gst-plugins-rs.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/cross-meson.sh | linux/scripts/03-media/build/gstreamer/common/build-gst-plugins-rs.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/build-helpers.sh | linux/scripts/03-media/build/onnxruntime/build/30-build-native.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/build-helpers.sh | linux/scripts/03-media/runtime/repair-wheels.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/common.sh | linux/scripts/tests/test-compiler-cache.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/compiler-resolution.sh | linux/scripts/03-media/runtime/configure-runtime.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/ffmpeg/ffmpeg-probe-framework.sh | linux/scripts/03-media/runtime/configure-runtime.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/compiler-resolution.sh | linux/scripts/01-core/compiler-resolution.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/01-core/setup-rocm-repo.sh | linux/scripts/02-toolchain/android-sdk.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/cross-env.sh | linux/scripts/02-toolchain/llvm.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/modules.sh | linux/scripts/01-core/modules.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in the same file, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/01-core/package-lists.sh | linux/scripts/03-media/runtime/verify-wheels.sh | 14 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it. F3 2026-09-04: measurement rose from 11 without an edit to either file — the lib/*.sh logging preamble moved to lib/log-bootstrap.sh, so its shingles dropped from 9+ owners to under MAX_OWNERS=6 and stopped being suppressed as idiom. Re-measured: 5 lines, still structural; verdict unchanged
linux/scripts/01-core/qnn-sdk.sh | linux/scripts/03-media/runtime/verify-media-artifacts.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/02-toolchain/android-sdk.sh | linux/scripts/02-toolchain/install-rust.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/02-toolchain/install-rust.sh | linux/scripts/06-packaging/setup-package-image.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/03-media/build/ffmpeg/build-ffmpeg.sh | linux/scripts/03-media/build/gstreamer/common/build-gstreamer-stage.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 4 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/03-media/build/ffmpeg/ffmpeg-probe-framework.sh | linux/scripts/setup-rootless-binfmt.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/04-runtime/libcamera-env.sh | linux/scripts/06-packaging/smoke-media.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (fi/esac/}/echo/return). That is the shape of sibling functions in these two, not a copied block. Budget pinned so a real copy still trips it
linux/scripts/06-packaging/smoke-common.sh | linux/scripts/06-packaging/smoke-common.sh | 11 | REVIEWED 2026-08-31 KEEP: the smoke_genai_py tier emitters share their heredoc wrapper by construction; that IS the decomposition
linux/scripts/setup-rootless-binfmt.sh | linux/scripts/setup-rootless-binfmt.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-chain-lifecycle.sh | linux/scripts/tests/test-chain-lifecycle.sh | 18 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 3 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned 11 -> 18 on 2026-09-07: the DISK3 wiring cases are the buildkit cases' shape over the image lever -- one asks whether a reclaimed STORE keeps the cache exports on, the other whether a reclaimed IMAGE STORE does. Two gates, two fixtures; longest identical run 2 lines.
linux/scripts/tests/test-iree-wheelhouse-stages.sh | linux/scripts/tests/test-iree-wheelhouse-stages.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-module-resolution.sh | linux/scripts/tests/test-module-resolution.sh | 11 | reviewed 2026-09-02 by MEASUREMENT: longest shared run 5 line(s) — below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned

# 2026-09-01: twins created or grown by the prevention-gate work. Reasons are
# specific; the two worth merging are recorded in docs/refactoring-backlog.md F3.
linux/scripts/06-packaging/smoke-torch-venv.sh | linux/scripts/06-packaging/smoke-torch-venv.sh | 58 | assert_pinned_versions / assert_app_venv_parity: each gate must read standalone in its own failure output. F1/F2 2026-09-05: 60 -> 58 with this file UNCHANGED -- the agentic-loop split added a ninth owner of two generic shingles, pushing them past the >6-owner idiom cutoff so they no longer count here. The mirror image of the unsuppressions the last two waves recorded; the pair itself is unchanged
linux/scripts/tests/test-disk-guard.sh | linux/scripts/tests/test-disk-guard.sh | 26 | two fixture builders; explicit beats shared in a test that must not hide its own setup
linux/scripts/01-core/disk-guard.sh | linux/scripts/01-core/disk-guard.sh | 15 | keep_n arg validation in the trim and the watch entry points; both are public and validate independently 2026-09-07 (2nd pass, same cascade): the parallelism/gst-rs extractions shifted the corpus again; measured DOWN once more and recorded, never re-suppressed.
linux/scripts/build-cross-chain.sh | linux/scripts/build-cross-chain.sh | 15 | numeric-knob guards in the lane gate and the disk watcher; same idiom, different knobs 2026-09-07: budget lowered with the DISK3 image lever in the corpus -- more owners for the df/threshold preamble pushed shingles under the idiom cutoff; re-read, not re-suppressed. RE-MEASURED 2026-09-07 after DISK3: extracting _disk_guard_lever_ready and _disk_guard_lever_needed took this block from >6 owners to fewer, which UNSUPPRESSED shingles that were never findings -- the same effect the log-bootstrap and ORT-summary extractions recorded. Read, not re-suppressed: longest identical run is 2-3 lines of local decls plus the `case ''|*[!0-9]*` numeric guard. 2026-09-07 (2nd pass, same cascade): the parallelism/gst-rs extractions shifted the corpus again; measured DOWN once more and recorded, never re-suppressed.
linux/scripts/01-core/cross-stage-build.sh | linux/scripts/build-cross-chain.sh | 18 | the free-space-vs-threshold guard; same idiom, different knob and failure text 2026-09-07: budget lowered with the DISK3 image lever in the corpus -- more owners for the df/threshold preamble pushed shingles under the idiom cutoff; re-read, not re-suppressed. RE-MEASURED 2026-09-07 after DISK3: extracting _disk_guard_lever_ready and _disk_guard_lever_needed took this block from >6 owners to fewer, which UNSUPPRESSED shingles that were never findings -- the same effect the log-bootstrap and ORT-summary extractions recorded. Read, not re-suppressed: longest identical run is 2-3 lines of local decls plus the `case ''|*[!0-9]*` numeric guard. 2026-09-07 (2nd pass, same cascade): the parallelism/gst-rs extractions shifted the corpus again; measured DOWN once more and recorded, never re-suppressed.
linux/scripts/01-core/disk-guard.sh | linux/scripts/build-cross-chain.sh | 17 | as above: the caller re-validates the knob it passes in 2026-09-07: budget lowered with the DISK3 image lever in the corpus -- more owners for the df/threshold preamble pushed shingles under the idiom cutoff; re-read, not re-suppressed. RE-MEASURED 2026-09-07 after DISK3: extracting _disk_guard_lever_ready and _disk_guard_lever_needed took this block from >6 owners to fewer, which UNSUPPRESSED shingles that were never findings -- the same effect the log-bootstrap and ORT-summary extractions recorded. Read, not re-suppressed: longest identical run is 2-3 lines of local decls plus the `case ''|*[!0-9]*` numeric guard. 2026-09-07 (2nd pass, same cascade): the parallelism/gst-rs extractions shifted the corpus again; measured DOWN once more and recorded, never re-suppressed.
linux/scripts/build-cross-chain.sh | linux/scripts/tests/test-chain-lifecycle.sh | 11 | the test EVALs the shipped function; overlap with the implementation is the point
linux/scripts/tests/test-chain-lifecycle.sh | linux/scripts/tests/test-parallel-loop.sh | 19 | is_truthy stub: a test that sources platform.sh could no longer prove the shipped copy
linux/scripts/tests/test-chain-lifecycle.sh | linux/scripts/tests/test-ffmpeg-dnn-contract.sh | 14 | is_truthy stub, as above
linux/scripts/01-core/platform.sh | linux/scripts/tests/test-chain-lifecycle.sh | 18 | is_truthy: the test stub deliberately mirrors the canonical definition
linux/scripts/01-core/runtime-build-fns.sh | linux/scripts/build-cross-chain.sh | 14 | reviewed 2026-09-02: `usage() { cat <<'EOF'` plus two option lines the chain orchestrator documents identically. NOT deliberate so much as unfinished — build-cross-chain.sh could call runtime_shared_usage_options too; it was left out because it is a different flow (stage chain, not runtime) and merging its help needs its own review. Budget pinned so the overlap cannot grow
docs/upstream/windows-containers-lsm-session-event-hang.md | docs/upstream/windows-containers-lsm-session-event-hang.md | 16 | reviewed 2026-09-02: the file repeats ITSELF -- the same Windows-Containers issue reference appears in the summary at the top and again in the reference list at the bottom, which is how an upstream write-up is supposed to read. It is a WINDOWS-lane document and the standing rule here is to note, not edit, Windows content from the Linux lane, so the budget is pinned rather than the prose reflowed
linux/scripts/01-core/compiler-resolution.sh | linux/scripts/03-media/android-build-preamble.sh | 63 | 64 -> 63 on 2026-09-05 (android ABI knob): reviewed 2026-09-02: the BOOTSTRAP PARADOX. android-build-preamble.sh sources the canonical 01-core/compiler-resolution.sh when present and falls back to an inline copy when it is not — a fallback that must duplicate the thing it stands in for, or it is not a fallback. Consolidating the iree+litert copies into the preamble did not create this overlap, it made a pre-existing one visible in ONE place instead of two F3 VERDICT 2026-09-07, per-CONSUMER like the gstreamer-env/libcamera-env pair: the fallback in ffmpeg-probe-framework.sh is reachable and the one in android-build-preamble.sh is not. build-ffmpeg.sh reaches resolve_host_compiler_for_lang only through media_common_init -> `source_module compiler-resolution.sh || true`, which TOLERATES an absent module, so the inline ladder is what runs on a host checkout -- LIVE. Dockerfile.android COPYs 01-core/compiler-resolution.sh to /opt/scripts/core (Dockerfile.android:96), so the preamble's `[ -f ]` branch always wins in the image -- its fallback is DEAD there and live only on a host checkout, which is the same shape the gstreamer-env pair was KEPT for. KEEP both: deleting a fallback that stands in for the module it duplicates is a build-closure edit no gate can prove, and the branch costs one [ -f ] per script start.
linux/scripts/03-media/android-build-preamble.sh | linux/scripts/03-media/build/ffmpeg/ffmpeg-probe-framework.sh | 29 | 30 -> 29 on 2026-09-05 (android ABI knob): reviewed 2026-09-02: same host-compiler-preference logic, fourth site. See the compiler-resolution entry above — this is a FAMILY (canonical + preamble fallback + ffmpeg probe + iree native), not a pair, and wants one owner
linux/scripts/01-core/abseil-headers.sh | linux/scripts/06-packaging/copy-media-payloads.sh | 16 | F3 2026-09-04: first-ever measurement — suppressed as idiom (>6 owners) until the lib/*.sh preamble moved to lib/log-bootstrap.sh. READ: 3 lines, the defensive-logger `if ! command -v warn; then warn() { printf '[WARN] %s\n' "$*" >&2; }; fi` guard. Same family as the abseil<->build-app-wheelhouse row above and KEPT for the same reason; copy-media-payloads.sh carries only warn, so there is not even a second function to share
linux/scripts/05-frameworks/torch/build-app-wheelhouse.sh | linux/scripts/06-packaging/copy-media-payloads.sh | 16 | F3 2026-09-04: first-ever measurement, same unsuppression and same defensive-logger family as the two rows above; 3 lines, the warn guard. KEEP
linux/scripts/01-core/abseil-headers.sh | linux/scripts/lib/log-bootstrap.sh | 22 | F3 2026-09-04: first-ever measurement, created BY the F3 extraction. 2-line longest run: both files guard a logger definition before defining it. That guard IS the bootstrap, so the owner cannot own it; and log-bootstrap.sh is outside the build closure while abseil-headers.sh is inside it. KEEP
linux/scripts/05-frameworks/torch/build-app-wheelhouse.sh | linux/scripts/lib/log-bootstrap.sh | 11 | F3 2026-09-04: first-ever measurement, created BY the F3 extraction; 2-line logger guard, same verdict as the abseil<->log-bootstrap row. KEEP
linux/scripts/06-packaging/copy-media-payloads.sh | linux/scripts/lib/log-bootstrap.sh | 11 | F3 2026-09-04: first-ever measurement, created BY the F3 extraction; 2-line logger guard, same verdict as the abseil<->log-bootstrap row. KEEP
linux/scripts/tests/test-dockerfile-env-order.sh | linux/scripts/tests/test-doc-links.sh | 15 | reviewed 2026-09-04 by MEASUREMENT: longest shared run 5 line(s) -- the standard suite preamble (set -u, TESTS_DIR, source test-harness.sh, REPO_ROOT) every file in linux/scripts/tests carries. It cannot live in the harness it bootstraps. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-crlf-guard.sh | linux/scripts/tests/test-dockerfile-env-order.sh | 12 | reviewed 2026-09-04 by MEASUREMENT: longest shared run 4 line(s) -- the standard suite preamble (set -u, TESTS_DIR, source test-harness.sh, REPO_ROOT) every file in linux/scripts/tests carries. It cannot live in the harness it bootstraps. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-runtime-image-gates.sh | linux/scripts/tests/test-setup-package-image.sh | 12 | reviewed 2026-09-04, READ: the 7-line run is seven consecutive `t_assert_contains "${_out}" "..." "..."` calls. The shingler normalises every string literal to S, so any two blocks of N assertions in a row read identical no matter what they assert -- these two assert the consumer-contract verdicts and the flutter bootstrap handover, which share nothing. An artefact of normalisation, not a copy. Budget pinned so a real copied block still trips it
linux/scripts/tests/test-doc-links.sh | linux/scripts/tests/test-doc-links.sh | 11 | reviewed 2026-09-05 by MEASUREMENT: longest shared run 4 line(s) — the t_case body every case in this suite carries (_fixture, printf the subject file, t_assert_eq "0" on the rc, rm -rf). The two cases it pairs assert different facts (a pointer on body line 11 is not a header vs a .mk fragment has no header rule); folding them behind a helper would hide which fact each one owns. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-doc-links.sh | linux/scripts/tests/test-shellcheck-warnings.sh | 11 | reviewed 2026-09-05 by MEASUREMENT: longest shared run 1 line(s) — both suites plant an allow file beside their copied gate and both spell that _freeze, but they write DIFFERENT files (doc-header-pointers.allow vs shellcheck-warnings.allow) into their own fixture tree. A shared name over one line, with no shared body to own. Budget pinned
linux/scripts/tests/test-mutation-gate.sh | linux/scripts/tests/test-version-snapshot.sh | 12 | reviewed 2026-09-05 by MEASUREMENT: longest shared run 4 line(s), and it is the harness bootstrap (set -u, TESTS_DIR, source test-harness.sh, REPO) — the code that FINDS the shared helper cannot live inside it. Budget pinned so a real copy still trips it
linux/scripts/tests/test-uv-conflict-extras.sh | linux/scripts/tests/test-uv-conflict-extras.sh | 13 | reviewed 2026-09-05 by MEASUREMENT: longest shared run 4 line(s), and the identical run IS the assertion — the inline-layout case claims "read exactly like the multi-line layout", which is only proven by spelling out the SAME expected groups against the same input. Folding the two into a shared asserter would hide the very equality under test. Budget pinned
linux/Dockerfile.package | linux/Dockerfile.toolchain | 13 | reviewed 2026-09-05 by MEASUREMENT: longest identical run 0 line(s) — the shingles are the BuildKit cache-mount idiom (`--mount=type=cache,target=...,id=...-${TARGETARCH}`), which both stages must spell out per RUN because a mount cannot be factored out of a Dockerfile instruction. The overlap grew when the package stage gained the same cargo registry mount the toolchain stage already had, for the same reason: cargo install must not re-fetch its crates. Budget pinned so a real copied block still trips it
linux/scripts/lib/app-packaging.sh | linux/scripts/lib/app-packaging.sh | 35 | reviewed 2026-09-05 by MEASUREMENT after extracting app_packaging_resolve_bundle_facts (56 -> 35): the six facts all three packagers derived identically now have ONE owner. What is left is the shape of a bundle packager — the two required positional args, and `return 1 / fi / app_packaging_assert_artifact / }` at the end. Longest identical LINE run measured with difflib on stripped lines: 4, all structural. Budget pinned so a real copy still trips it
linux/scripts/tests/test-mutation-gate.sh | linux/scripts/tests/test-mutation-gate.sh | 14 | reviewed 2026-09-05 by MEASUREMENT: longest identical run 3 line(s), the t_case + _fixture preamble each case needs to state its own scenario. Folding it would hide which fixture a case runs against, which is the thing under test. Budget pinned
linux/scripts/tests/test-cmake-format.sh | linux/scripts/tests/test-shared-config.sh | 15 | reviewed 2026-09-07 by MEASUREMENT: longest shared run 4 line(s) -- the standard suite preamble plus the t_fn_src/mktemp/trap fixture idiom every preflight-gate suite carries. It cannot live in the harness it bootstraps. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-cmake-format.sh | linux/scripts/tests/test-crlf-guard.sh | 12 | reviewed 2026-09-07 by MEASUREMENT: longest shared run 4 line(s) -- the standard suite preamble (set -u, TESTS_DIR, source test-harness.sh, REPO_ROOT) every file in linux/scripts/tests carries. It cannot live in the harness it bootstraps. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-cmake-format.sh | linux/scripts/tests/test-doc-links.sh | 12 | reviewed 2026-09-07 by MEASUREMENT: longest shared run 4 line(s) -- the standard suite preamble (set -u, TESTS_DIR, source test-harness.sh, REPO_ROOT) every file in linux/scripts/tests carries. It cannot live in the harness it bootstraps. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-cmake-format.sh | linux/scripts/tests/test-dockerfile-env-order.sh | 12 | reviewed 2026-09-07 by MEASUREMENT: longest shared run 4 line(s) -- the standard suite preamble (set -u, TESTS_DIR, source test-harness.sh, REPO_ROOT) every file in linux/scripts/tests carries. It cannot live in the harness it bootstraps. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-crlf-guard.sh | linux/scripts/tests/test-shared-config.sh | 12 | reviewed 2026-09-07 by MEASUREMENT: longest shared run 4 line(s) -- the standard suite preamble (set -u, TESTS_DIR, source test-harness.sh, REPO_ROOT) every file in linux/scripts/tests carries. It cannot live in the harness it bootstraps. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-doc-links.sh | linux/scripts/tests/test-shared-config.sh | 12 | reviewed 2026-09-07 by MEASUREMENT: longest shared run 4 line(s) -- the standard suite preamble (set -u, TESTS_DIR, source test-harness.sh, REPO_ROOT) every file in linux/scripts/tests carries. It cannot live in the harness it bootstraps. A shared idiom, not a copy. Budget pinned
linux/scripts/tests/test-dockerfile-env-order.sh | linux/scripts/tests/test-shared-config.sh | 12 | reviewed 2026-09-07 by MEASUREMENT: longest shared run 4 line(s) -- the standard suite preamble (set -u, TESTS_DIR, source test-harness.sh, REPO_ROOT) every file in linux/scripts/tests carries. It cannot live in the harness it bootstraps. A shared idiom, not a copy. Budget pinned
linux/scripts/lint-dockerfiles.sh | linux/scripts/lint-python.sh | 13 | reviewed 2026-09-08 by MEASUREMENT: longest shared run 4 line(s), and it is the CALL of a shared owner, not a copy of one — 01-core/lint-root.sh holds the --root parsing, resolution and git-ls-files scope for all three lint gates; what repeats is `. lint-root.sh` / `lint_root_begin` / `SCAN_ROOT=` / `set --`. Extracting a call site would need another indirection to hide four lines. Budget pinned so a real copy still trips it
linux/scripts/lint-python.sh | linux/scripts/lint-shell.sh | 13 | reviewed 2026-09-08 by MEASUREMENT: longest shared run 4 line(s) — the same shared-owner call as the row above (01-core/lint-root.sh). Budget pinned
linux/scripts/tests/test-dockerfile-env-order.sh | linux/scripts/tests/test-python-lint-gate.sh | 21 | reviewed 2026-09-08 by MEASUREMENT: longest shared run 4 line(s). The fixture itself has one owner already (t_consumer_fixture / t_git_commit in test-harness.sh); what repeats is its one-line call plus the three REFUSALS every root-taking gate owes — a root that is not a git checkout, a root that does not exist, --root with no value. Those must be asserted per gate: asserting them once would leave two gates unproven, which is the exact shape this repo keeps finding. Budget pinned
linux/scripts/tests/test-dockerfile-env-order.sh | linux/scripts/tests/test-lint-shell-scope.sh | 17 | reviewed 2026-09-08 by MEASUREMENT: longest shared run 4 line(s) — the same per-gate refusal assertions as the row above. Budget pinned
linux/scripts/tests/test-lint-shell-scope.sh | linux/scripts/tests/test-python-lint-gate.sh | 17 | reviewed 2026-09-08 by MEASUREMENT: longest shared run 3 line(s) — the same per-gate refusal assertions as the row above. Budget pinned

# --- PowerShell lane, frozen 2026-09-08 (ITEM 9) ------------------------------
# windows/ held 50,862 lines of .ps1/.psm1 that no structural gate scanned. The
# rows below are that lane's duplication AS MEASURED the day the gate learned to
# read PowerShell: nothing was fixed to get here, and nothing may grow from here.
# Same contract as every row above -- the budget EQUALS the measurement, so a
# shrink must be recorded and a pair that stops overlapping must be deleted.
windows/scripts/tests/SourceBuild.PinParity.Tests.ps1 | windows/scripts/tests/SourceBuild.PinParity.Tests.ps1 | 235 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. READ 2026-09-09, and this is real logic, not test data: Get-PinParitySite (line 90) and Get-RcivSite (line 357) share a 20-line VERBATIM run — the CommandElements walk that pairs each CommandParameterAst with its argument (attached -Name:value or the next element), plus the whole 'DefaultValue' switch arm. That block contains no domain content at all; it is PowerShell AST mechanics. The two scanners' real difference is three lines further down (-EnvironmentVariables is an array scanned with a nested-literal FindAll, -EnvironmentVariable is a single literal that must NOT be, and StripVPrefix vs TrimVPrefix), and the file already documents that difference in a comment. One owner leaves exactly those differences visible, which is what the reader wants. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/modules/WindowsFormatting.Common.psm1 | windows/scripts/modules/WindowsFormatting.Common.psm1 | 134 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Get-ProjectCmakeFiles and Get-ProjectCppFiles share 29 lines of real logic in two contiguous runs (18 lines at 23-42 vs 68-99, 11 lines at 44-55 vs 101-112): the same git ls-files fast path with the same four-regex exclusion list, and the same Get-ChildItem -Recurse fallback with the same seven-regex exclusion list, both ending in Sort-Object -Unique. Only the pathspec list and one file predicate differ. This is exclusion POLICY, not shape, and the copies have already drifted once with a real cost: the file's own comment records that the git-path _deps regex in the C/C++ copy read -match instead of -notmatch until 2026-07-20, which kept only files under _deps/ and so silently formatted zero files. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/diagnostics/Test-Sccache2726Repro.ps1 | windows/scripts/diagnostics/Test-SccachePrHygiene.ps1 | 124 | reviewed 2026-09-09, READ: the block is the proposed-PR build recipe (pin + patch dir), and Test-Sccache2726Repro.ps1:32 declares the copy in its own comment - "same recipe as the hygiene probe / Install-RustToolchain.ps1". These are two settled sccache investigation probes that must each stay runnable on their own years later; a shared owner would couple two frozen reproductions to one another and to a third file. Surfaced on 2026-09-09 by the MAX_OWNERS=6 idiom cutoff, not newly written. Budget at the measurement
windows/scripts/tests/WindowsAgenticLoop.Common.Tests.ps1 | windows/scripts/tests/WindowsAgenticLoop.Common.Tests.ps1 | 101 | reviewed 2026-09-09, READ: longest shared run 8 line(s), and the run is the agentic-loop CONFIG LITERAL — the intervals/git/build hashtable that the Context BeforeAll (line 462) and the two engine cases (lines 495, 511) each spell out before calling Invoke-AgenticLoop. The config IS the input under test: one case pins the claude engine, the next an override to opencode, and the shared middle (buildEveryNTasks/maxIterations/loopDelaySeconds) is the neutral background they both need held constant. A builder taking an override per case would need one parameter for every field a future case wants to vary, and the case would then read as a call whose subject is invisible. Budget pinned so a real copy still trips it
windows/scripts/build/Test-Container.ps1 | windows/scripts/modules/WindowsContainerImage.Common.psm1 | 94 | reviewed 2026-09-09, READ: this is the SOURCE-OR-FALLBACK shape, and the file's own header already declares it. Test-Container.ps1:89 says 'Imported conditionally with local fallbacks so the suite stays runnable in an image whose modules dir is broken. The fallbacks mirror the module functions exactly -- keep them in sync.' The module IS the one owner (WindowsContainerImage.Common.psm1:14 Resolve-ContainerImageValue, :51 Resolve-VsBuildToolsRoot); Test-Container.ps1:91-93 imports it and only defines the twins behind `if (-not (Get-Command ... -ErrorAction SilentlyContinue))`, so exactly one definition is ever live. The 94 shingles are Resolve-ContainerImageValue (longest run 7 lines) plus Resolve-VsBuildToolsRoot (43 shingles, run 5). Same class the linux gstreamer-env/libcamera-env and compiler-resolution rows were kept for: a fallback must duplicate what it stands in for, and deleting half of a bootstrap pair is a build-closure edit no gate can prove. Budget pinned so a THIRD copy, or drift in the guard, still trips it
windows/scripts/tests/SourceBuild.VerifyTargetArch.Tests.ps1 | windows/scripts/tests/SourceBuild.VerifyTargetArch.Tests.ps1 | 85 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. READ 2026-09-09: the ar (COFF archive) MEMBER HEADER is written out by hand four times — once inside the $script:NewArchive fixture (lines 65-69) and three more times inline, twice in the linker-member case (lines 189-193 and 198-202) and once in the malformed-size case (lines 248-252). All four are the same five `$bw.Write([System.Text.Encoding]::ASCII.GetBytes(<name>.PadRight(16)))` / PadRight(12) / PadRight(6) / PadRight(6) / '100644'.PadRight(8) lines, and those widths are the ar spec, not test data: getting one of them wrong makes the fixture silently unparseable and the case vacuously green. This is the one block in the file that has a name ("the ar member header") and exactly one correct spelling. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
linux/scripts/lib/agentic-loop.sh | windows/scripts/modules/WindowsAgenticLoop.Common.psm1 | 74 | reviewed 2026-09-08: the first cross-LANE twin this gate has ever been able to see, and it is real -- invoke_build_fixer (bash) and Invoke-AgentProcess (PowerShell) are the same agentic loop written twice. Longest identical run is 2 lines because one is bash and the other PowerShell; neither language can call the other's helper, so there is no shared owner to extract to. Frozen, not excused: the budget is the measurement, so the next paste into either side fails
windows/scripts/diagnostics/Get-HostLsm.ps1 | windows/scripts/diagnostics/Get-SiloProcesses.ps1 | 16 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Longest identical run 8 lines (Get-HostLsm.ps1:97-104 / Get-SiloProcesses.ps1:50-57) and it is real setup logic, not shape: the bait-container block -- buildctl path check, nonce, $env:TEMP bait dir, the four-line `ARG BASE / FROM ${BASE} / ARG NONCE / RUN echo bait-$NONCE` here-string, and the eight-argument buildctl Start-Process. The two copies differ in exactly two tokens: the temp-dir prefix (hostlsm- vs silo-) and the image tag (diag-hostlsm- vs diag-silo-). One `-Tag` parameter covers both. On top of that the same two files share the cdb.exe discovery and the OutDir default verbatim. This is the densest cluster in the diagnostics scope and one module retires ten rows. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/diagnostics/Invoke-LlvmAarch64Layout.ps1 | windows/scripts/diagnostics/Invoke-LlvmAarch64Layout.ps1 | 68 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 3 line(s) within this 544-line file (127-129 vs 135-137), and those lines are structural only (closing braces and a Write-Host). The 68 shingles are spread across nine sibling functions -- Split-CommandLine, Get-CodegenFlags, Resolve-NinjaCommand, Invoke-Capture, Get-BaselineClangCl, Invoke-Arm, Invoke-Run -- which share a house style (param block, foreach over $Cases, per-case verdict accumulation) but no block. That is the shape of sibling functions in one file, not a copied block. A local helper would have to take the loop body as a scriptblock to serve any two of them. Budget pinned so a real copy still trips it.
windows/scripts/modules/WindowsSmokeTest.Common.psm1 | windows/scripts/modules/WindowsSmokeTest.Common.psm1 | 68 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. The module Add-Types the KataNativeProbe P/Invoke class TWICE (lines 261-271 inside Assert-NativeLinkRun's closure, lines 327-336 at the top of Assert-AllDllsLoad), 7 substantive lines shared - and the two copies are not identical: the second omits GetProcAddress. An Add-Type'd type is session-global and both sites guard with `if (-not ('KataNativeProbe' -as [type]))`, so whichever function runs first wins. Run a DLL sweep and then an Assert-NativeLinkRun with -Export and the GetProcAddress call lands on a type that does not have that method: a missing-method failure reported as a missing export. The duplication is not cosmetic, it is a live ordering bug. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/build/Build-LlvmFromSource.ps1 | 67 | reviewed 2026-09-09, READ: the shared 6-line run is compiler-rt discovery - resolving the LLVM install dir via clang-cl on PATH rather than a hardcoded prefix, which both need for lld-link (__udivti3). Build-Llvm holds it as Install-TargetCompilerRt:53; Build-Gstreamer inlines the resolution at :407 inside a larger stage. Worth an owner eventually, and it is on the deferred-extraction list; not done here because the two use it at different granularity (a function vs a step inside one). Surfaced by the MAX_OWNERS=6 cutoff on 2026-09-09. Budget at the measurement
windows/scripts/diagnostics/Test-SccacheWrite.ps1 | windows/scripts/diagnostics/Test-SccacheWrite.ps1 | 66 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) within this 889-line file (669-673 vs 804-808) -- the sccache server restart: --stop-server, the SCCACHE_DIR assignment, `Push-Location 'C:\'`, `try { --start-server / Where-Object { $_ -match 'Listening/error' } } finally { Pop-Location }`, --zero-stats. It repeats four times and the repeats are NOT interchangeable: two restore the captured originals ($origChain/$origEndpoint/$origDir) while two REMOVE the variables outright with `Remove-Item Env:\SCCACHE_MULTILEVEL_CHAIN`, and restore-vs-unset is precisely the axis this probe measures -- an inherited-but-empty variable and an absent variable are different inputs to sccache. A helper would need the dir, the exe, a restore-or-unset switch and the output filter, and every call site would still have to set up its own environment first. That is four parameters to save five lines, four times. The `Push-Location 'C:\'` around --start-server is load-bearing for the same reason it is in Test-SccacheRelativeFo.ps1: the server inherits its CWD and relative -Fo resolution depends on it. Budget pinned so a real new copy still trips it.
windows/scripts/tests/SourceBuild.Artifact.Tests.ps1 | windows/scripts/tests/SourceBuild.Artifact.Tests.ps1 | 65 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 5 line(s), and those five are `It "S" { Invoke-InTestDir { param($dir) / $bd = Join-Path $dir 'build' ... / Set-Content ... }` — the arrange half of a copy-by-extension case. The gate folds every string literal to "S", which is exactly the half that differs: a.dll/b.lib/deep.dll and the -Filter routing are the whole subject of each case. The cleanup is already owned (Invoke-InTestDir in TestHarness.psm1); what is left is three lines of New-Item plus the filenames the assertion names. Budget pinned
windows/scripts/diagnostics/Find-LsmEventHolder.ps1 | windows/scripts/diagnostics/Get-HostLsm.ps1 | 11 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Longest identical run 6 lines (Find-LsmEventHolder.ps1:115-120 / Get-HostLsm.ps1:99-104) -- the bait Dockerfile here-string and its Set-Content, inside the same ~20-line bait-container block described on the Get-HostLsm/Get-SiloProcesses row. Both files additionally carry the cdb.exe discovery walk and the out\lsm-attach OutDir default verbatim. Same single extraction; nothing here is language-mandated shape. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/diagnostics/Test-LayerRename.ps1 | windows/scripts/diagnostics/Test-ProcessIsolationCommit.ps1 | 52 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Longest identical run 9 lines (Test-LayerRename.ps1:60-68 / Test-ProcessIsolationCommit.ps1:61-69), byte-for-byte including the comment: the docker.exe candidate walk over $env:DOCKER_EXE, D:\Stevedore\bin\docker.exe and $env:ProgramFiles\Stevedore\bin\docker.exe, then Get-Command, then a throw. This is the ONE case in the diagnostics scope where the owner already exists and the callers simply never migrated: Get-PreferredToolPath in windows/scripts/modules/WindowsScripts.Shared.psm1:618 is exactly candidate-paths-then-PATH with a -Required switch for the throwing half, and Invoke-DiagnosticProbe.ps1:98 records the migration -- 'Central candidate walk instead of the pasted Stevedore-then-PATH block that used to live in every runner (backlog #101)'. #101 converted the buildctl walks and left the docker ones behind. Nine pasted lines become one call; no new parameter, no new file, no new concept. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/diagnostics/Test-OnnxTuReplay.ps1 | windows/scripts/diagnostics/Test-OnnxTuReplay.ps1 | 58 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 2 line(s) within this 330-line file (139-140 vs 159-160), one of them structural -- the `$tag = if ($_.SideIndicator -eq '<=') {...}` / `Write-Host (" {0}: {1}" -f $tag, $_.InputObject)` pair that formats a Compare-Object result. It appears twice with different labels (ONLY-IN-PLAN/ONLY-IN-EXEC at 137-140, LOST-BY-SCCACHE/ADDED-BY-SCCACHE at 159-162) because the two comparisons answer different questions -- one diffs the define token lists, the other the preprocessor command lines -- and the label pair IS the finding. A helper taking two label strings and a Compare-Object result would be longer than the two lines it replaces. Below any threshold worth extracting a helper for. Budget pinned.
windows/scripts/build/Test-Container.ps1 | windows/scripts/build/Test-Container.ps1 | 57 | reviewed 2026-09-09, READ: the generic half is ALREADY owned. The shared 6-line run (Test-Container.ps1:259/:326/:373) is `Assert-Test -Name .. -Condition { $dir = Join-Path ..; Initialize-SmokeScratch -Path $dir; ..; 'src' / Set-Content ..; }` -- and Assert-Test, Initialize-SmokeScratch and Skip-Test all live in WindowsSmokeTest.Common.psm1. What repeats is the CALL to those helpers; what differs is the whole point of each test (rustc compile+link, glslc shader->SPIR-V, nvcc PTX device code), each with its own source text, its own compiler invocation and its own artifact predicate. One owner would need Name/Source/Extension/Compiler/Args/ArtifactPredicate to serve three callers -- six parameters, and every caller passes a different value for all six. That is worse than three honest copies. Budget pinned
windows/scripts/diagnostics/Find-LsmEventHolder.ps1 | windows/scripts/diagnostics/Get-SiloProcesses.ps1 | 24 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Longest identical run 6 lines (Find-LsmEventHolder.ps1:115-120 / Get-SiloProcesses.ps1:52-57) -- the bait Dockerfile here-string and its Set-Content. Both also carry the cdb.exe walk, the OutDir default and the wininit-diff wait loop. Same single extraction. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/diagnostics/Test-OnnxTuReplay.ps1 | windows/scripts/diagnostics/Test-SccacheNvccInstantiation.ps1 | 56 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- `#requires -Version 7.0`, the two Kataglyphis/SPDX copyright lines, and the `<#` + `.SYNOPSIS` that opens comment-based help. None of that can move: the copyright header is what the license gate requires in every file and `#requires` is only honoured in the script that carries it. The remaining shingles are the in-container probe preamble (`param([string]$WorkDir,[string]$Nonce)`, `$ErrorActionPreference`, the `=== ... nonce=$Nonce ===` banner, `Import-Module 'C:\bkmnt\modules\WindowsSourceBuild.Common.psm1' -Force`, `Enter-VsDevCmdEnvironment`, `New-Item -Force -Path $WorkDir`, `Set-Location $WorkDir`) -- which is the CALL into an owner that already exists, not a block wanting one, plus the sccache server reset. That reset is deliberately unshared: every copy in this family carries a DIFFERENT SCCACHE_SERVER_PORT (4235 here, 4236 in the replay) because two probes on one port would attach to each other's server and grade the wrong binary. Budget pinned at the measurement so a real copied block still trips it.
windows/scripts/diagnostics/Test-OpencvCudaCmdshape.ps1 | windows/scripts/diagnostics/Test-Sccache2726Repro.ps1 | 56 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- `#requires -Version 7.0`, the two Kataglyphis/SPDX copyright lines and the `<#` + `.SYNOPSIS` opener, none of which can live outside the file it governs. The rest is the in-container probe preamble, whose only logic (`Import-Module 'C:\bkmnt\modules\WindowsSourceBuild.Common.psm1'` + `Enter-VsDevCmdEnvironment`) is already a call into an owner, plus the `C:\bkmnt\patch\*.patch` apply loop -- which Dockerfile.probe:16-21 records as reading a mount that #137 deleted, making Test-Sccache2726Repro 'dead by construction' pending backlog #159. Neither half is a dedup target. The sccache server reset the two share differs in the port by design (computed `43${i}0` per round here, `45${Slot}0` per candidate there) so the probes can run concurrently without colliding on one server. Budget pinned.
windows/scripts/modules/WindowsSourceBuild.Patches.psm1 | windows/scripts/modules/WindowsSourceBuild.Patches.psm1 | 54 | self-pair: Invoke-InlineRegexPatch and Edit-SourceFile share a param block and the read/transform/write frame. corpus effect of the 2026-09-08 deletion pass, not new code: twelve files left the tree, so a shingle these two share lost an owner and fell back under the MAX_OWNERS=6 idiom cutoff, which re-counts it against the pair. Neither file was edited. Re-measured, not chosen.
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/build/Build-GstreamerFromSource.ps1 | 51 | reviewed 2026-09-09, READ: the 10-line run is a MESON INI SCHEMA, not logic -- the `[binaries] c=/cpp=/ar=/strip=/windres=/pkg-config=/cmake=` block of the cross file (Build-GstreamerFromSource.ps1:927) and of the native file (:985). Meson mandates the section and the key names; that is the shape a meson machine file has, and it cannot live anywhere but in the here-string that becomes the file. The two are DELIBERATELY independent and the code says why (#128 comment at :949): a cross file describes the host machine only, so the build machine had no C compiler at all and libnice/webrtc went missing -- the native file exists precisely to carry the SAME compilers WITHOUT --target. They then diverge completely (cross adds needs_exe_wrapper + [host_machine]; native adds [built-in options] link args, /vctoolsdir:, /winsdkdir:). One emitter would need a mode flag per section, and coupling them is the exact bug #128 fixed. Budget pinned
windows/scripts/tests/Modules.ReExport.Tests.ps1 | windows/scripts/tests/Modules.ScriptCallClosure.Tests.ps1 | 50 | reviewed 2026-09-09, READ: longest shared run 5 line(s), and the run lives INSIDE a `@"..."@` here-string that the gate tokenises as PowerShell rather than as the string literal it is (the normalised form comes out mangled: `$ ( $V = @ ( "S" , "S" ) / Where-Object ...`). Both suites embed a probe script and hand it to a CLEAN child `pwsh -NoProfile -NonInteractive`, deliberately, so the parent session's already-imported modules cannot mask a missing export — that shared last line is the point, not an accident. What each probe CONTAINS is different: ReExport imports one .psm1 and asserts its re-exports resolve, ScriptCallClosure builds a per-script import list and asserts the closure a build script actually needs. A shared owner would take the import lines, the name list and the failure text, and it would sit in the harness where one bug in it greens BOTH closure gates at once — the exact coupling these two are kept apart to avoid. Budget pinned
windows/scripts/tests/SourceBuild.Chain.Tests.ps1 | windows/scripts/tests/SourceBuild.Chain.Tests.ps1 | 16 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. READ 2026-09-09: this is the repeated-FIXTURE case the Linux side already solved with t_consumer_fixture. Four It blocks (lines 53, 82, 100, 130) each hand-build the SAME stage tree — the same `$body` fake-stage script that appends its own -SourceDir to a log, the same `foreach ($s in 'a','b',...) { Set-Content ... }` loop, and the same `$stages = @( @{ Name='A'; Script='a.ps1'; SourceDir='src-a' } ... )` array differing only in how many entries it has (3, 3, 4, 2). Longest shared run 10 lines, and unlike every other row in this scope those ten lines are identical as TEXT, not merely after the gate's "S"/$V folding. One fixture with a count parameter serves all four without a single mode flag, and each case shrinks to the one line that is actually its subject (-StartAt 'B' / -Until 'B' / the two-call partition / -StartAt ''). Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/tests/SourceBuild.NinjaRetry.Tests.ps1 | windows/scripts/tests/SourceBuild.NinjaRetry.Tests.ps1 | 49 | reviewed 2026-09-09, READ: longest shared run 14 line(s), and every one of those lines is the arrange skeleton after the gate's folding — `It "S" { / Invoke-InTestDir { param($V) / & $V $V / $V = Join-Path $V "S" / Invoke-WithEnv @{ ... }`. The WBT_NINJA_MODE / WBT_NINJA_FAILONCE / BUILD_JOBS / stall-marker values that fold to "S" are precisely the case matrix: succeed first try, fail then retry at -j<RetryJobs>, retry a guard-kill at FULL -j, bound the stall retries, do not retry when jobs are already <= RetryJobs. A shared runner would need a mode, a job count, a stall-marker switch and an expected call-log list — four parameters for five callers, which is the shape rule 4 refuses. The env hashtable is the test. Budget pinned so a real copy still trips it
windows/scripts/tests/SourceBuild.CrossHelpers.Tests.ps1 | windows/scripts/tests/SourceBuild.Qnn.Tests.ps1 | 48 | reviewed 2026-09-09, READ: longest shared run 5 line(s) — the Pester BeforeAll preamble (`$root = Split-Path (Split-Path $PSScriptRoot -Parent) -Parent` / `Import-Module (Join-Path $root 'scripts\modules\WindowsSourceBuild.Common.psm1') -Force -DisableNameChecking` / `$savedArch = $WINDOWS_TARGET_ARCH` / `$tmp = Join-Path ([IO.Path]::GetTempPath()) ('wbt-<suite>-' + [guid]::NewGuid().ToString('N'))`). A BeforeAll cannot delegate the Import-Module line: the module it loads is the thing under test, and the $tmp prefix is per-suite by design so a leaked directory names its owner. The repo-root walk and the temp-dir line DO have owners already (Get-RepoRoot and New-TestDir in TestHarness.psm1) and adopting them is worth doing as hygiene, but it is a 19-file sweep that retires no row: the gate suppresses that block as idiom at >6 owners (measured: 7 shingles across 19 files, 8 across 9). Budget pinned
windows/scripts/host/Optimize-HostVhdx.ps1 | windows/scripts/host/Publish-ShimPatch.ps1 | 41 | reviewed 2026-09-09, READ (the largest clone family in the repo: Optimize-HostVhdx:75 / Publish-ShimPatch:64 / Update-HostVhdx:67). The 47 shingles are ONE loose chunk and the longest identical run is 5 lines: the #108 $scriptAssetRoot/$repoRoot resolution, the Import-Module lines, New-HostMaintenanceLog, and the two one-line wrappers `function Write-Step` / `function Save-Transcript`. None of it can move. The first two lines LOCATE the modules directory, so they cannot live inside the module they find (bootstrap paradox), and the wrappers must be defined in SCRIPT scope to close over $hostLog -- a module-scoped copy would not see it, which is exactly why WindowsHostMaintenance.Common.psm1 exports Write-HostStep taking $Log explicitly and lets the ~50 call sites keep their old signature through a local wrapper. The real de-duplication already happened on 2026-08-21 (~90 lines: transcript preamble + service stop-loop); what is left is the seam that hands the module its context. Budget pinned so a real copy still trips it
windows/scripts/modules/WindowsFormatting.Common.psm1 | windows/scripts/modules/WindowsWebDav.Common.psm1 | 44 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Both files inline the uv delegate trio (LogInfo, LogWarning, CommandRunner) that an owner ALREADY exports and neither of them calls: New-UvBuildDelegates in WindowsBuild.Common.psm1:638, whose own comment calls itself 'the three delegates every python CI entry script hands to New-UvProjectEnvironment/Remove-UvProjectEnvironment'. Today only Initialize-CiEnvironment.ps1:79 uses it. WindowsFormatting.Common.psm1:133-144 inlines all three verbatim; WindowsWebDav.Common.psm1:59-72 inlines the two log delegates verbatim. This is the cheapest kind of extraction - repointing to an owner that exists, with no new API. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/build/Test-Health.ps1 | windows/scripts/build/Test-Health.ps1 | 46 | reviewed 2026-09-09, READ: the lookup is already owned -- Resolve-ToolPath -BinEnvVar -ExeName is the one resolver, and both blocks (Test-Health.ps1:83-94 ffmpeg, :97-108 gst-launch-1.0) call it. The 7-line shared run is the cross-skip-or-check wrapper around it, and every one of its six moving parts differs per tool: the [SKIP] prose, the Check name, the env var, the exe name, the version flag (-version vs --version), and the failure text. A helper would take six parameters to serve two callers -- worse than two honest copies. This is a container HEALTHCHECK: the per-tool prose is the diagnostic, and flattening it into a format string is the readability the file is built around. Budget pinned
windows/scripts/host/Optimize-HostVhdx.ps1 | windows/scripts/host/Update-HostVhdx.ps1 | 46 | reviewed 2026-09-09, READ: same module-bootstrap chunk (Optimize:75 <-> Update:67, longest identical run 6 lines -- these two are the pair that also share the WindowsScripts.Shared import). Kept for the reasons on the other two rows of this family: the layout resolution is the bootstrap paradox and the two wrappers are scope-bound. The next-largest block between these two is the final service-restart loop (34 shingles, Optimize:230 <-> Update:311); it is byte-equivalent in all three scripts and WindowsHostMaintenance.Common could own it as a Start-HostServices mirroring the Stop-HostServices it already exports -- but that would not retire or shrink ANY row, because every row in this family is measured by the bootstrap chunk, not by the restore. Recorded as a separate cleanup, not as this row's business. Budget pinned
windows/scripts/host/Publish-ShimPatch.ps1 | windows/scripts/host/Update-HostVhdx.ps1 | 46 | reviewed 2026-09-09, READ: the same module-bootstrap chunk as the Optimize/Publish row (Publish-ShimPatch:64 <-> Update-HostVhdx:67, longest identical run 6 lines -- Update also imports WindowsScripts.Shared, which Publish does not). Un-ownable for the same two reasons: the $scriptAssetRoot/$repoRoot pair must run BEFORE any Import-Module because it is what finds the modules directory, and Write-Step/Save-Transcript must be script-scope to close over $hostLog. A dot-sourced fragment could technically hold the last four lines, but it would take -Name, -RepoRoot and -LogPath per caller plus a switch for the Shared import, and it would put the definition of Write-Step out of sight of the file that calls it fifty times. Budget pinned
windows/scripts/tests/Modules.Orchestrators.Tests.ps1 | windows/scripts/tests/Modules.Orchestrators.Tests.ps1 | 46 | reviewed 2026-09-09, READ: longest shared run 9 line(s), and the run is the docker-fake arrange — It / Invoke-InTestDir / `& $newFakeDocker` / try / `$log = Join-Path $dir 'docker.log'` / `Invoke-WithEnv @{ WBT_DOCKER_LOG = $log; WBT_D_IMGID = ...; WBT_D_STATE = ...; WBT_D_INSPECT_EXIT = ... }`. Those three WBT_D_* values fold to "S" and are the entire case matrix (nothing exists -> fresh container; RUNNING on the same image -> reuse; survives rm -> unique-name fallback). The fake docker itself is already shared, declared once at line 82 with a comment explaining why it is a FUNCTION and not a .bat. What is left is one hashtable per case, which is the case. Budget pinned so a real copy still trips it
windows/scripts/tests/Shared.VersionsEnv.Tests.ps1 | windows/scripts/tests/Shared.VersionsEnv.Tests.ps1 | 45 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 7 line(s), split across two shapes and neither is extractable. The 7-line run (lines 15 and 37) is a whole two-line case — `ConvertFrom-TestEnvFile -Dir $dir -Lines @(...)` then two Assert-Equal — where the -Lines array and the expected values ARE the test (trimming vs quote-stripping). The 4-line run (lines 144, 162) is the `foreach ($k in ...) { Assert-True ($args_.ContainsKey($k)) ...; Assert-True ([bool]$args_[$k]) ... }` tail; the loop body is two lines and the key list it walks is what differs per branch (media-core vs media-litert with its protoc/JRE pins). Budget pinned
windows/scripts/tests/SourceBuild.GstWrapProvisioning.Tests.ps1 | windows/scripts/tests/SourceBuild.GstWrapProvisioning.Tests.ps1 | 44 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — `It "S" {` plus the two-line hand-rolled temp directory (`$dir = Join-Path ([IO.Path]::GetTempPath()) ("gstwrap-" + [Guid]::NewGuid().ToString('N'))` / `New-Item -ItemType Directory -Path $dir -Force / Out-Null`). Below any threshold worth a helper, and the one real observation here is hygiene rather than duplication: this suite hand-rolls what TestHarness.psm1 already exports as New-TestDir / Invoke-InTestDir (which also guarantee the cleanup this file does by hand). Adopting them is a two-line tidy, not a dedup, and the block is idiom-suppressed at >6 owners across the tree so it would retire nothing. Budget pinned
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/host/Install-ScoopTools.ps1 | 43 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Same block as the Build-GstreamerFromSource/Build-LlvmFromSource row: the aarch64 compiler-rt staging recipe, third copy. Longest shared run 9 lines (the else/warn arm plus the whole catch/finally cleanup). Retired by the same single owner. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/modules/WindowsBuild.Common.psm1 | windows/scripts/modules/WindowsFlutter.Common.psm1 | 43 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 5 line(s), all five of them a param() header, and the longest run carrying any statement at all is 2 ($V = $V then $V = "S"). The 43 shingles are scattered token overlap between two unrelated concerns - Invoke-BuildExternal's process plumbing and the Flutter plugin source patcher - with no block a helper could own. A shared idiom, not a copy. Budget pinned so a real copy still trips it
windows/scripts/tests/Testing.ManualExecutable.Tests.ps1 | windows/scripts/tests/Testing.ManualExecutable.Tests.ps1 | 11 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. READ 2026-09-09: four It blocks (lines 42-49, 56-63, 72-79, 105-112) hold the SAME ten lines character for character — `$root = New-FakeBuildRoot` / try / `$result = @(Invoke-ManualTestExecutable -Context ([pscustomobject]@{}) -BuildRoot $root -ExecutableName 'x.exe')` / `$result.Count / Should -Be 1` / `$result[0] / Should -Be <bool>` / finally / Remove-Item. Only the expected boolean differs (and the executable name in the fourth). The file's own header says what is being pinned is "the return value must be exactly ONE boolean" — that is one assertion, written out four times, and the try/finally around it is the leak-safety the harness already owns elsewhere as Invoke-InTestDir. This is not a case where the copies carry the meaning: the meaning is entirely in the Mock lines above them, which stay. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/modules/WindowsBuild.Common.psm1 | windows/scripts/modules/WindowsBuild.Common.psm1 | 41 | reviewed 2026-09-09, READ: longest shared run 16 line(s) and all 16 are the Export-ModuleMember -Function name list matched against itself off by one - distinct function names that the normaliser folds to "S". The longest run of real statements is 5: the stopwatch / try / & $Script / $Context.Results.Succeeded.Add shape shared by Invoke-BuildOptional (line 332) and Invoke-BuildStep (line 460). The file ALREADY documents why those two are not merged, in a six-line comment at line 327: Invoke-BuildStep returns $true/$false while Invoke-BuildOptional has always returned nothing and passed the script block's own output straight through, so delegating would either inject a stray boolean into that output or, piped to Out-Null, swallow the tool output the caller wants to read. Two honest copies, deliberately. Budget pinned
windows/scripts/modules/WindowsLint.Common.psm1 | windows/scripts/modules/WindowsLint.Common.psm1 | 41 | reviewed 2026-09-09, READ: the 4-line shared run (21-24 vs 102-105) is this module's rule CONTRACT, not a copied body - param([Ast]$Ast, [string]$Label = '<text>'), $out = @(), $Ast.FindAll({ CommandAst }, $true), foreach. All three rules (Get-BarewordCommaAttrViolation, Get-SwitchShadowViolation, Get-GluedParameterViolation) declare it identically ON PURPOSE so a driver can invoke any of them the same way and concatenate the results. What each rule then DECIDES shares nothing: an ArrayLiteralAst '=$' test, a switch-parameter shadowing test, a glued-parameter offset test. Extracting the preamble means inverting the file into a visitor framework for three rules, and the four lines it would save are the four the contract requires each rule to spell out. Budget pinned
windows/scripts/tests/Testing.Asan.Tests.ps1 | windows/scripts/tests/Testing.Asan.Tests.ps1 | 41 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 5 line(s) — It / Invoke-InTestDir / param($dir) / `New-Item -ItemType Directory -Force -Path (Join-Path $dir "S")` / `Set-Content -Path (Join-Path $dir "S") -Value 'x'`. The two paths that fold to "S" are the whole difference between the cases: 'Release\unit.exe' (build root beats a configuration subdirectory), 'Test\perf\bench.exe' (caller-supplied extra directory), and the deep tree (recursive fallback). The remaining 3-line run at lines 42/50 is `Assert-Equal 'C:\base' $PATH 'PATH must be restored'` plus two closing braces — structural. Budget pinned
windows/scripts/build/Build-LlvmFromSource.ps1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 40 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. The 'verify against an optional pin, warn when unpinned' POLICY, hand-written in four places. Longest shared run 7 lines and it is the whole ladder: `if ($sha) { $actual = (Get-FileHash -Algorithm SHA256 -Path $x).Hash; if (-not [string]::Equals($actual,$sha,[StringComparison]::OrdinalIgnoreCase)) { throw ... }; Write-Host verified } else { Write-Warning unpinned }`. The gate names the family outright: 'ONE block of 25 shingle(s), held by 4 files -- Build-LlvmFromSource.ps1:53, Install-Tensorrt.ps1:111, WindowsScripts.Shared.psm1:119, WindowsSourceBuild.Common.psm1:694'. This is a download-policy decision (mismatch is fatal, absent pin is a warning) that must be identical everywhere, and four copies is how it stops being. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/build/Write-BundleManifest.ps1 | windows/scripts/build/Write-BundleManifest.ps1 | 39 | reviewed 2026-09-09, READ: a NORMALISATION ARTEFACT, not a copy. The shared run is `[System.Collections.Generic.List[string]]::new()` followed by a column of `$V.Add("S")` -- the gate folds every string literal to "S", so two document builders emitting different text measure identical. Write-BundleManifest.ps1:96 assembles BUNDLE-ENV.ps1 (PowerShell env-setter lines); :110 assembles BUNDLE-README.md (Markdown prose). They share the List-and-Add idiom and nothing else: no line of actual content is common. There is no block to give an owner -- one document cannot serve the other. Budget pinned so a real copied block still trips it
windows/scripts/diagnostics/Find-LsmEventHolder.ps1 | windows/scripts/diagnostics/Get-LsmWaitObject.ps1 | 16 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Longest identical run 8 lines (Find-LsmEventHolder.ps1:133-140 / Get-LsmWaitObject.ps1:55-62), only one of them structural: the silo wait loop -- `$deadline = (Get-Date).AddSeconds($WaitForSiloSec)`, the while-poll that diffs Win32_Process wininit.exe against the baseline PID set, and its throw. The gate names it as a 35-shingle block held by four files. Beyond it the two share the cdb.exe walk, the OutDir default and the services.exe/svchost descent. Real discovery logic with one honest parameter (the timeout), not a shape. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/diagnostics/Find-LsmEventHolder.ps1 | windows/scripts/diagnostics/Get-LsmWaitstack.ps1 | 11 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Longest identical run 8 lines (Find-LsmEventHolder.ps1:133-140 / Get-LsmWaitstack.ps1:53-60) -- the same silo wait loop plus the `foreach ($i in 1..20)` services.exe poll. Get-LsmWaitstack uses comsvcs MiniDump instead of cdb, so these two share the discovery half only; that half is exactly what the new module owns. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/diagnostics/Get-LsmWaitObject.ps1 | windows/scripts/diagnostics/Get-LsmWaitstack.ps1 | 16 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Longest identical run 8 lines (Get-LsmWaitObject.ps1:55-62 / Get-LsmWaitstack.ps1:53-60) -- the silo wait loop and the services.exe poll. Get-LsmWaitObject's own comment at line 50 already says 'Same silo detection as Get-LsmWaitstack.ps1', so the duplication is known and simply has no owner. That is the clearest possible signal to give it one. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/diagnostics/Get-LsmWaitObject.ps1 | windows/scripts/diagnostics/Get-SiloProcesses.ps1 | 16 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Longest identical run 6 lines (Get-LsmWaitObject.ps1:55-60 / Get-SiloProcesses.ps1:67-72) -- the deadline plus the wininit-diff while loop. Both also carry the cdb.exe walk and the OutDir default. The two poll intervals differ (3 s vs 2 s), which is one argument, not a mode. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/diagnostics/Get-LsmWaitstack.ps1 | windows/scripts/diagnostics/Get-SiloProcesses.ps1 | 11 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Longest identical run 6 lines (Get-LsmWaitstack.ps1:53-58 / Get-SiloProcesses.ps1:67-72) -- the deadline plus the wininit-diff while loop, the same 35-shingle block the gate reports across four files. Both also share the OutDir default. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/diagnostics/Test-Sccache2726Repro.ps1 | windows/scripts/diagnostics/Test-SccacheRspShapes.ps1 | 39 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- the `#requires` line, the two-line Kataglyphis/SPDX copyright the license gate requires everywhere, and the `<#` + `.SYNOPSIS` opener. The remaining shingles are the in-container probe preamble (param/$ErrorActionPreference/banner/Import-Module C:\bkmnt\modules/Enter-VsDevCmdEnvironment/New-Item/Set-Location), which is a call into an existing owner rather than a block wanting one, and the sccache server reset -- kept separate because the ports differ by construction (`45${Slot}0` vs `424${shape}`) so concurrent probes never share a server. Budget pinned so a real copied block still trips it.
windows/scripts/tests/Shared.Download.Tests.ps1 | windows/scripts/tests/Shared.Download.Tests.ps1 | 38 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — `It "S" { / Invoke-InTestDir { param($dir) / $src = Join-Path $dir "S"`. Three lines is below any threshold worth extracting a helper for, and the filename folded to "S" (ok.exe / error.html / ok.zip) is the signature case each block then writes magic bytes for. A shared idiom, not a copy. Budget pinned
windows/scripts/tests/SourceBuild.GitCloneRetry.Tests.ps1 | windows/scripts/tests/SourceBuild.GitCloneRetry.Tests.ps1 | 38 | reviewed 2026-09-09, READ: longest shared run 9 line(s) — the same arrange skeleton the NinjaRetry suite carries (It / Invoke-InTestDir / `& $newFakeGit $dir` / three Join-Path lines / Invoke-WithEnv @{...}), with WBT_GIT_MODE and WBT_GIT_FAILONCE folded to "S". Those two values are the case matrix: green first try, transient failure retried once with the partial tree wiped, MaxAttempts exhausted throwing with the attempt count, MaxAttempts exhausted returning $false under -SkipOnFailure. The fake git is already declared once at line 12. Four cases, four env combinations, and a runner that took them as parameters would have four of them. Budget pinned
windows/scripts/modules/WindowsSourceBuild.Common.psm1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 37 | reviewed 2026-09-09 by MEASUREMENT: the 115-line "run" is the Export-ModuleMember -Function list at line 1850 matched against itself off by one - 115 distinct function names, every one folded to "S" by the normaliser. A module's export manifest is a language-mandated declaration and has nowhere else to live. The longest run of actual statements is 4: the `if ($X) { $cmakeArgs += "-DCMAKE_...=$X" }` ladder that two cmake-argument builders each spell out (219-222 vs 220-223), where the whole value of each line is the flag it names - folding them into a table would replace four readable lines with a table plus a loop and hide which flag is conditional on what. Budget pinned
windows/scripts/rust/Build-Windows.ps1 | windows/scripts/rust/Build-Windows.ps1 | 44 | measured 2026-09-08 after the deletion pass: twelve files left the tree, so shingles these two share lost owners and fell back under the MAX_OWNERS=6 idiom cutoff, which re-counts them against the pair. Neither file was edited in that pass. Recorded at the measurement so it cannot grow further.
windows/scripts/diagnostics/Test-OpencvVideoBackends.ps1 | windows/scripts/diagnostics/Test-SccacheWrite.ps1 | 35 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) (Test-OpencvVideoBackends.ps1:24-28 / Test-SccacheWrite.ps1:55-59) and it is the whole of `function Write-Result` -- a param line, the `[ OK ]`/`[FAIL]` ternary and one Write-Host format string. Exactly two files in the tree define it (verified 2026-09-09), and both are in-container probes that otherwise import nothing but the one module they need. Moving a five-line console formatter into a shipped module to save five lines twice adds an import and an exported name for no behaviour anyone depends on, and the two probes print to different audiences -- one grades opencv video backends, the other sccache writes. Below any threshold worth extracting a helper for. Budget pinned.
windows/scripts/modules/WindowsAgenticLoop.Common.psm1 | windows/scripts/tests/TestHarness.psm1 | 35 | reviewed 2026-09-09, READ: longest shared run 3 line(s) - the save/set/restore env-var idiom ($saved[$k] = [Environment]::GetEnvironmentVariable($k); [Environment]::SetEnvironmentVariable(...)). TestHarness.psm1 already owns it as Invoke-WithEnv, but that is the PESTER harness under windows/scripts/tests: a production module must not import a test harness, and the harness must stay standalone so a suite runs without the module tree (which is exactly why Invoke-Tests.ps1 is what provides Get-RepoRoot and ConvertFrom-VersionsEnv). The AgenticLoop copy also interleaves a Write-AgenticLog line between set and run and returns the inner command's value, neither of which Invoke-WithEnv's -Body contract expresses. Budget pinned
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/modules/WindowsSourceBuild.Patches.psm1 | 34 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) -- 34 shingles scattered across a patch-application idiom with no contiguous block behind them. WindowsSourceBuild.Patches.psm1 is already the owner of the inline-patch primitives (Invoke-InlineRegexPatch, Edit-CppKeywordAlternatives); what the script shares with it is the call shape and the Test-Path/WriteAllText guard around it, not logic the module does not already hold. A shared idiom, not a copy. Budget pinned
windows/scripts/modules/WindowsMeson.Common.psm1 | windows/scripts/modules/WindowsMeson.Common.psm1 | 34 | reviewed 2026-09-09, READ: two shapes repeat inside this module and both are deliberate. (a) The 3-line idempotency guard `$marker = '[kataglyphis ... fix]'; if ($text -notmatch [regex]::Escape($marker)) {` - that IS the module's contract for re-runnable source patches, written once per patch site with a different marker, and the marker is what gets written into the patched file. (b) The 9-line download / extract / record-failure / cleanup run shared by the wrap loop (line 326) and the libffi force-download (line 347); they delete different files ($_.FullName for the .wrap in the loop, an explicitly composed path for libffi) and report different failure text, so one owner needs a mode flag per caller. Budget pinned
windows/scripts/patches/opencv/Get-Ffmpeg9AvcodecConfig.ps1 | windows/scripts/patches/opencv/Get-Ffmpeg9AvcodecConfig.ps1 | 34 | reviewed 2026-09-09, READ: the twin is not PowerShell. The two units are the `$shimHw` (line 57) and `$shimImpl` (line 82) here-strings -- C source injected into OpenCV's videoio -- and the gate is shingling embedded C. Longest identical run 4 lines and it is the shared tail `(const void**)&p, NULL) < 0)` / `return NULL;` / `return p;` / `}`. The two shims differ in return type (const enum AVPixelFormat* vs const AVRational*), config selector (AV_CODEC_CONFIG_PIX_FORMAT vs AV_CODEC_CONFIG_FRAME_RATE), function name, and the pre-avcodec-61.13 fallback field (c->pix_fmts vs c->supported_framerates) -- four substitutions to template two 18-line functions. Doing so would emit C assembled from a PowerShell format string into a third-party tree, which is exactly what this file's own header refuses: it applies the fix as an in-script edit rather than a .patch ON PURPOSE so the anchors stay matchable and the injected text stays greppable when OpenCV reshuffles. Budget pinned so a THIRD shim still trips the gate.
windows/scripts/tests/SourceBuild.Patch.Tests.ps1 | windows/scripts/tests/SourceBuild.Patch.Tests.ps1 | 34 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 6 line(s) — the patch-repo arrange (It / Invoke-InTestDir / `& $newPatchRepo $dir` / `$patch = Join-Path $dir 'p.patch'` / `[System.IO.File]::WriteAllText($patch, $goodPatch)` / `$flag = ...`). The only thing that differs is $goodPatch vs $badPatch, which is the entire subject of the rung-1-vs-fallback pair; both fixtures are already declared once at file scope. The 5-line run at lines 38/149 is the warning-capture tail (`$warns = @($captured / Where-Object { $_ -is [WarningRecord] })` + two asserts + two braces) shared by the two no-op cases, which assert about DIFFERENT no-ops (present-but-unchanged vs anchor-absent). Budget pinned
windows/scripts/tests/WindowsContainerBuild.Reuse.Tests.ps1 | windows/scripts/tests/WindowsContainerBuild.Reuse.Tests.ps1 | 34 | reviewed 2026-09-09, READ: longest shared run 5 line(s) — the parameter-metadata assertion `$mandatory = @($p[$name].Attributes / Where-Object { $_ -is [ParameterAttribute] -and $_.Mandatory })` plus its Assert-True, appearing in exactly two It blocks (lines 116 and 141) over two DIFFERENT commands (Invoke-ContainerBuild, Wait-ContainerExit); the 3-line `Assert-True $p.ContainsKey($name)` run at lines 99/134 is the same story. Two call sites each, and in both cases the five lines ARE the entire body of the case. An Assert-MandatoryParameter owner is possible but would put each suite's only assertion behind a helper the suite does not itself cover, and would buy two call sites in one file — two honest copies read better and cost less. Budget pinned
windows/scripts/build/Build-LlvmFromSource.ps1 | windows/scripts/host/Install-ScoopTools.ps1 | 33 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Same block again -- the aarch64 compiler-rt staging recipe, the Llvm/ScoopTools edge of the same three-file family. Longest shared run 6 lines (catch/finally cleanup), 4 more at the verified/warn arms. Retired by the same single owner. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/diagnostics/Test-OpencvCudaCmdshape.ps1 | windows/scripts/diagnostics/Test-SccacheRspShapes.ps1 | 33 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- `#requires -Version 7.0`, the two Kataglyphis/SPDX copyright lines and the comment-based-help opener, none of which can live in a module. The rest is the in-container probe preamble, whose only logic is the Import-Module/Enter-VsDevCmdEnvironment pair that already calls an owner, plus the sccache server reset with its deliberately per-probe port (`43${i}0` vs `424${shape}`). Budget pinned.
windows/scripts/host/Reset-ContainerLocks.ps1 | windows/scripts/host/Reset-ContainerStores.ps1 | 33 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 0 line(s) -- 33 shingles of overlapping TOKENS without one identical line. Both walk `& $bt --addr npipe:////./pipe/buildkitd debug workers 2>&1 / Select-String -Pattern 'windows/amd64/worker' / ForEach-Object { $_.Line } / Write-Host` after locating buildctl, but Reset-ContainerStores calls Get-PreferredToolPath and Reset-ContainerLocks spells its candidate list inline. That inline copy is already reviewed and documented at Reset-ContainerLocks.ps1:44: "Inline ON PURPOSE (#101 reviewed 2026-08-17): this is an elevated REPAIR tool for a wedged container stack -- a module import is one more thing that can be broken exactly when this script is needed." WindowsScripts.Shared's Assert-Elevated docstring names the same two scripts as exempt. Budget pinned
windows/scripts/tests/WindowsBuildSweep.Common.Tests.ps1 | windows/scripts/tests/WindowsBuildSweep.Common.Tests.ps1 | 33 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s) — `It "S" { / $results = @( / [pscustomobject]@{ Name = "S"; Ok = $V; Skipped = $V; ExitCode = N; Message = "S" } / ...`. The gate folds the booleans to $V and the exit codes to N, which erases the only thing that separates the three Write-SweepSummary cases (all passed -> 0; first non-zero wins; a throw with no exit code -> 1). The result array IS the input; a builder for it would take one argument per column. Budget pinned
windows/scripts/diagnostics/Test-Arm64Prereqs.ps1 | windows/scripts/diagnostics/Test-Arm64Prereqs.ps1 | 32 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 1 line(s) within this 221-line file -- not one consecutive pair repeats. The 32 shingles are the repeated `Add-Finding -Question ... -Ok ... -Detail ...` calls that the file's own `$script:Findings` accumulator and Add-Finding function (declared at 54-58) exist to serve. That is a table of questions expressed as calls to a local helper the file ALREADY owns; the repetition is the data, and collapsing it further would mean encoding each prerequisite check as a data row and losing the per-check code that computes it. A shared idiom, not a copy. Budget pinned.
windows/scripts/diagnostics/Test-SccacheNvccInstantiation.ps1 | windows/scripts/diagnostics/Test-SccacheRspShapes.ps1 | 32 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- the `#requires` line, the two-line copyright the license gate mandates, and the `<#`/`.SYNOPSIS` opener. The rest is the in-container probe preamble (a call into WindowsSourceBuild.Common.psm1, not a block wanting an owner) and the sccache server reset, whose ports differ on purpose (4235 vs the computed `424${shape}`) so two probes never share a server. Budget pinned so a real copied block still trips it.
windows/scripts/host/Sync-DefenderExclusions.ps1 | windows/scripts/host/Sync-DefenderExclusions.ps1 | 32 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Genuine self-duplication with a local owner and no downside: lines 32-38 and 48-54 are the SAME six-line dump of the Defender exclusion set (blank line, cyan `== <label> ==` banner, Get-MpPreference, then ExclusionPath and ExclusionProcess each sorted and indented), printed once as BEFORE and once as AFTER. Longest identical run 7 lines -- the largest true run in the whole host/ scope. The two copies exist precisely so the reader can diff them, which is the case where they MUST stay identical, and today nothing enforces that. One local helper, one parameter, two call sites, no module dependency added (the script already imports WindowsScripts.Shared). Retires the row outright: it is this pair's only unit-pair overlap, so the measurement drops to 0 and the gate reports the row as stale. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/host/Test-HostSetup.ps1 | windows/scripts/modules/WindowsBuildDriver.Common.psm1 | 32 | reviewed 2026-09-09, READ: longest shared run 3 line(s) -- the per-drive free-space probe (`$psDrive = Get-PSDrive $letter -ErrorAction SilentlyContinue` / skip a drive whose .Free is null / `[math]::Round($psDrive.Free / 1GB, 1)`). Test-HostSetup.ps1:245 already names the twin in a comment: "Mirrors Assert-DiskHeadroom." The two ends are opposite by construction: Assert-DiskHeadroom THROWS to stop a build before hcsshim starts failing dishonestly, while Test-HostSetup reports PASS/FAIL through Write-Check with a fix line and must never abort -- a diagnostic that dies on the disk check cannot report its other checks. Merging would make the host probe depend on the build-driver module for three lines of arithmetic. Budget pinned
windows/scripts/modules/WindowsAgenticLoop.Common.psm1 | windows/scripts/modules/WindowsBuildDriver.Common.psm1 | 31 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), and the longest run carrying a statement is 2 ($V = $V -join "S" then if ($V) {). 31 scattered shingles across two files that share only PowerShell's own vocabulary - joining an array and testing it. A shared idiom, not a copy. Budget pinned
windows/scripts/tests/BuildDriver.HostGates.Tests.ps1 | windows/scripts/tests/BuildDriver.HostGates.Tests.ps1 | 31 | reviewed 2026-09-09, READ: longest shared run 8 line(s) — the shim-patch-state arrange (It / Invoke-InTestDir / `$shim = Join-Path $dir 'shim.exe'` / `$stock = Join-Path $dir 'shim.exe.orig'` / two Set-Content / `$statePath = Join-Path $dir 'state.json'` / Write-ShimPatchState). Three cases need the identical two-binary sandbox because what they assert is what happens AFTER it: the stock hash is recorded, a revert-to-stock is named specifically, a state file recorded for a different install path is ignored. Hoisting the sandbox into a BeforeEach would put the file the third case deliberately mismatches out of the case that mismatches it. Budget pinned so a real copy still trips it
windows/scripts/tests/BuildKit.SolveOrderParity.Tests.ps1 | windows/scripts/tests/BuildKit.SolveOrderParity.Tests.ps1 | 31 | reviewed 2026-09-09, READ: longest shared run 4 line(s) — the `$map = @{}; foreach ($m in [regex]::Matches($text, '(?m)^...')) { $map[$m.Groups[2].Value] = $m.Groups[1].Value }` shape, used once on the Dockerfile side (FROM ${MEDIA_CORE_X_IMAGE} AS stage) and once on the driver side ($xArg = @{ MEDIA_CORE_Y_IMAGE = ... }). The two regexes are the entire content and they parse two different file formats; folding them behind one owner would mean passing the pattern and the group order in, which is a regex-runner, not a concept. The remaining 3-line runs are `Assert-True ($bad.Count -eq 0) (...)` tails. Budget pinned
windows/scripts/tests/SourceBuild.BuildMachineMsvcTool.Tests.ps1 | windows/scripts/tests/SourceBuild.TvmAssembledWheel.Tests.ps1 | 31 | reviewed 2026-09-09 by MEASUREMENT, and this pair shares NO TEXT AT ALL: the reported 6-line run is `New-Item -ItemType Directory -Force -Path (Join-Path $V "S") / Out-Null` + three `Set-Content (Join-Path $V "S") "S"` + `}` + `AfterAll { Remove-Item $V -Recurse -Force -ErrorAction SilentlyContinue }`. One suite builds VC\Tools\MSVC\...\bin\HostX64\{x64,ARM64}\cl.exe, the other root\tvm_ffi\{__init__.py,core.cp314-win_arm64.pyd,lib\tvm_ffi.dll}; every path and payload folds to "S", so what matched is the SHAPE of a BeforeAll that seeds a small tree and an AfterAll that removes it. There is nothing here to give an owner. Budget pinned
windows/scripts/tests/SourceBuild.CmakeArgsConsumed.Tests.ps1 | windows/scripts/tests/SourceBuild.CmakeArgsConsumed.Tests.ps1 | 31 | reviewed 2026-09-09, READ: longest shared run 5 line(s), and the identical part is the ACT — `$w = @()` then `Assert-CmakeArgsConsumed -BuildDir $d -PassedArgs @('-DUSE_QNN=ON') -WarningVariable w -WarningAction SilentlyContinue`. It is identical on purpose: the whole point of these cases is that the SAME call must warn or stay silent depending on what New-FakeCache wrote (USE_QNN:UNINITIALIZED vs USE_QNN:BOOL vs the prefix-only USE_QNN_EXTRA). Merging the act would delete the constant the comparison rests on. The fake-cache builder is already owned by New-FakeCache. Budget pinned
windows/scripts/build/Build-IreeFromSource.ps1 | windows/scripts/build/Build-TvmFromSource.ps1 | 30 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (a Join-Path assignment and a brace). The 30 shingles are the CMake-argument accumulation idiom (`$args += '-DFOO=ON'` ladders) that every source-build script in this family runs; the flag NAMES are project-specific (IREE_* vs TVM_/USE_*) and share nothing. The generic half already has an owner -- both call Initialize-SourceBuildScript and Get-SourceBuildVersion from WindowsSourceBuild.Common. A shared idiom, not a copy. Budget pinned
windows/scripts/build/Build-LlvmFromSource.ps1 | windows/scripts/host/Install-Tensorrt.ps1 | 30 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Same four-file SHA256 verify-or-warn family as the Build-LlvmFromSource <-> WindowsSourceBuild.Common row (Install-Tensorrt.ps1:111 is the third holder). Longest contiguous run here is only 2 lines because the two wrap the ladder differently, but the 25-shingle block itself is identical. Retired by the same owner. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/build/Test-Container.ps1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 30 | reviewed 2026-09-09, READ: the 5-line run is a PARAM BLOCK, which is language-mandated shape and cannot live anywhere else. Test-Container.ps1:96 Resolve-ContainerImageValue (itself the declared module fallback -- see the WindowsContainerImage.Common row) and WindowsSourceBuild.Common.psm1:42 Get-SourceBuildVersion are two DIFFERENT resolvers that happen to open the same way: `[switch]$X )` then `$resolved = $DefaultValue` then `if (-not [string]::IsNullOrWhiteSpace($Value)) { $resolved = $Value }`. They resolve different things (a container image value vs a source-build version, the latter taking an ARRAY of env vars) and are deliberately separate -- SourceBuild.PinParity.Tests.ps1:30 documents them as the two distinct resolvers on purpose. Merging them would couple the smoke gate to the source-build module. Budget pinned
windows/scripts/diagnostics/Test-SccachePatchVerify.ps1 | windows/scripts/host/Install-RustToolchain.ps1 | 30 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 2 line(s), and both are the Kataglyphis/SPDX copyright header. The 30 shingles are the patch-apply idiom -- glob a directory of .patch files, iterate, `git apply`, check $LASTEXITCODE -- and the two copies are deliberately OPPOSITE where it matters. Install-RustToolchain.ps1:283 globs with `-ErrorAction SilentlyContinue` and TOLERATES zero patches, because :279-281 records that '#114 auto-retired' the machinery when the fixes merged upstream on 2026-08-28; it throws only when a present patch fails to apply, with a five-line message about rebasing or bumping the pin. Test-SccachePatchVerify.ps1:34 takes `Select-Object -First 1` and treats an empty dir as fatal. Merging them would force the host installer to fail closed on an empty dir it is designed to accept, or the probe to grade unpatched sccache silently -- and Test-SccachePrHygiene.ps1:38-41 documents that exact defect ('hygiene run 3 reported green with 0002/0003 never applied'). On top of that the probe is dead: Dockerfile.probe:16-21 names Test-SccachePatchVerify one of three readers of the removed C:\bkmnt\patch mount, 'dead by construction -- backlog #159's subject'. Budget pinned; #159 owns the disposition.
windows/scripts/host/Install-Tensorrt.ps1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 30 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) (`if ($sha) {` / `$actual = (Get-FileHash -Algorithm SHA256 -Path $zip).Hash`). Part of the 4-file SHA256-verify family the gate names in its own family listing -- 25 shingles across windows/scripts/build/Build-LlvmFromSource.ps1:53, windows/scripts/host/Install-Tensorrt.ps1:111, windows/scripts/modules/WindowsScripts.Shared.psm1:119 and windows/scripts/modules/WindowsSourceBuild.Common.psm1:694, with four further spellings in build/ and diagnostics/ that differ in -Path vs -LiteralPath and in whether they .ToLower() first. One owner is right and the gate is correct to point at it, but it has to be created in a pass that owns build/ AND modules/ together: a host/ row can only repoint one of the eight call sites, and doing that alone would add a NINTH definition rather than remove any. Recorded, not owned. Budget pinned
windows/scripts/modules/WindowsBuild.Common.psm1 | windows/scripts/modules/WindowsCMake.Common.psm1 | 30 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. 7 consecutive substantive lines (WindowsBuild.Common.psm1:736-744 vs WindowsCMake.Common.psm1:286-294) set exactly the same five environment variables to the resolved sccache path - CMAKE_C_COMPILER_LAUNCHER, CMAKE_CXX_COMPILER_LAUNCHER, RUSTC_WRAPPER, CC_WRAPPER, CXX_WRAPPER - and both carry the same four-line comment explaining why CMAKE_CUDA_COMPILER_LAUNCHER is deliberately absent (tried and reverted 2026-08-08; sccache-wrapped nvcc loses its per-arch .cubin files before fatbinary). The CMake copy's own comment already names the verdict: 'Near-duplicate of the wrapper wiring in Initialize-BuildCacheEnvironment (WindowsBuild.Common.psm1) ... merge candidate.' The duplication is known, flagged in the code, and has simply had no owner. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/modules/WindowsContainerImage.Common.psm1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 30 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 14 line(s), all of it Export-ModuleMember name list against Export-ModuleMember name list (ContainerImage:219 / SourceBuild:1850) - distinct function names the normaliser folds to "S". The longest run of real statements is 3, and those are a Join-Path and an IsNullOrWhiteSpace guard. Not a dedup target. Budget pinned
windows/scripts/modules/WindowsUv.Common.psm1 | windows/scripts/modules/WindowsUv.Common.psm1 | 30 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 9 line(s), of which 7 are param-block lines ([Parameter(Mandatory)][string]$X, [scriptblock]$Y) - the delegate-injection signature every function in this module takes so its caller supplies CommandRunner / LogInfo / LogWarning instead of the module reaching for Write-BuildLog itself. That uniform signature is the reason this module can be the single source of truth for the uv venv lifecycle. The longest run of statements is 2. A param() header cannot live anywhere else. Budget pinned
windows/scripts/tests/BkWarm.Forwarding.Tests.ps1 | windows/scripts/tests/BkWarm.Forwarding.Tests.ps1 | 30 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 7 line(s) across exactly TWO It blocks (lines 16 and 47) — the arrange that points at ..\host\Invoke-BkWarm.ps1 and writes a fixture build script with Set-Content. The fixtures then diverge completely: one records the forwarded parameters into a file, the other exits non-zero to prove bk-warm aborts before the export. Two callers, one of them needing a different fixture body, is not a helper; it is two cases that happen to open the same way. Budget pinned
windows/scripts/build/Build-IreeFromSource.ps1 | windows/scripts/build/Build-IreeFromSource.ps1 | 29 | reviewed 2026-09-09, READ: the 8-line run IS the call to the owner, and the owner already exists. Build-IreeFromSource.ps1:46 and :90 both read `[void](Invoke-InlineRegexPatch -Path $f `` -SkipIfMatch .. -Pattern .. -Replacement .. -AssertGone .. -Description ..)` -- WindowsSourceBuild.Patches.psm1 owns Invoke-InlineRegexPatch, and what the gate measures is that cmdlet's own parameter list spread over backtick continuations. The two call sites patch DIFFERENT upstream bugs (the x86-64 MASM trampoline in hal/local/elf/CMakeLists.txt, and C `inline` linkage in one arm_64 ukernel) with different regexes. Nothing is left to extract: a wrapper would carry five parameters to save one line. Budget pinned
windows/scripts/build/Build-LitertLmFromSource.ps1 | windows/scripts/modules/WindowsAgenticLoop.Common.psm1 | 29 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s) -- below any threshold worth extracting a helper for, and the two files are conceptually unrelated (a LiteRT-LM source patcher and the agentic-loop module). The overlap is the regex-replace-with-scriptblock idiom `[regex]::Replace($text, $pat, { param($m) ... })`, a .NET call shape. Merging would couple the LiteRT-LM patch stack to an unrelated module. Budget pinned
windows/scripts/build/Build-LlvmFromSource.ps1 | windows/scripts/modules/WindowsScripts.Shared.psm1 | 29 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Same four-file SHA256 verify-or-warn family; WindowsScripts.Shared.psm1:193 holds the copy embedded in Invoke-DownloadWithRetry. Longest run 3 lines, same 25-shingle block. Retired by the same owner, which lands in this very file. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/host/Test-HostSetup.ps1 | windows/scripts/modules/WindowsBuildKit.Common.psm1 | 29 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- a single `([regex]::Match($text, '...')).Groups[1].Value` field extraction, plus a lone `}`. Both pull one named value out of a config blob they each read for their own reason; nothing on either side of that line lines up. A shared idiom, not a copy. Budget pinned
windows/scripts/modules/WindowsAgenticLoop.Common.psm1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 29 | reviewed 2026-09-09 by MEASUREMENT: the 38-line run is Export-ModuleMember list against Export-ModuleMember list (AgenticLoop:1344 / SourceBuild:1848); every entry on both sides is a distinct function name folded to "S". The longest run of real statements is 2 (Push-Location $V then try {). Not a dedup target. Budget pinned
windows/scripts/modules/WindowsContainerBuild.Reuse.psm1 | windows/scripts/modules/WindowsContainerBuild.Reuse.psm1 | 29 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 13 line(s), and the substantive part of it is five consecutive `[string[]]$X = @(),` declarations inside Invoke-ContainerBuild's param block (line 788 onward) matched against a sibling param block. Named, individually-documented parameters of this module's one public entry point - IncrementalDirs, IncrementalExclude, OutputDirs, VerifyDirs, InboundExclude, OutboundExclude, KeepDirs. A param() header cannot live anywhere else, and collapsing seven named path lists into one hashtable would cost every caller its parameter validation. Budget pinned
windows/scripts/tests/SourceBuild.CrossHelpers.Tests.ps1 | windows/scripts/tests/SourceBuild.CrossHelpers.Tests.ps1 | 29 | reviewed 2026-09-09, READ: longest shared run 6 line(s) and it is the Describe/BeforeAll preamble repeated across this file's four Describes (lines 13, 58, 109, 142, 170) — each imports the module its own Describe is about (WindowsSourceBuild.Common, .Patches, WindowsTargetArch.Common) and names its own temp prefix (wbt-ninja / wbt-absent / wbt-tpy / wbt-patch). A Pester BeforeAll cannot delegate the Import-Module line without hiding which module the block is testing, and the per-Describe prefixes are what make a leaked directory identifiable. The repo-root walk in line 2 of each has an owner already (Get-RepoRoot); adopting it is hygiene worth doing but retires nothing, the block being idiom-suppressed at >6 owners. Budget pinned
windows/scripts/build/Build-OpencvFromSource.ps1 | windows/scripts/build/Build-OpencvFromSource.ps1 | 28 | reviewed 2026-09-09, READ: same shape as the IREE self-pair -- the 5-line run is the call to Invoke-InlineRegexPatch (owned by WindowsSourceBuild.Patches.psm1), at Build-OpencvFromSource.ps1:103 and :134. Two different ARM-only upstream bugs (cv::float32_t shadowing clang's ::float32_t in softfloat.cpp, and bundled MLAS remapping vmaxvq_f32/vminvq_f32 onto MSVC neon intrinsics), each with its own Guard/Pattern/Replacement. The owner exists; the duplication the gate sees is the parameter list. Budget pinned
windows/scripts/diagnostics/Test-OnnxTuReplay.ps1 | windows/scripts/diagnostics/Test-OpencvCudaCmdshape.ps1 | 28 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- `#requires -Version 7.0`, the two-line copyright the license gate requires, and the comment-based-help opener. The rest is the in-container probe preamble (Import-Module C:\bkmnt\modules + Enter-VsDevCmdEnvironment, a call into an existing owner) and the sccache server reset, whose port differs per probe by design (4236 vs the computed `43${i}0`). Budget pinned.
windows/scripts/diagnostics/Test-OnnxTuReplay.ps1 | windows/scripts/diagnostics/Test-Sccache2726Repro.ps1 | 28 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- the `#requires` line, the mandated copyright pair and the `<#`/`.SYNOPSIS` opener. The rest is the in-container probe preamble plus the sccache server reset with its per-probe port (4236 vs `45${Slot}0`); and Test-Sccache2726Repro is one of the three probes Dockerfile.probe:16-21 marks 'dead by construction' pending backlog #159, so its blocks are not a refactoring target until #159 decides. Budget pinned.
windows/scripts/diagnostics/Test-SccacheRelativeFo.ps1 | windows/scripts/diagnostics/Test-SccacheRelativeFo.ps1 | 28 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 1 line(s) within this 90-line file -- `& $sccache --stop-server 2>&1 / Out-Null` at :43 and :89, the open and close of the probe's server lifecycle. Not one consecutive pair repeats. The 28 shingles are the sccache env-and-server idiom this whole probe family shares, and here it is the SUBJECT rather than boilerplate: line 47 starts the server inside `try { ... } finally { Pop-Location }` after a Push-Location precisely because the server inherits its working directory and relative `-Fo` resolution against that directory is what the probe measures. A helper that owned the start would own the variable under test. A shared idiom, not a copy. Budget pinned.
windows/scripts/host/Optimize-HostVhdx.ps1 | windows/scripts/host/Set-ContainerdConfig.ps1 | 28 | reviewed 2026-09-09, READ: longest shared run 2 line(s) -- the first two lines of the live-build guard (`$live = @(Get-Process -Name $BlockingProcess -ErrorAction SilentlyContinue)` / `if ($live.Count -gt 0 -and -not $Force) {`). Four scripts carry it (Optimize:142, Publish-ShimPatch:186, Set-ContainerdConfig:146, Update:142) and no two agree past those two lines, because the THROW TEXT is the whole point of the guard and each reports something different: Optimize prints count + unique names + pids, Update prints pids only, Set-ContainerdConfig prints names and explains that applying restarts containerd, Publish nests the guard inside `if (-not $Force)` so it can add a second shim-liveness check with its own message. Save-Transcript runs first in three of them and Set-ContainerdConfig has no transcript at all. One owner would need a message, a pids switch and a transcript switch to serve four callers -- worse than four honest two-line guards. Budget pinned
windows/scripts/modules/WindowsFlutter.Common.psm1 | windows/scripts/modules/WindowsFlutter.Common.psm1 | 28 | reviewed 2026-09-09, READ: the 8-line run (123-131 vs 136-144) is two back-to-back idempotent source patches inside ONE function - the permission_handler_windows Success call and its for-loop - each written as already-patched? -> log; target present? -> log, -replace, $changed = $true. A shared applier would need the already-pattern, the target pattern, the replacement, a label AND a by-reference content buffer (PowerShell cannot return content and a changed flag cleanly): five parameters to serve two call sites in one function. The second block also deliberately has no 'target not found' warning, because that patch is optional upstream while the first one is not. Two honest copies. Budget pinned
windows/scripts/tests/SourceBuild.FindPythonPrefix.Tests.ps1 | windows/scripts/tests/SourceBuild.FindPythonPrefix.Tests.ps1 | 28 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. This is a source-TEXT suite: every case reads a build script with Get-Content -Raw and asserts about what it does or does not spell, so the shingles it shares with itself are the `foreach ($path in ...) { $text = Get-Content -Raw ... }` idiom, not a block. A shared idiom, not a copy. Budget pinned
windows/scripts/tests/SourceBuild.NinjaTargets.Tests.ps1 | windows/scripts/tests/SourceBuild.NinjaTargets.Tests.ps1 | 28 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. The 28 shingles are scattered across the BeforeAll preamble and the Invoke-HostToolCmakeBuild block whose own comment ("arm64 run 18, 2026-08-25") explains why it stands alone. A shared idiom, not a copy. Budget pinned
windows/scripts/tests/SourceBuild.SccacheSession.Tests.ps1 | windows/scripts/tests/SourceBuild.SccacheSession.Tests.ps1 | 28 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. READ 2026-09-09: `$newFakeSccache` is defined TWICE in this file, character for character, 11 lines each — once at line 11 inside Describe 'Start-SccacheServerSession' and again at line 67 inside Describe 'Complete-SccacheServerSession'. There is no reason for the second copy; the two Describes want the identical fake (a sccache.cmd that appends its arguments to %WBT_SCC_LOG% and exits 0). This is not a deliberate twin, it is a copy-paste that nothing has caught because the gate froze it wholesale. The cheapest possible fix: one definition, zero parameters, zero behaviour change. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/build/Build-IreeFromSource.ps1 | windows/scripts/build/Build-LitertFromSource.ps1 | 27 | reviewed 2026-09-09, READ: a NORMALISATION ARTEFACT. The 6-line run is `$flags = [ordered]@{ 'S'='S'; 'S'='S'; ... }` -- the gate folds every string to "S", so two per-TU compiler-flag tables of the same arity measure identical while sharing no actual entry. Build-IreeFromSource.ps1:268 holds per-TU flags for IREE's arm_64 ukernels; Build-LitertFromSource.ps1:241 holds them for XNNPACK's aarch64 microkernels. Different upstream projects, different translation units, different flags. There is no block to own. Budget pinned
windows/scripts/build/Test-TargetArch.ps1 | windows/scripts/tests/SourceBuild.VerifyTargetArch.Tests.ps1 | 27 | reviewed 2026-09-09, READ: a TEST-HARNESS FIXTURE the suite cannot avoid, and its own comment says so. The 13-line run is the COFF machine-name table ($machineNames, Test-TargetArch.ps1:113). SourceBuild.VerifyTargetArch.Tests.ps1:18 tests the script by LIFTING function bodies out of it -- `Get-ScriptFunctionDefinition -ScriptPath .. -FunctionName 'Get-CoffMachine','Get-ArchiveMachine','Format-Machine'` -- and its comment states the constraint exactly: '$machineNames is a script-level hashtable the functions reference; Get-ScriptFunctionDefinition lifts only function bodies, so it must be in scope before the dot-source.' Moving the table to a module would force Test-TargetArch.ps1 to import one, which is the very thing the lift-a-function-body technique exists to avoid. The copy is the test oracle. Budget pinned
windows/scripts/diagnostics/Test-LayerRename.ps1 | windows/scripts/diagnostics/Test-LayerRename.ps1 | 27 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 2 line(s) within this 150-line file (106-107 vs 144-145), one of them structural. The 27 shingles are the CONTROL and VERDICT halves the file's header describes -- two `docker run --isolation process` invocations that differ only in whether the create-then-rename loop targets a fresh directory or an image-layer directory, and whose per-half verdict printing is the finding. Collapsing the two halves into one parameterised call would hide the control/verdict distinction that makes the probe's output readable, which is the whole point of the file. Note the separate docker.exe resolver block in this file is being retired against Get-PreferredToolPath under the Test-LayerRename / Test-ProcessIsolationCommit row; this budget will fall on its own when that lands. Budget pinned meanwhile.
windows/scripts/diagnostics/Test-Sccache2726Repro.ps1 | windows/scripts/diagnostics/Test-SccacheNvccInstantiation.ps1 | 27 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- `#requires`, the mandated two-line copyright, and the comment-based-help opener. The rest is the in-container probe preamble (a call into WindowsSourceBuild.Common.psm1) and the sccache server reset, whose ports differ by construction (`45${Slot}0` vs 4235). Test-Sccache2726Repro is additionally one of the three readers Dockerfile.probe:16-21 marks dead pending backlog #159. Budget pinned.
windows/scripts/host/Install-Tensorrt.ps1 | windows/scripts/modules/WindowsScripts.Shared.psm1 | 27 | reviewed 2026-09-09, READ: longest shared run 5 line(s) -- the verify-or-throw arm (`$actual = (Get-FileHash -Algorithm SHA256 -Path $p).Hash` / `if (-not [string]::Equals($actual, $expected, [StringComparison]::OrdinalIgnoreCase)) {` / `throw ...`). Same 4-file family as the Install-Tensorrt <-> WindowsSourceBuild.Common row, and the two ends are not interchangeable even so: Shared's copy lives INSIDE Invoke-DownloadWithRetry's retry loop, where a mismatch means "a truncated download -- delete the partial and try again", while Install-Tensorrt hashes a EULA-gated zip a human STAGED by hand into windows\downloads, which is never downloaded and must never be retried. -ExpectedSha256 therefore cannot own it. The right owner is one Assert-FileSha256 serving all eight sites, created in a cross-directory pass. Budget pinned
windows/scripts/modules/WindowsCMake.Common.psm1 | windows/scripts/rust/New-MsixPackage.ps1 | 27 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s), and all four are consecutive `Write-BuildLog -Context $Context -Message "S"` diagnostic-banner lines whose MESSAGES are entirely different - CMake's '========== SYSTEM INFO ==========' block (CPU count, ParallelJobs, Preset, Configuration) against the MSIX packaging environment block (Workspace, Binary, PackageName). The normaliser folds every string literal to "S", so this run is an artefact of the measurement, not a copy. Budget pinned
windows/scripts/modules/WindowsLint.Common.psm1 | windows/scripts/tests/SourceBuild.PinParity.Tests.ps1 | 27 | reviewed 2026-09-09, READ: longest shared run 3 line(s) and it is the PowerShell AST API's own shape - walk CommandElements by index, skip anything that is not a CommandParameterAst, then take the argument either attached (-Name:value) or as the next element. Both sides carry their own comment explaining that attached-or-next rule, because both had to learn it. A Pester suite cannot import a production module's walker without becoming a test of the walker instead of a test of the repo's pin parity, and the module must not depend on the suite. Budget pinned
windows/scripts/modules/WindowsScripts.Shared.psm1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 27 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 20 line(s), all Export-ModuleMember name list on both sides (Shared:737 / SourceBuild:1850), and the next three runs are the same list matched against different windows of the same list. The longest run of real statements is 3 - `$result += "$Prefix$key"` style array appends in a foreach, which is how PowerShell builds an argument list. Nothing to own. Budget pinned
windows/scripts/tests/SourceBuild.TvmAssembledWheel.Tests.ps1 | windows/scripts/tests/SourceBuild.TvmAssembledWheel.Tests.ps1 | 27 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — `) -join "`n"` / `$deps = @(Get-PyprojectDependencies -PyprojectText $py)` / one Assert-Equal. The two cases feed DIFFERENT pyproject shapes (lists before the dependencies block; a one-line list followed by [project.urls]) to the same parser and assert different exact outputs; the three lines they share are the call itself, which must stay identical for the comparison to mean anything. Budget pinned
windows/scripts/tests/WindowsAppRunner.Common.Tests.ps1 | windows/scripts/tests/WindowsAppRunner.Common.Tests.ps1 | 27 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s) — `It "S" { / $resolved = Resolve-AppExecutablePath -BuildRoot $buildRoot -ExecutableName $appName -Configurations @("S") / $resolved / Should -Be (Join-Path $buildRoot "S") / }`. Two cases, four lines each, and the two "S" values (the configuration passed in and the path expected back) are the entire test. There is no block here, only a two-line assertion written twice. Budget pinned
windows/scripts/build/Build-IreeFromSource.ps1 | windows/scripts/build/Build-OnnxFromSource.ps1 | 26 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s) -- below any threshold worth extracting a helper for. The overlap is the CMake-argument ladder these source-build siblings all run; the flag names are project-owned (IREE_* vs onnxruntime_*) and Build-OnnxFromSource.ps1:163 states the rule explicitly: 'names like onnxruntime_USE_CUDA are ORT-only -- kept local, not in the generic helper'. A shared idiom, not a copy. Budget pinned
windows/scripts/build/Build-LitertLmFromSource.ps1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 25 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only (a Join-Path assignment and a closing brace). Build-LitertLmFromSource.ps1 is the LiteRT-LM patch stack -- a file whose whole content is Edit-SourceFile calls against one upstream tree -- and what it shares with the common module is the guard idiom around them, not logic. A shared idiom, not a copy. Budget pinned
windows/scripts/build/Build-OnnxFromSource.ps1 | windows/scripts/build/Build-OnnxFromSource.ps1 | 25 | reviewed 2026-09-09, READ: the 5-line run is the call to an owner that already exists -- `Invoke-SourcePatchWithFallback -PatchFile (Join-Path ..) -SourceDir .. -FallbackNote .. -Fallback { Invoke-InlineRegexPatch .. }` at Build-OnnxFromSource.ps1:116 and :135. Both primitives live in WindowsSourceBuild.Patches.psm1. The two sites patch unrelated defects (CUDA 13.x CCCL breaking clang-cl PCH, and XQA's HAS_SM80_OR_LATER host-pass guard dropped by sccache's nvcc decomposition) with different patch files and different fallback regexes. The reviewable-.patch-with-inline-fallback shape is the repo's documented patch contract, not a copy. Budget pinned
windows/scripts/host/Clear-DiskSpace.ps1 | windows/scripts/host/Repair-WindowsComponentstore.ps1 | 25 | reviewed 2026-09-09, READ: the third leg of the `Say` triangle -- one logical line of timestamped Write-Host, longest identical run 0 (formatting differs, tokens do not). Both holders are deliberately module-free (see the two sibling rows). Note for the next reader: five host scripts carry this echo under two names (`Say` in Clear-DiskSpace / Repair-WindowsComponentstore / Reset-ContainerStores, `Write-Step` in Install-NewHost / Set-ContainerdConfig) and three more get it from WindowsHostMaintenance.Common as Write-HostStep. A fifth home in WindowsScripts.Shared would have to be given a THIRD name, because Optimize-HostVhdx, Publish-ShimPatch and Update-HostVhdx already define a script-scope Write-Step that would shadow it. Two extra names for one console line is worse than the copies. Budget pinned
windows/scripts/host/Clear-DiskSpace.ps1 | windows/scripts/host/Reset-ContainerStores.ps1 | 25 | reviewed 2026-09-09, READ: same one-line `Say` echo as the Repair/Reset row; longest identical run 0 line(s), and 0 only because Clear-DiskSpace wraps the body over three lines while Reset-ContainerStores keeps it on one -- the tokens are the same, the lines are not. Clear-DiskSpace imports no module either, and its header is explicit about why the file is self-contained: it is "THE ONLY SANCTIONED WAY TO RECLAIM HOST DISK IN THIS REPO" and design rule 1 makes its allowlist "the whole security boundary". Adding an import so it can borrow a Write-Host wrapper widens that boundary for nothing. Budget pinned
windows/scripts/host/Repair-WindowsComponentstore.ps1 | windows/scripts/host/Reset-ContainerStores.ps1 | 25 | reviewed 2026-09-09, READ: the shared block is ONE logical line -- the timestamped console echo `function Say([string]$m, [string]$c = 'Gray') { Write-Host ('[{0}] {1}' -f (Get-Date -Format HH:mm:ss), $m) -ForegroundColor $c }` -- plus the runs of `Say '== ... ==' 'Cyan'` calls around it; longest identical run 1 line. The owner would have to be WindowsScripts.Shared, and Repair-WindowsComponentstore imports NO module at all, by design: it is the hand-launched elevated repair for a damaged Windows component store (`Set-StrictMode -Off`, `$ErrorActionPreference = 'Continue'`, launched via Start-Process -Verb RunAs), and Assert-Elevated's own docstring already exempts it from the module gate for that reason. One line of Write-Host is not worth turning into a load-bearing import for a script whose job is to run when the host is broken. Budget pinned
windows/scripts/tests/Modules.Orchestrators.Tests.ps1 | windows/scripts/tests/SourceBuild.GitCloneRetry.Tests.ps1 | 25 | reviewed 2026-09-09 by MEASUREMENT, VERIFIED against the normalised form: the 10-line run is `"S" ,` seven times, `)`, the shared `Set-Content -LiteralPath (Join-Path $V "S") -Value ($V -join "S") -Encoding ASCII`, and a closing brace. The raw text on the two sides is `'if "%WBT_WASMOPT_MODE%"=="failfirst" ('` against `'if exist "%WBT_GIT_FAILONCE%" ( del ... )'` — different tools, different protocols, nothing in common. This row measures the .bat-writing IDIOM, and the one line it really shares is not worth an owner. Budget pinned
windows/scripts/tests/Modules.Orchestrators.Tests.ps1 | windows/scripts/tests/SourceBuild.NinjaRetry.Tests.ps1 | 25 | reviewed 2026-09-09 by MEASUREMENT: same artefact as the GitCloneRetry/NinjaRetry row — the 9-line run is `"S" ,` repeated inside a `$lines = @( ... )` batch-file array ($newFakeWasmOpt vs $newFakeNinja), with zero shared text. The secondary 5-line run is the assertion tail `Assert-Equal N $calls.Count "S"` / `Assert-Match "S" $calls[N] "S"` / two braces, which is the shape of every call-log assertion in this tree once the patterns and messages fold away. Budget pinned
windows/scripts/tests/SourceBuild.GitCloneRetry.Tests.ps1 | windows/scripts/tests/SourceBuild.NinjaRetry.Tests.ps1 | 25 | reviewed 2026-09-09 by MEASUREMENT, and the run is an ARTEFACT of the gate's string folding: the reported 9-line run is literally `"S" ,` six times over, then `)`, then the Set-Content line. Both suites declare a fake tool as a `$lines = @( '@echo off', 'echo GIT %* >> "%WBT_GIT_LOG%"', ... )` array written out as a .bat — every batch line folds to "S", so two fakes with nothing in common (git clone with a partial-tree wipe vs ninja with a stall guard) read as an identical block. The only genuinely shared line is `Set-Content -LiteralPath (Join-Path $dir "S") -Value ($lines -join "`r`n") -Encoding ASCII`. One line is not an owner. Budget pinned
windows/scripts/tests/SourceBuild.StageTargetPythonDeps.Tests.ps1 | windows/scripts/tests/SourceBuild.StageTargetPythonDeps.Tests.ps1 | 25 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 5 line(s) — the four Get-WheelRequirements cases each build a wheel with the already-shared `& $NewWheel` fixture, then call `$reqs = @(Get-WheelRequirements $p)` and count. What folds to "S" is the -RequiresDist array, which is the only thing under test (plain requirements, extra== markers dropped, stop at the first blank line, none at all). The fixture already has one owner; what remains is the call and the count. Budget pinned
windows/scripts/host/Reset-ContainerLocks.ps1 | windows/scripts/host/Set-BuildkitdGcpolicy.ps1 | 25 | reviewed 2026-09-09, READ: ALREADY DECLARED DELIBERATE IN BOTH FILES. Reset-ContainerLocks.ps1:42 and Set-BuildkitdGcpolicy.ps1:74 each carry the comment "Inline ON PURPOSE (#101 reviewed 2026-08-17): elevated repair/maintenance". The shared 3-line run is the buildctl-reaches-the-worker probe those two elevated host tools each run before acting. #101 settled this; nothing to redo. Surfaced by the MAX_OWNERS=6 cutoff on 2026-09-09. Budget at the measurement
windows/scripts/tests/SourceBuild.PkgConfig.Tests.ps1 | windows/scripts/tests/SourceBuild.PkgConfig.Tests.ps1 | 24 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s) — the `foreach ($expected in 'libav','opencv','onnx','tflite','webrtc','nice') { Assert-True ($names -contains $expected) ... }` roll-call, and a 3-line `$byName = @{}; foreach ($p in @(Get-RequiredGstPlugin)) { $byName[$p.Name] = $p }` index. The roll-call is deliberately spelled out twice against two different projections of the same list (the required set, and how each is detected) so that adding a plugin to one and not the other fails; collapsing them would delete that. Budget pinned
windows/scripts/tests/WindowsPerfBaseline.Common.Tests.ps1 | windows/scripts/tests/WindowsPerfBaseline.Common.Tests.ps1 | 24 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — `It "S" {` plus `$path = Join-Path $root "S"` plus `New-BenchmarkDocument -Path $path -Benchmarks @(` (or the Compare-BenchmarkTimeMap equivalent). Three lines is below any threshold worth extracting a helper for, and the benchmark document each case then writes (time units, cpu_time vs real_time, a zero baseline) is the case. The document builder already has one owner, New-BenchmarkDocument. A shared idiom, not a copy. Budget pinned
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/build/Build-IreeFromSource.ps1 | 23 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (else/Write-Host/brace). Two source-build siblings whose overlap is the family preamble plus the inline-patch call shape, both already owned (WindowsSourceBuild.Common, WindowsSourceBuild.Patches). A shared idiom, not a copy. Budget pinned
windows/scripts/build/Build-LitertLmFromSource.ps1 | windows/scripts/build/Build-LitertLmFromSource.ps1 | 23 | reviewed 2026-09-09, READ: the 7-line run is the call shape of Edit-SourceFile -Path -Marker -Description -WarnMessage -Transform { param($t) ... }, invoked ~20 times in this one file (:205, :219, :229, :238, :253, :272, :322, :349, :379, :394, :427, :460, :503, :512, :523 ...). Edit-SourceFile is the owner and it already exists; every call carries a different Marker, a different regex and a different upstream defect. This file IS the LiteRT-LM patch stack -- the allowlist header names that stack as a protected, permanent twin class. A wrapper would take the same five parameters. Budget pinned
windows/scripts/build/Export-LitertLmBridge.ps1 | windows/scripts/build/Export-LitertLmBridge.ps1 | 23 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) -- below any threshold worth extracting a helper for. The repetition is sibling graft steps in Invoke-LiteRtLmSupportGraft, each a `if ((Test-Path $src) -and -not (Test-Path $dst)) { Copy-Item ..; Write-Host .. }` over a DIFFERENT source/destination pair from the LiteRT-LM winfix graft list. A shared idiom, not a copy. Budget pinned
windows/scripts/build/Test-Container.ps1 | windows/scripts/modules/WindowsSmokeTest.Common.psm1 | 23 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (a return, a closing brace, a Write-Host). WindowsSmokeTest.Common.psm1 is ALREADY the owner of this file's harness (Assert-Test, Skip-Test, Write-TestHeader, Initialize-SmokeScratch, Assert-ArtifactPresent); the residual overlap is the shape of a script calling its own harness. Nothing is left to move. Budget pinned
windows/scripts/diagnostics/Get-HostLsm.ps1 | windows/scripts/diagnostics/Get-LsmWaitObject.ps1 | 11 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Longest identical run 5 lines (Get-HostLsm.ps1:35-39 / Get-LsmWaitObject.ps1:39-43) -- the OutDir default: the triple `Split-Path ... -Parent` climb to repoRoot, `Join-Path $repoRoot 'out\lsm-attach'` and the New-Item. All five LSM probes carry it byte-identically. Both files also share the cdb.exe walk. A three-deep Split-Path climb repeated five times is the kind of path arithmetic that breaks silently when a directory moves; one owner fixes that too. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/modules/WindowsPerfBaseline.Common.psm1 | windows/scripts/modules/WindowsPerfBaseline.Common.psm1 | 23 | reviewed 2026-09-09, READ: the shared runs are three `[pscustomobject]@{ Name; Baseline; Candidate; Delta; Status; IsRegression }` literals, one per arm of a single if / elseif / else (Status 'compared', 'only-base', 'only-candidate'). The six field names are identical BY DESIGN - that is the row schema the comparison emits - while the values and the Status differ per arm. A factory would take six parameters to serve three arms of one conditional and would hide which arm reports what. Budget pinned
windows/scripts/tests/Native.Shielded.Tests.ps1 | windows/scripts/tests/Native.Shielded.Tests.ps1 | 23 | reviewed 2026-09-09, READ: longest shared run 7 line(s) across three It blocks, and the run is the two-line assertion pair `Assert-Match "S" ($out / Out-String) "S"` / `Assert-Equal 0 $LASTEXITCODE "S"` plus the closing braces — the whole point of Invoke-ShieldedNative is that stdout, stderr noise and a clean exit code all behave the same way, so the three cases assert the same two facts about three different fixtures (a quiet tool, a noisy one, a quoted path). Identical assertions over different inputs is what a parameterised contract looks like; the fixtures are two lines each and cannot be shared. Budget pinned
windows/scripts/host/Install-Cuda.ps1 | windows/scripts/host/Install-ScoopTools.ps1 | 22 | reviewed 2026-09-09, READ: longest shared run 3 line(s), and all three lines are CALLS to helpers that already have exactly one owner -- `$dest = Join-Path $env:TEMP '<file>'`, `$sha = Resolve-ContainerImageValue -EnvironmentVariable '<PIN>' -DefaultValue '...'`, `Invoke-DownloadWithRetry -Url $url -DestinationPath $dest -Description '...' -ExpectSignature MZ -ExpectedSha256 $sha` (both in WindowsScripts.Shared). This is what a correctly de-duplicated download site LOOKS like; what repeats is the helper's argument list, and extracting that would only move the arguments somewhere the reader cannot see which artifact is being fetched. Budget pinned
windows/scripts/host/Publish-ShimPatch.ps1 | windows/scripts/host/Set-ContainerdConfig.ps1 | 22 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- the single `$live = @(Get-Process -Name $BlockingProcess -ErrorAction SilentlyContinue)` line of the live-build guard already argued on the Optimize/Set-ContainerdConfig row (four callers, four different throw messages, no common owner without three flags). The only other overlap is the `[CmdletBinding(SupportsShouldProcess, ConfirmImpact = '...')]` + `param(` header, which is language-mandated and cannot live anywhere else. Budget pinned
windows/scripts/tests/BuildKit.Subnet.Tests.ps1 | windows/scripts/tests/BuildKit.Subnet.Tests.ps1 | 22 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 6 line(s) — one whole It block's worth of `$a = '{ "ipam": { "subnet": "..." } }' / ConvertFrom-Json` / Assert-True over ConvertTo-CanonicalJson, matched against its sibling because both CIDR literals fold to "S". The pair exists precisely to contrast: one asserts a cosmetic difference IS normalised away, the other that a real subnet change is NOT. Merging them would merge the two verdicts. The 5-line run at lines 154/203 is the `} else { Write-Host '(skipped: CNI confs not present)' } } }` host-dependent skip tail — structural. Budget pinned
windows/Build-Buildkit.ps1 | windows/scripts/diagnostics/Invoke-DiagnosticProbe.ps1 | 20 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. The block is byte-identical, 5 lines, and it is LOGIC plus a hardcoded install-layout list, not a language-mandated shape: Build-Buildkit.ps1:153-157 and Invoke-DiagnosticProbe.ps1:100-104 both read `if (-not $BuildCtl) { $BuildCtl = Get-PreferredToolPath -CommandName 'buildctl' -CandidatePaths @("$env:ProgramFiles\Stevedore\bin\buildctl.exe", 'D:\Stevedore\bin\buildctl.exe') }` followed by `if (-not $BuildCtl) { throw 'buildctl.exe not found (Stevedore bin or PATH).' }`. Get-PreferredToolPath is already the shared walker; what has no owner is the CANDIDATE LIST, which is repeated in nine files (grep 'Stevedore.*buildctl'). One zero-mode-flag helper serves every caller because the Stevedore layouts are a property of the host, not of the caller. Both comments at the call sites already say the block was supposed to have one owner ('shared helper, #101' / 'Central candidate walk instead of the pasted Stevedore-then-PATH block that used to live in every runner (backlog #101)') -- the walk was extracted, the list was left behind. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/build/Test-TargetArch.ps1 | windows/scripts/build/Test-TargetArch.ps1 | 21 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) -- below any threshold worth extracting a helper for. The file holds Get-CoffMachine and Get-ArchiveMachine, deliberately separate readers: its own header (:30) records why -- 'COFF archives (.lib) are NOT PE files. A naive bytes[0x3C] walk over an archive' is the bug this file exists to prevent. Folding them would recreate exactly that defect. Budget pinned
windows/scripts/certificates/GenerateCertificateMSIX.ps1 | windows/scripts/rust/New-MsixPackage.ps1 | 21 | reviewed 2026-09-09, READ: the shared run is 4 lines and every one of them is language-mandated placement. GenerateCertificateMSIX.ps1:13-16 and New-MsixPackage.ps1:25-28 are the pair of `[Diagnostics.CodeAnalysis.SuppressMessageAttribute('PSAvoidUsingConvertToSecureStringWithPlainText', ...)]` / `('PSAvoidUsingPlainTextForPassword', ...)` lines, then `param(`, then a `[Parameter(Mandatory)][string]` declaration. A PSSA suppression attribute is bound lexically to the script it suppresses and cannot be hoisted into a module; neither can a top-level param() block. The two files already carry their OWN justification comments above the attributes (one for a manual utility, one for CI-injected dev signing) and share zero executable statements -- one creates a KSP self-signed cert and exports a PFX, the other packs and signs an MSIX. Budget pinned.
windows/scripts/diagnostics/Test-OnnxTuReplay.ps1 | windows/scripts/diagnostics/Test-SccacheOptionsStrict.ps1 | 21 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- the `#requires` line, the license-gate copyright pair and the `<#`/`.SYNOPSIS` opener, none of which can live anywhere else. The rest is the in-container probe preamble, whose only logic already calls an owner, and the sccache server reset with its deliberately distinct ports (4236 vs 4770). Budget pinned.
windows/scripts/host/Reset-ContainerStores.ps1 | windows/scripts/host/Reset-ContainerStores.ps1 | 21 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) -- `Say '== ... ==' 'Cyan'` followed by `Get-PreferredToolPath -CommandName '<tool>' -CandidatePaths @(...)`, once for buildctl.exe and once for docker.exe. Two probes of two different tools through the one helper that already owns the lookup (the file's own comment at line 68 records why: the old `Test-Path $bt` threw on a $null path when buildctl was absent). A loop over a tool table would hide which two tools this repair script actually verifies, which is the thing an operator reads it for. Budget pinned
windows/scripts/modules/WindowsAppRunner.Common.psm1 | windows/scripts/modules/WindowsTesting.Common.psm1 | 20 | reviewed 2026-09-09, READ: longest shared run 4 line(s) - the recursive fallback both executable finders end with (Get-ChildItem -Path $BuildRoot -Filter $ExecutableName -File -Recurse / Select-Object -First 1; return .FullName; return $null). Everything above it differs: AppRunner walks candidate relative paths and returns (Resolve-Path $candidate).Path, Testing composes BuildRoot/relative/name itself and returns the composed path. Four lines, and neither module imports the other, so an owner means a new import edge or a new home in WindowsScripts.Shared for less code than the edge costs. Budget pinned
windows/scripts/modules/WindowsGstPlugins.Common.psm1 | windows/scripts/modules/WindowsRustToolchain.Common.psm1 | 21 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 7 line(s), 4 of them substantive, and those four are the multi-line `throw ("..." + "..." + "...")` shape both files use to raise a long, actionable diagnostic. The messages are completely different - pkg-config failing to resolve a plugin dependency against a missing sibling module needed by Invoke-DownloadWithRetry - and the normaliser folds every string to "S", which is the whole of this match. A shared idiom, not a copy. Budget pinned
windows/scripts/tests/Guard.DestructiveDeletes.Tests.ps1 | windows/scripts/tests/Guard.DestructiveDeletes.Tests.ps1 | 21 | reviewed 2026-09-09, READ: longest shared run 5 line(s), and it is a case body that is nothing but assertions — `Assert-Equal 'deny' (Get-GuardDecision -Tool ... -Command '...')` three times. The commands folded to "S" (winget uninstall / scoop uninstall / Remove-AppxPackage) ARE the test corpus; a table-driven rewrite would move them into a data structure and buy nothing, since the guard's whole contract is a list of spellings. The other runs are the `$script = Join-Path (Split-Path (Split-Path (Split-Path $PSScriptRoot -Parent) -Parent) -Parent) '...'` + Test-Path + Get-Content -Raw preamble (3-4 lines, and Get-RepoRoot already owns the walk) and a 3-line sandbox teardown. Budget pinned
windows/scripts/tests/Smoke.FloorCalibration.Tests.ps1 | windows/scripts/tests/Smoke.FloorCalibration.Tests.ps1 | 21 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and the longest run in the whole file across any unit pair is 2 — below any threshold worth extracting a helper for. The shingles are scattered across four sibling cases, never contiguous. A shared idiom, not a copy. Budget pinned
windows/scripts/tests/WindowsMsix.Signing.Tests.ps1 | windows/scripts/tests/WindowsMsix.Signing.Tests.ps1 | 21 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. The 21 shingles are spread over three sibling It blocks with no contiguous block between them; two consecutive matching lines is the signature of a shared call shape, not a copied gesture. A shared idiom, not a copy. Budget pinned
windows/scripts/Invoke-Lint.ps1 | windows/scripts/tests/SourceBuild.PinParity.Tests.ps1 | 20 | reviewed 2026-09-09, READ: longest identical run 3 line(s) and those lines are structural only (`}` / `continue` / `}`). What is shared is the PowerShell AST API the language forces on every caller -- `[System.Management.Automation.Language.Parser]::ParseFile($f, [ref]$tokens, [ref]$errors)` at Invoke-Lint.ps1:90 and SourceBuild.PinParity.Tests.ps1:51, each followed by `if ($errors -and $errors.Count -gt 0)`. The surrounding walks are different (Invoke-Lint enumerates caller-supplied -Path roots for .ps1+.psm1 and excludes \archive\; Get-PinScanAst walks windows/scripts excluding \(tests/modules)\ then adds modules/*.psm1) and the parse-error POLICIES are opposite and load-bearing: Invoke-Lint RECORDS the error, because reporting parse errors is its product, while Get-PinScanAst throws only when the file also matches -MustMentionPattern and otherwise skips it as another gate's business. One owner would need roots, an exclusion regex and an error-policy callback -- three parameters for two callers whose only real difference is that policy. PinParity's own header already records where consolidation deliberately stopped (#126: the scan surface got one owner, 'the MATCHER logic stays per-Describe on purpose'). Budget pinned.
windows/scripts/build/Build-OnnxFromSource.ps1 | windows/scripts/build/Build-OpencvFromSource.ps1 | 20 | reviewed 2026-09-09, READ: the 5-line run is a column of `$cmakeArgs += 'S'` with an else arm -- CMake flag accumulation, where the gate folds each flag string to "S" so two unrelated flag lists measure identical. Build-OnnxFromSource.ps1:163 says it outright: 'ONNX-specific CMake flags (names like onnxruntime_USE_CUDA are ORT-only -- kept local, not in the generic helper)'. The generic half is already in the helper; what is left is per-project by design. Budget pinned
windows/scripts/build/Test-Toolchain.ps1 | windows/scripts/diagnostics/Test-Arm64Prereqs.ps1 | 14 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- there is no contiguous block at all, only 20 scattered shingles over the arm64 prerequisite vocabulary. A build gate and a hand-run diagnostic that ask about the same components are expected to name them alike; the diagnostic must keep working when the gate is not present. A shared idiom, not a copy. Budget pinned
windows/scripts/diagnostics/Test-OnnxTuReplay.ps1 | windows/scripts/diagnostics/Test-SccacheRspShapes.ps1 | 20 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- `#requires -Version 7.0`, the mandated copyright pair and the comment-based-help opener. The rest is the in-container probe preamble plus the sccache server reset, whose ports differ per probe (4236 vs `424${shape}`) so concurrent runs never share a server. Budget pinned so a real copied block still trips it.
windows/scripts/diagnostics/Test-Sccache2726Repro.ps1 | windows/scripts/diagnostics/Test-SccacheOptionsStrict.ps1 | 20 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- `#requires`, the license-gate copyright pair, and the `<#`/`.SYNOPSIS` opener. The rest is the in-container probe preamble and the sccache server reset with distinct ports (`45${Slot}0` vs 4770). Test-Sccache2726Repro is one of the three probes Dockerfile.probe:16-21 marks dead by construction pending backlog #159. Budget pinned.
windows/scripts/diagnostics/Test-SccacheOptionsStrict.ps1 | windows/scripts/diagnostics/Test-SccacheRspShapes.ps1 | 20 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- the `#requires` line, the two-line copyright the license gate requires everywhere, and the comment-based-help opener. The rest is the in-container probe preamble, whose only logic (Import-Module + Enter-VsDevCmdEnvironment) already calls an owner, plus the sccache server reset with per-probe ports (4770 vs `424${shape}`). Budget pinned.
windows/scripts/diagnostics/Test-SccacheWrite.ps1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 20 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 3 line(s) (Test-SccacheWrite.ps1:207-209 / WindowsSourceBuild.Common.psm1:58-60) and those lines are structural only -- a param entry, a closing brace and a blank-guarded assignment. The 20 shingles are the sccache environment vocabulary (SCCACHE_DIR / SCCACHE_ERROR_LOG / SCCACHE_MULTILEVEL_CHAIN / SCCACHE_WEBDAV_ENDPOINT) appearing in both a probe that manipulates those variables to measure write behaviour and the module that configures them for real builds. Matching variable NAMES across a consumer and its subject is not a copied block, and the direction of any merge would be backwards: the probe must be able to set states the module would never produce. That is the shape of a probe and its subject, not a dedup target. Budget pinned.
windows/scripts/host/Install-NewHost.ps1 | windows/scripts/host/Set-ContainerdConfig.ps1 | 20 | reviewed 2026-09-09, READ: longest shared run 4 line(s) -- the whole of `function Write-Step { param([string]$Message, [string]$Color = 'Gray') Write-Host ('[{0}] {1}' -f (Get-Date -Format 'HH:mm:ss'), $Message) -ForegroundColor $Color }`, i.e. one logical line of timestamped console echo declared over four. It is the same block as the `Say` triangle (Clear-DiskSpace / Repair-WindowsComponentstore / Reset-ContainerStores) under a second name. There is no clean owner: WindowsHostMaintenance.Common already exports Write-HostStep for the three VHDX-family scripts, which wrap it in a SCRIPT-SCOPE `Write-Step` -- so a Write-Step exported from WindowsScripts.Shared would be silently shadowed in three files, and giving it a third name means the tree would carry three names for one console line. That is worse than five short copies. Budget pinned
windows/scripts/host/Install-NewHost.ps1 | windows/scripts/host/Test-HostSetup.ps1 | 20 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) -- `try {` plus one `(Invoke-WebRequest -Uri $u -Method Head -TimeoutSec N -UseBasicParsing).StatusCode` reachability probe, and elsewhere a bare `@('...', '...')` literal of the same two paths. Install-NewHost DRIVES the host setup and Test-HostSetup GRADES it, so both must name the same endpoint and the same expected files; that agreement is the contract between them, not a copied block. Budget pinned
windows/scripts/modules/WindowsLint.Common.psm1 | windows/scripts/tests/Driver.ClosureScope.Tests.ps1 | 20 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) and NOT ONE of them carries a statement - they are closing braces and a param header. 20 scattered shingles between a lint rule and the Pester suite that exercises closure scoping, both of which necessarily name the same System.Management.Automation.Language types. There is no block here at all. Budget pinned
windows/scripts/python/Invoke-CiPackaging.ps1 | windows/scripts/python/Invoke-CiPackaging.ps1 | 21 | measured 2026-09-08 after the deletion pass: twelve files left the tree, so shingles these two share lost owners and fell back under the MAX_OWNERS=6 idiom cutoff, which re-counts them against the pair. Neither file was edited in that pass. Recorded at the measurement so it cannot grow further.
windows/scripts/tests/Diagnostics.Llvm135Repro.Tests.ps1 | windows/scripts/tests/Diagnostics.Llvm135Repro.Tests.ps1 | 20 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. The 20 shingles are scattered across three sibling cases with no contiguous block anywhere in the file. A shared idiom, not a copy. Budget pinned
windows/scripts/tests/Modules.Orchestrators.Tests.ps1 | windows/scripts/tests/WindowsWebDav.Common.Tests.ps1 | 20 | reviewed 2026-09-09, READ: longest shared run 4 line(s) and it is the module-import header — `$modDir = Join-Path (Split-Path $PSScriptRoot -Parent) 'modules'` followed by three `Import-Module (Join-Path $modDir '<Name>.psm1') -Force -DisableNameChecking` lines. A suite's import list is its declaration of what it is testing; the two files import overlapping sets because the orchestrator suite covers several modules at once. There is nowhere for this to live: a shared import block would import modules a suite does not test and would defeat the per-file closure that BuildKit.ModuleClosure.Tests.ps1 exists to police. Budget pinned
windows/scripts/tests/SourceBuild.Qnn.Tests.ps1 | windows/scripts/tests/SourceBuild.Qnn.Tests.ps1 | 20 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. The shingles come from the three throw-cases sharing the `$threw = $false; try { ... } catch { $threw = $true }; Assert-True $threw` shape, which the harness already offers as Assert-Throws and which this file uses where it can; the remaining hand-rolled spellings are the ones that need to inspect the caught object. A shared idiom, not a copy. Budget pinned
windows/scripts/tests/WindowsRepoHygiene.Common.Tests.ps1 | windows/scripts/tests/WindowsRepoHygiene.Common.Tests.ps1 | 20 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — `It "S" {` plus two lines of git-fixture setup that ALREADY go through this file's own owners (`script:New-TestRepo`, `script:Add-TestCommit`). The remaining duplication is the call to those helpers with different arguments, which is what calling a helper looks like. A shared idiom, not a copy. Budget pinned
windows/scripts/build/Build-IreeFromSource.ps1 | windows/scripts/modules/WindowsSourceBuild.Patches.psm1 | 19 | reviewed 2026-09-09, READ: the 5-line run is the read-modify-write-if-changed tail -- `if ($new -ne $old) { [System.IO.File]::WriteAllText($path, $new); Write-Host .. }` -- shared between Build-IreeFromSource.ps1:356 and Edit-CppKeywordAlternatives in the module. That guard is the patch contract (never rewrite an unchanged file), already expressed by the module's own primitives; the IREE site applies it to a generated Python wheel tree the module knows nothing about. A shared idiom, not a copy. Budget pinned
windows/scripts/build/Build-LlvmFromSource.ps1 | windows/scripts/modules/WindowsRustToolchain.Common.psm1 | 19 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- no contiguous block. WindowsRustToolchain.Common.psm1 is a merge-lane leaf module, mounted by Dockerfile.media-merge-builder only, and Build-GstreamerFromSource.ps1:88 records the rule: 'Do NOT fold them into WindowsSourceBuild.Common -- that one is in the closure of all 11 media RUNs.' Merging anything here would widen that closure. Budget pinned
windows/scripts/build/Build-OpencvFromSource.ps1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 19 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- no contiguous block, only scattered shingles where a source-build script calls the module that owns its scaffolding. A shared idiom, not a copy. Budget pinned
windows/scripts/diagnostics/Test-SccacheNvccInstantiation.ps1 | windows/scripts/diagnostics/Test-SccacheOptionsStrict.ps1 | 19 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- `#requires -Version 7.0`, the mandated copyright pair and the `<#`/`.SYNOPSIS` opener. The rest is the in-container probe preamble and the sccache server reset, kept per-probe because the ports must differ (4235 vs 4770) and because these two capture different sccache log levels ('trace' here to dump parse lines, per the OptionsStrict synopsis). Budget pinned.
windows/scripts/host/Publish-ShimPatch.ps1 | windows/scripts/host/Test-HostSetup.ps1 | 19 | reviewed 2026-09-09, READ: longest shared run 3 line(s) -- `try {` / `Import-Module (Join-Path $scriptAssetRoot 'modules\WindowsBuildDriver.Common.psm1') -Force` / `$statePath = Get-ShimPatchStatePath`. Both files read the shim-patch state through the module that already owns it, and Publish-ShimPatch does so from four separate guarded sites so a missing module degrades one report instead of failing the deploy. What is shared here IS the call to the shared owner; there is nothing left underneath it. Budget pinned
windows/scripts/host/Repair-WindowsComponentstore.ps1 | windows/scripts/host/Reset-ContainerLocks.ps1 | 19 | reviewed 2026-09-09, READ: longest shared run 2 line(s) -- the inline elevation check (`$principal = [Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()` / `if (-not $principal.IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)) {`). WindowsScripts.Shared's Assert-Elevated owns this everywhere else and its own docstring exempts exactly these two files by name: "the module-free repair tools (reset-container-locks, repair-windows-componentstore) keep their inline check BY DESIGN -- a wedged-stack repair must not depend on a module import." Reset-ContainerLocks.ps1:44 records the same rule for its buildctl lookup. This is the one pair of the eight-site elevation family that must NOT be repointed. Budget pinned
windows/scripts/modules/WindowsCMake.Common.psm1 | windows/scripts/modules/WindowsMsix.Signing.psm1 | 19 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s), longest run carrying a statement 2 (a Write-BuildLog line then `if ($V) {`). 19 shingles of scattered overlap between the CMake driver and the MSIX signing helper, which are identical after normalisation only because both log through the same Write-BuildLog family. A shared idiom, not a copy. Budget pinned
windows/scripts/modules/WindowsSourceBuild.Common.psm1 | windows/scripts/modules/WindowsSourceBuild.Patches.psm1 | 19 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 13 line(s), of which the substantive part is 3 - the tail-of-output error shape `$V = ($V / Select-Object -Last N) -join [Environment]::NewLine; throw "S"`. The rest is the module preamble and the Export list. Two files that both report a failed external step by quoting its last lines; three lines with different messages either side. Budget pinned
windows/scripts/modules/WindowsTargetArch.Common.psm1 | windows/scripts/modules/WindowsTargetArch.Common.psm1 | 19 | reviewed 2026-09-09, READ: two runs, both deliberate. The 33-line one is the amd64 entry of $script:ArchTable against the arm64 entry (lines 37-72 vs 80-97): the SAME key set with per-arch values IS the table - ClangTriple, VsDevCmdArch, PeMachine, VcpkgTriplet, MsvcTargetLibDir, VulkanLibDir, PythonWheelTag, RustTarget, QnnLibDir and the rest - and each key carries its own comment on the amd64 side explaining what consumes it. The 8-line one is the PE import-descriptor walk (20-byte records, Name RVA at +12) against the delay-load walk (32-byte records, DllNameRVA at +4): two distinct PE/COFF structures whose record sizes and field offsets ARE the spec, and a shared walker would have to take both the $rvaToOffset and $readAscii scriptblocks plus the stride and the offset - six parameters for two call sites in one function. Budget pinned
windows/scripts/tests/BuildDriver.Retry.Tests.ps1 | windows/scripts/tests/BuildDriver.Retry.Tests.ps1 | 19 | reviewed 2026-09-09, READ: longest shared run 4 line(s) — `It "S" { / $tail = '<a real buildkit failure line>' / $r = Invoke-TransientCooldown -Tail ... -PreviousTail ... -Attempt 1 -MaxAttempts N -CooldownSeconds 0 -Label 't' / Assert-* `. The identical part is the call; the differing part is the failure TEXT, and these are captured incident strings (hcsshim::ImportLayer, the windows-layer mount tail, the '#9 627.3' buildkit timing prefix). The corpus is the test, each string is quoted once, and the call must stay spelled the same for the pass/fail contrast to mean anything. Budget pinned
windows/scripts/tests/Native.ArgQuoting.Tests.ps1 | windows/scripts/tests/TestHarness.psm1 | 19 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) — there is no shared block here at all. The 19 shingles are all against TestHarness.psm1's Get-ScriptFunctionDefinition (line 185), whose `[Parser]::ParseFile` + `$ast.FindAll({ param($n) $n -is [...Ast] -and ... }, $true)` walk is the AST idiom every suite in this tree uses; the harness function exists precisely because 9 sites had grown that boilerplate, and its docstring says so. A suite that also walks an AST for its own purpose is using the idiom, not copying the function. Budget pinned
windows/scripts/tests/TargetArch.PeInspection.Tests.ps1 | windows/scripts/tests/TargetArch.PeInspection.Tests.ps1 | 19 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. Every case here writes a handful of PE header bytes and reads the machine word back; the shingles it shares with itself are the `New-Object byte[]` / index-assignment idiom, whose indices and values fold to N and are the entire content. A shared idiom, not a copy. Budget pinned
windows/Build-Buildkit.ps1 | windows/Build-Buildkit.ps1 | 18 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. A byte-identical 6-line block pasted twice in ONE file: Build-Buildkit.ps1:743-748 and 763-768, the -NoCacheStage matched-nothing gate (`$unmatchedNoCacheStage = @($NoCacheStage / Where-Object { -not $script:NoCacheStageMatched.ContainsKey($_) })` then a three-line throw). The file's own comment at 760 explains why there are TWO SITES -- the first fires pre-export inside the `final` block, the second covers runs without 'final' where that site never executes -- and that is a reason for two CALLS, not two copies. The gate measures it at 18 shingles with a 6-line identical run, the largest self-pair run in this scope. One local function, no mode flag, both sites become one line; the next-largest unit pair in this file is 9 shingles, under the threshold of 10, so the row disappears rather than shrinks. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/build/Build-OnnxFromSource.ps1 | 18 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s) -- below any threshold worth extracting a helper for. Two source-build siblings sharing the CMake/arg-accumulation and inline-patch idioms whose generic halves already have owners. A shared idiom, not a copy. Budget pinned
windows/scripts/host/Set-ContainerdConfig.ps1 | windows/scripts/host/Update-HostVhdx.ps1 | 18 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) -- the two opening lines of the live-build guard, the fourth pairing of the four-script family argued in full on the Optimize-HostVhdx <-> Set-ContainerdConfig row: each caller's throw text differs in what it reports and whether a transcript is flushed first, so one owner would need three flags to serve four callers. Budget pinned
windows/scripts/host/Test-HostSetup.ps1 | windows/scripts/modules/WindowsBuild.Common.psm1 | 18 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only -- a trailing `[string]$Name = ''` parameter and the closing `)` of a param() header. A param block is a language-mandated shape that cannot live anywhere else. Budget pinned so a real copy still trips it
windows/scripts/modules/WindowsBuild.Common.psm1 | windows/scripts/modules/WindowsFormatting.Common.psm1 | 16 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 6 line(s), 3 of them substantive: the missing-path guard `if (-not (Test-Path $V)) { Write-BuildLog -Context $V -Message "S"; return $V }`. Both modules log through the same Write-BuildLog family, which is what makes the lines identical after normalisation; the paths tested and the messages logged are unrelated. A shared idiom, not a copy. Budget pinned
windows/scripts/modules/WindowsGstPlugins.Common.psm1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 18 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 7 line(s), 3 of them substantive, and those three are the multi-line `throw ("..." + "...")` concatenation both use for long operator-facing diagnostics - different messages, identical only after the normaliser folds every string to "S". Same finding as the GstPlugins/RustToolchain row in this file. Budget pinned
windows/scripts/tests/BuildKit.ModuleClosure.Tests.ps1 | windows/scripts/tests/BuildKit.ModuleClosure.Tests.ps1 | 18 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — `} / $runs / }` (a scriptblock returning its accumulator) and the two-line `$bad / Should -BeNullOrEmpty -Because (...)` tail. Both are structural: the shape of a collector scriptblock and the shape of a failure message, not a copied gesture. The long -Because strings that carry this suite's real content fold to "S" and are what the reader is meant to see. Budget pinned
windows/scripts/tests/BuildKit.PatchedLlvm.Tests.ps1 | windows/scripts/tests/BuildKit.SolveOrderParity.Tests.ps1 | 18 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — repo-root walk plus two Get-Content -Raw lines. These two suites are deliberately independent halves of the BuildKit contract (the patched-LLVM stage vs the media solve order); they read one file in common (Build-Buildkit.ps1) because it is the driver both are about, and they read it separately so either can change what it asserts without touching the other. Three lines of preamble is not a dedup target. Budget pinned
windows/scripts/tests/BuildKit.PatchedLlvm.Tests.ps1 | windows/scripts/tests/SourceBuild.FindPythonPrefix.Tests.ps1 | 18 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — the same source-text preamble as the SolveOrderParity/FindPythonPrefix row: a repo-root Split-Path chain plus two Get-Content -Raw lines naming DIFFERENT files (Build-Buildkit.ps1 and Dockerfile.toolchain-builder here). Three lines, two of which are the suite's declaration of what it reads. Get-RepoRoot could own line 1; the block is idiom-suppressed at >6 owners so that change retires no row. Budget pinned
windows/scripts/tests/BuildKit.SolveOrderParity.Tests.ps1 | windows/scripts/tests/SourceBuild.FindPythonPrefix.Tests.ps1 | 18 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — `$repoWin = Split-Path (Split-Path $PSScriptRoot -Parent) -Parent` plus two `Get-Content -Raw (Join-Path $repoWin '<file>')` lines. That is how every source-text suite in this tree opens, and the files each one reads (Dockerfile.media-builder / Build-Buildkit.ps1 / the Build-*FromSource scripts) are the subject. The repo-root walk has an owner already (Get-RepoRoot in TestHarness.psm1) and adopting it is worth doing as hygiene, but it would retire nothing: the gate suppresses that block as idiom at >6 owners (measured 7 shingles across 19 files). Budget pinned
windows/scripts/tests/WindowsSmokeTest.Common.Tests.ps1 | windows/scripts/tests/WindowsSmokeTest.Common.Tests.ps1 | 18 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 5 line(s) — `$s = Get-SmokeTestSummary` followed by four `Assert-Equal N $s.<Field> "S"` lines. The counters are a fixed set (Passed/Failed/Skipped/Total/Aborted) and every case asserts the whole set, deliberately, so that a counter which stops being updated fails somewhere rather than being silently unchecked. Folding the five lines into an Assert-Summary helper would take five parameters to serve four callers. Budget pinned
shared/windows/tests/Submodule.Pins.Tests.ps1 | shared/windows/tests/Submodule.Pins.Tests.ps1 | 17 | reviewed 2026-09-09, READ: the two units are the `It 'has every configured submodule initialised'` (line 195) and `It 'has no submodule checked out away from its recorded commit'` (line 210) blocks. Longest identical run 6 lines, and all six are the Pester assertion skeleton: `It "..." {` / `if ($script:X.Count -gt 0) {` / a banner Write-Host / `$script:X / ForEach-Object { Write-Host ... }` / `throw ("..." +` / `"..." +`. Every token of substance is the message, and the message IS the deliverable: one tells you to check out with `submodules: true` in actions/checkout, the other to restore with `git submodule update --checkout --recursive` or update the gitlink deliberately. A shared `Assert-EmptyPinSet -Items -Banner -Message` costs three parameters to save four lines and moves the failure text one indirection away from the `It` a reader opens the suite to read. Budget pinned so a fifth copy still trips the gate.
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/diagnostics/Test-OpencvVideoBackends.ps1 | 17 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- no contiguous block, only scattered shingles over shared GStreamer/OpenCV vocabulary. A hand-run diagnostic must stay self-contained: it is what an operator runs when the build script is not the thing being trusted. Budget pinned
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 17 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. The script imports this module (:81) and the residual overlap is the shape of that relationship. Budget pinned
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/tests/SourceBuild.PkgConfig.Tests.ps1 | 17 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. A Pester suite that asserts the pkg-config emitter's behaviour necessarily restates the shapes it checks -- that restatement is the test oracle. Merging would make the test assert itself. Budget pinned
windows/scripts/build/Test-Toolchain.ps1 | windows/scripts/host/Install-ScoopTools.ps1 | 17 | reviewed 2026-09-09, READ: a DELIBERATE install-side/verify-side pair, and the code names it. The 7-line run is the Vulkan ARM64 tri-state gate (`Write-Host ok / elseif ($strict) { throw .. } / else { Write-Warning .. }`) at Test-Toolchain.ps1:238 and Install-ScoopTools.ps1:233, and Test-Toolchain.ps1:241 states the relationship: 'Same escape hatch as the install step ... mirrors Install-ScoopTools.ps1's install-side gate and the CUDA_STACK_STRICT idiom.' The two must stay independent: the install layer has to fail on its own even when the verify script is absent, and they sit in different image closures. Budget pinned
windows/scripts/diagnostics/Find-LsmEventHolder.ps1 | windows/scripts/diagnostics/Find-LsmEventHolder.ps1 | 15 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 1 line(s) within this 240-line file -- not one consecutive pair repeats. The gate's 58-shingle family here is the three `& $cdb.FullName -pv -p <pid> -y $sym -c '<commands>' > <log> 2>&1` invocations at :166, :183 and :225, and what differs between them is the entire payload: pass 1 runs `.reload /f; ~*kb; qd` over every silo svchost, pass 2 runs `.reload /f; ~<idx>s; r r10; qd` against one thread of one process, and the third reads `!handle <h> f` on the located handle. The comments at :160-164 and :180-183 explain why they cannot be combined -- 'Combining them as `~*e .printf ...; kb` makes .printf swallow the semicolons ... and NO stack is printed at all - measured 2026-09-02'. A local helper would take the command string and the log path and replace one line at each of three call sites, saving nothing and burying the measured cdb quoting hazard. Below any threshold worth extracting a helper for. Budget pinned.
windows/scripts/modules/WindowsBuildDriver.Common.psm1 | windows/scripts/modules/WindowsCMake.Common.psm1 | 17 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s), longest substantive run 2 (`if ($V) {` then `try {`). Nothing above the level of PowerShell control flow is shared between the host-side build driver and the CMake step runner. A shared idiom, not a copy. Budget pinned
windows/scripts/tests/SourceBuild.CrossHelpers.Tests.ps1 | windows/scripts/tests/SourceBuild.NinjaTargets.Tests.ps1 | 17 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 5 line(s) — the Pester BeforeAll preamble (repo-root walk / Import-Module of the module under test / temp dir named after the suite / New-Item). Same verdict as the CrossHelpers/Qnn row: the Import-Module line names the subject and cannot move, the wbt-<suite> prefix exists so a leaked directory identifies its owner, and the two lines that DO have owners (Get-RepoRoot, New-TestDir) sit in a block the gate already suppresses as idiom at >6 owners, so adopting them retires no row. Budget pinned
windows/Build-Buildkit.ps1 | windows/scripts/diagnostics/Test-CudaCache.ps1 | 16 | reviewed 2026-09-09: EXTRACTION IDENTIFIED, NOT YET APPLIED. Same block and same extraction as row 404 -- Build-Buildkit.ps1:153-157 against Test-CudaCache.ps1:35-39, the buildctl Stevedore candidate list plus its not-found throw. Counted here as 16 shingles because Test-CudaCache writes the call on one line and spells the command 'buildctl.exe'. retires_rows is 0 only to avoid double-counting: the single Resolve-BuildCtlPath owner described on row 404 retires 404, this row, and row 472 (Invoke-DiagnosticProbe <-> Test-CudaCache, budget 16, which is the third face of the same triangle and sits in the diagnostics reviewer's scope). Do not perform this as a separate change. Plan and verification in docs/refactoring-backlog.md F4; the budget stays at the measurement until the work lands.
windows/scripts/Invoke-Lint.ps1 | windows/scripts/tests/Invoke-Tests.ps1 | 16 | reviewed 2026-09-09, READ: longest identical run 5 lines, and once the strings are put back nothing is shared. Invoke-Lint.ps1:179-184 reports the files PSScriptAnalyzer crashed on and warns that lint coverage is incomplete; Invoke-Tests.ps1:129-133 reports skipped Pester suites and prints the Install-Module line that fixes them. What matches is only the red-list skeleton `if ($x.Count -gt 0) { Write-Host <header> -ForegroundColor Red; foreach ($y in $x) { Write-Host <item> -ForegroundColor Red }; Write-Host <trailer> -ForegroundColor Red }`. A helper would take header, items, trailer and colour -- four parameters for four lines apiece -- and would put an indirection between two top-level gate scripts and the exact wording of their failure output, which is the one thing an operator reads. Budget pinned.
windows/scripts/build/Build-FfmpegFromSource.ps1 | windows/scripts/build/Build-IreeFromSource.ps1 | 16 | reviewed 2026-09-09, READ: a NORMALISATION ARTEFACT. The 6-line run is `[ordered]@{ 'S'='S'; ... }` -- the gate folds every string literal to "S", so two unrelated ordered tables of similar arity measure identical. Build-FfmpegFromSource.ps1:507 holds inline patch targets that are GENERATED by configure; Build-IreeFromSource.ps1:269 holds per-TU flags for arm_64 ukernels. No entry is shared. There is no block to own. Budget pinned
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/build/Build-LitertFromSource.ps1 | 16 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Two source-build siblings whose shared scaffolding already has an owner. A shared idiom, not a copy. Budget pinned
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/build/Build-OpencvFromSource.ps1 | 16 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s) -- the inline-patch call shape, whose owner (Invoke-InlineRegexPatch, WindowsSourceBuild.Patches.psm1) already exists. Below any threshold worth a further helper. Budget pinned
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/diagnostics/Test-CudaRuntimeClosure.ps1 | 16 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. A build script and a hand-run CUDA closure diagnostic in different closures; the diagnostic stays standalone by design. Budget pinned
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/host/Test-HostSetup.ps1 | 16 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Test-HostSetup.ps1 is a host-side preflight an operator runs before any build; it is self-contained on purpose so it can report on a host where the build modules are not present. Budget pinned
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/modules/WindowsCMake.Common.psm1 | 16 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only. WindowsCMake.Common.psm1 already owns the CMake invocation helpers; what is shared is the argument-list idiom around them. A shared idiom, not a copy. Budget pinned
windows/scripts/build/Build-LitertLmFromSource.ps1 | windows/scripts/modules/WindowsSourceBuild.Patches.psm1 | 16 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. This script is the LiteRT-LM patch stack and the module is the patch primitive owner; the overlap is the call relationship between them. Budget pinned
windows/scripts/build/Build-TargetCpython.ps1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 16 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (Copy-Item/Write-Host/brace inside sibling staging steps). The module already owns the CPython scaffolding this script uses (Copy-CpythonPyConfigHeader at :404). A shared idiom, not a copy. Budget pinned
windows/scripts/diagnostics/Invoke-DiagnosticProbe.ps1 | windows/scripts/diagnostics/Test-CudaCache.ps1 | 16 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 2 line(s) (Invoke-DiagnosticProbe.ps1:49-50 / Test-CudaCache.ps1:21-22) and both are structural -- a param entry and a closing brace. The 16 shingles are the repo-root climb and the `Import-Module ... WindowsScripts.Shared.psm1` line that BOTH files use to reach the shared module. That line is the call into the owner, not a block wanting one; the two differ in how they compute the module root because Invoke-DiagnosticProbe must work in both layouts ('repo layout is scripts/<group>/ while every container mount stays FLAT', :93-96) and Test-CudaCache only ever runs from the repo. Extracting a bootstrap that finds the module would need the module to find it. Budget pinned so a real copy still trips it.
windows/scripts/host/Install-Cuda.ps1 | windows/scripts/host/Install-Cuda.ps1 | 16 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) -- `$dest = Join-Path $env:TEMP '<file>'` followed by `$sha = Resolve-ContainerImageValue -EnvironmentVariable '<PIN>' -DefaultValue '...'`, once for the CUDA installer and once for the cuDNN archive. Two different artifacts set up through the one helper that already owns the pin lookup; the second-largest self-overlap is a two-line mkdir idiom. Nothing left to own. Budget pinned
windows/scripts/modules/WindowsRustToolchain.Common.psm1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 16 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 5 line(s), longest substantive run 2 (`$V = Join-Path $V "S"` then `if (Test-Path $V) {`). Probing for a file under a directory is not extractable. Budget pinned
windows/scripts/tests/SourceBuild.BuildMachineMsvcTool.Tests.ps1 | windows/scripts/tests/WindowsSlang.Common.Tests.ps1 | 16 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — `$x = Join-Path $tmp "S"` plus two `New-Item -ItemType Directory -Force -Path (Join-Path $V "S") / Out-Null`. One suite is building VC\Tools\MSVC\...\bin\HostX64, the other a shader include tree; after the gate folds every path to "S" the two are indistinguishable, and they share not one character of real text. A fixture-tree shape, not a copy. Budget pinned
windows/scripts/tests/SourceBuild.BundleManifest.Tests.ps1 | windows/scripts/tests/SourceBuild.BundleManifest.Tests.ps1 | 16 | reviewed 2026-09-09, READ: longest shared run 6 line(s), and the pair it comes from is deliberate — `BUNDLE-ENV.cmd registers the bundle root and arch` and `BUNDLE-ENV.ps1 registers the same facts in PowerShell syntax` assert the SAME three facts in two different syntaxes (`set "PATH=` vs `env:PATH =`, `KATA_BUNDLE_ARCH=amd64` vs `KATA_BUNDLE_ARCH = 'amd64'`). The regexes are the entire content and they must not be shared: the second case exists precisely because the two emitters can drift. Budget pinned
windows/scripts/tests/SourceBuild.Handoff.Tests.ps1 | windows/scripts/tests/SourceBuild.Handoff.Tests.ps1 | 16 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 5 line(s) — the file-endpoint arrange (`$server = Join-Path $dir 'srv'` / `New-Item ... 'bkhandoff'` / `$endpoint = 'file:///' + ($server -replace '\\','/')`). Three cases need the identical local endpoint because what they differ in is what happens to the tree afterwards (export/import round trip, a deleted subtree whose parent chain must be recreated, an import into a fresh directory). Hoisting it into a BeforeEach would put the directory the second case deliberately deletes outside the case that deletes it. Budget pinned
windows/scripts/tests/SourceBuild.MesonLogExcerpt.Tests.ps1 | windows/scripts/tests/SourceBuild.MesonLogExcerpt.Tests.ps1 | 16 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. The shingles are the shared call to the excerpt helper across four cases that feed it different meson log shapes; nothing contiguous. A shared idiom, not a copy. Budget pinned
windows/scripts/tests/SourceBuild.Resolve.Tests.ps1 | windows/scripts/tests/SourceBuild.Resolve.Tests.ps1 | 16 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 9 line(s), and those nine are an entire It block (`Invoke-InTestDir` / `New-Item` a subdirectory / `Invoke-WithEnv @{ <ROOT_VAR> = $dir }` / one Assert-Equal / three closing braces). Every Resolve-* case is that shape because every one of them answers the same question about a different SDK root (TensorRT, cuDNN, ...); the env var name and the expected path fold to "S" and are the whole case. A shared runner would take the env name, the subdirectory and the expectation — three parameters for cases that are four lines each. Budget pinned
windows/scripts/tests/WindowsSlang.Common.Tests.ps1 | windows/scripts/tests/WindowsSlang.Common.Tests.ps1 | 16 | reviewed 2026-09-09, READ: longest shared run 7 line(s) and it is a WGSL SHADER LITERAL — `Set-Content -Path $badWgsl -Encoding utf8 -Value @('struct VertexOut', '{', ' @builtin(position) pos : vec4<f32>,', ...)`, folded to `"S"` per line so two different shader fixtures read as one block. The shader source is the input the validator is being run against; there is no owner for a test's input data. The 3-4 line runs elsewhere are `Test-SlangcVersionAtLeast -Have '...' -Want '...' / Should -Be $true` pairs, where the version strings are the case. Budget pinned
windows/scripts/Invoke-Lint.ps1 | windows/scripts/build/Test-Container.ps1 | 15 | reviewed 2026-09-09, READ: the same red-list skeleton as the Invoke-Lint <-> Invoke-Tests row, measured at a 5-line identical run between Invoke-Lint.ps1:179-184 and Test-Container.ps1:1511-1517. Not even the control flow matches: Test-Container's copy ends in `exit 1` because a smoke failure is terminal, while Invoke-Lint's falls through to a verdict block that can still exit 2 for an infrastructure fault or 0 for a clean run. The contents differ completely (PSSA crash files vs Get-SmokeTestSummary FailureDetails). A shared printer would take header, items and trailer and would sit between a lint gate and a container smoke gate that share no other line. Budget pinned.
windows/scripts/build/Build-TvmFromSource.ps1 | windows/scripts/host/Install-Cuda.ps1 | 15 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Shared CUDA path vocabulary between an installer and a consumer, not shared logic. Budget pinned
windows/scripts/build/Test-Container.ps1 | windows/scripts/host/Install-Cuda.ps1 | 15 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only. An install-side script and the smoke gate that verifies its result, in different image closures; both naming the same CUDA paths is the contract between them, not a copy. Budget pinned
windows/scripts/build/Test-Container.ps1 | windows/scripts/tests/Invoke-Tests.ps1 | 15 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s) -- below any threshold worth extracting a helper for. Invoke-Tests.ps1 is the Pester HARNESS (it is what provides Get-RepoRoot and ConvertFrom-VersionsEnv to every suite); Test-Container.ps1 is the in-image smoke gate. Coupling the harness to the gate it may one day run would be circular. A shared idiom, not a copy. Budget pinned
windows/scripts/diagnostics/Measure-BuildWarnings.ps1 | windows/scripts/modules/WindowsAgenticLoop.Common.psm1 | 15 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 2 line(s) (Measure-BuildWarnings.ps1:6-7 / WindowsAgenticLoop.Common.psm1:116-117) and they are comment-based-help lines, not code. The 15 shingles are the warning-classification vocabulary -- the compiler diagnostic patterns (C4xxx / warning: / -W...) that a measurement script and the agentic loop that consumes its output both have to name. Two components at opposite ends of one pipeline spelling the same regexes is a contract, and the right fix if it ever grows is to move the pattern table, not the surrounding code; at 15 shingles across a script and a module with no common import today that is not yet worth a new dependency edge from a diagnostics script to WindowsAgenticLoop.Common.psm1. A shared idiom, not a copy. Budget pinned so growth trips the gate.
windows/scripts/diagnostics/Test-BuildCopy.ps1 | windows/scripts/diagnostics/Test-BuildCopy.ps1 | 15 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 2 line(s) within this 157-line file (150-151 vs 154-155), one of them structural -- the `Write-Host '...' -ForegroundColor Red` / `exit 1` pair that closes the INCONCLUSIVE branch and again the FAILED branch of the final verdict. Two exit-1 arms of one if/else are the shape of a fail-closed verdict, not a copied block, and merging them would erase the distinction the probe exists to report: 'no lane could run' is explicitly NOT a healthy verdict but is also not the build-COPY defect. Note this file also pastes the docker.exe and buildctl candidate walks; those are being retired against Get-PreferredToolPath under the Test-LayerRename / Test-ProcessIsolationCommit row and this budget will fall on its own when that lands. Budget pinned meanwhile.
windows/scripts/diagnostics/Test-CudaRuntimeClosure.ps1 | windows/scripts/tests/BuildKit.ModuleClosure.Tests.ps1 | 15 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 3 line(s) and it is lines 1-3 of BOTH files -- the Kataglyphis/SPDX copyright pair and the `#requires` line the license and lint gates require in every file. The 15 shingles beyond it are the closure vocabulary: both walk a dependency set and assert nothing is missing, one over CUDA runtime DLLs in an image and one over PowerShell module imports in the repo. Different subjects, different resolvers (Get-ChildItem over a container path vs AST import parsing), and the test suite must not depend on a diagnostics script that only runs against a live image. Merging would couple a Pester gate to a host probe. Budget pinned at the measurement so a real copied block still trips it.
windows/scripts/diagnostics/Test-OpencvCudaCmdshape.ps1 | windows/scripts/diagnostics/Test-SccacheOptionsStrict.ps1 | 15 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- `#requires -Version 7.0`, the license-gate copyright pair and the `<#`/`.SYNOPSIS` opener. The rest is the in-container probe preamble (a call into WindowsSourceBuild.Common.psm1, already owned) and the sccache server reset with per-probe ports (`43${i}0` vs 4770). Budget pinned.
windows/scripts/diagnostics/Test-SccacheOptionsStrict.ps1 | windows/scripts/diagnostics/Test-SccacheRelativeFo.ps1 | 15 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- the `#requires` line, the mandated copyright pair and the comment-based-help opener. The rest is the in-container probe preamble and the sccache server reset, which these two must NOT share: ports differ (4770 vs 4750) and Test-SccacheRelativeFo starts its server inside `Push-Location 'C:\'` / `finally { Pop-Location }` because the server's inherited working directory is exactly the variable that probe measures. An owner would own the subject. Budget pinned.
windows/scripts/host/Clear-DiskSpace.ps1 | windows/scripts/host/Clear-DiskSpace.ps1 | 15 | reviewed 2026-09-09, READ: longest shared run 3 line(s) -- `param([Parameter(Mandatory)][string]$Path)` / `try {` / `$x = Get-Item -LiteralPath $Path -Force -ErrorAction Stop`, the opening of Test-HasReparsePoint (:138) and of Get-EntrySizeGB (:153). Same three lines, opposite jobs and, more importantly, OPPOSITE failure semantics: the reparse probe returns $true on any error ("Cannot tell = treat as unsafe", its own comment, and design rule 4 in this file's header) while the sizer returns 0.0. Folding their preamble together would put this file's fail-closed safety check and its cosmetic size report behind one Get-Item, in the script whose header calls its allowlist "the whole security boundary". Budget pinned
windows/scripts/host/Optimize-HostVhdx.ps1 | windows/scripts/host/Optimize-HostVhdx.ps1 | 15 | reviewed 2026-09-09, READ: longest shared run 2 line(s) -- `$physical<X> = (Get-Item $VhdxPath).Length` and `$free<X> = (Get-PSDrive (Split-Path $VhdxPath -Qualifier).TrimEnd(':')).Free`, taken once BEFORE the compaction (:112) and once AFTER (:210) so the four numbers can be subtracted into "reclaimed" and "host free delta". The symmetry IS the measurement: a helper returning both would still be called twice and would cost the reader the ability to see, at the line that prints a delta, which of the two snapshots each half came from. Budget pinned
windows/scripts/host/Test-HostSetup.ps1 | windows/scripts/host/Test-HostSetup.ps1 | 15 | reviewed 2026-09-09, READ: longest shared run 5 line(s), and every one of them is a CALL SITE of this file's own Write-Check helper -- the three-line `Write-Check FAIL "<what>" `` / '<why it matters>' `` / '<fix>'` form, once for CNI subnet drift (:157) and once for disk headroom (:246), with the strings that are the entire content folded to "S" by the gate's normaliser. Write-Check was already extracted (line 50) and does the colouring, the indenting and the $script:Fail counting; what repeats is its four-argument signature spread over three continued lines. Budget pinned
windows/scripts/modules/WindowsAgenticLoop.Common.psm1 | windows/scripts/modules/WindowsVulkanValidation.Common.psm1 | 15 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s), 2 substantive: `$V.WaitForExit()` then `$V = $V.ExitCode`. That is System.Diagnostics.Process's API in the only order it can be called, not a copied block. Budget pinned
windows/scripts/modules/WindowsBuild.Common.psm1 | windows/scripts/modules/WindowsWebDav.Common.psm1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 6 line(s), 2 substantive, and those two are the module-load guard both files carry verbatim by convention - `$path = Join-Path $PSScriptRoot "S"` then `if (-not (Get-Module -Name "S")) { Import-Module $path }`. That is the no -Force rule both headers document, which exists precisely so a nested force-reimport cannot move a module's exports out of the global session state. Language shape, not a dedup target. Budget pinned. NOTE for whoever lands the WindowsFormatting/WindowsWebDav delegate repoint: this pair then MEASURES 15, not 13 - one more shingle unsuppressed on each side because removing the inlined delegates dropped a block below the gate's >6-owner idiom cutoff. Measured with AND without an added Import-Module and it is 15 either way, so it is a suppression flip, not a new copy - the same bookkeeping several rows in this file already record
windows/scripts/modules/WindowsClang.Common.psm1 | windows/scripts/modules/WindowsFormatting.Common.psm1 | 15 | reviewed 2026-09-09, READ: longest shared run 5 line(s), 3 substantive - the tool-resolution guard `$V = Get-Command "S" -ErrorAction SilentlyContinue; if (-not $V) { throw "S" }`. Each names a different tool (clang-tidy against cmake-format / clang-format) and throws different install instructions. Three lines is the whole of it, and WindowsClang.Common's own header already records that it reuses Get-ProjectCppFiles from WindowsFormatting.Common rather than re-implementing the part that mattered. Budget pinned
windows/scripts/modules/WindowsConfig.Common.psm1 | windows/scripts/modules/WindowsContainerImage.Common.psm1 | 15 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 5 line(s) and exactly ONE of them carries a statement (`return $V`); the other four are closing braces and a param header. 15 shingles of scattered vocabulary overlap between a config-path resolver and an image-reference resolver. There is nothing here to own. Budget pinned
windows/scripts/rust/Build-Windows.ps1 | windows/scripts/rust/New-MsixPackage.ps1 | 15 | reviewed 2026-09-09, READ: longest identical run 8 line(s) -- the largest run in this whole review -- and it is the one shape that cannot be extracted at all. Build-Windows.ps1:138-145 and New-MsixPackage.ps1:311-318 are the top-level `exit 0 } catch { Write-BuildLogError ...; Write-BuildSummary ...; exit 1 } finally { Close-BuildLog ... }` epilogue. `catch` and `finally` must sit lexically on the `try` they belong to, so a helper could own only the three-line catch BODY, and moving `exit 1` inside a function turns the script's exit code into implicit control flow. Everything else in the pair already has an owner: New-BuildContext, Open-BuildLog, Write-BuildLog, Write-BuildSummary and Close-BuildLog all come from WindowsBuild.Common, and Invoke-BuildStep/Invoke-BuildExternal from the same module. The one remaining candidate -- the 4-line `$logDir = Join-Path $Workspace 'logs'` + mkdir preamble, which New-CiSession in windows/scripts/modules/Initialize-CiEnvironment.ps1 already covers for the four Invoke-Ci* drivers -- touches 3 of the 15 counted shingles, so the row would stand at 15 either way; it is worth doing on its own merits, not to retire this row. Budget pinned.
windows/scripts/tests/BuildKit.TwinParity.Tests.ps1 | windows/scripts/tests/BuildKit.TwinParity.Tests.ps1 | 15 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s), and all of it is failure-message tail — `$unmirrored / Should -BeNullOrEmpty -Because "..."` / `}` / `}`. The -Because strings that carry the meaning fold to "S", which is why three separate ARG/ENV parity cases read as one block. Structural only. Budget pinned so a real copy still trips it
windows/scripts/build/Build-FfmpegFromSource.ps1 | windows/scripts/modules/WindowsSourceBuild.Patches.psm1 | 14 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only. The patch primitives are already owned by this module; the script's share is the call shape. Budget pinned
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/host/Install-Vs.ps1 | 14 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- no contiguous block, only scattered shingles over MSVC path vocabulary. Install-Vs.ps1:29 records that its closure carries only three modules; nothing may be moved across that edge. Budget pinned
windows/scripts/build/Build-LitertFromSource.ps1 | windows/scripts/tests/SourceBuild.Artifact.Tests.ps1 | 14 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only. Same test-oracle relationship as the Build-OnnxGenaiFromSource edge of this suite: the expected artifact set is stated independently on purpose. Budget pinned
windows/scripts/build/Build-OnnxGenaiFromSource.ps1 | windows/scripts/tests/SourceBuild.Artifact.Tests.ps1 | 14 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. A Pester suite that asserts which artifacts a build produces must name those artifacts; that restatement is the test oracle, and importing it from the script under test would make the assertion vacuous. Budget pinned
windows/scripts/build/Build-OpencvFromSource.ps1 | windows/scripts/build/Debug-LitertlmLink.ps1 | 14 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Debug-LitertlmLink.ps1 is a hand-run link diagnostic, deliberately standalone so it can be dropped into a broken image. Merging would couple a debugging aid to a production build script. Budget pinned
windows/scripts/build/Copy-TargetPythonDeps.ps1 | windows/scripts/build/Copy-TargetPythonDeps.ps1 | 14 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- there is no contiguous block at all, only scattered shingles between sibling copy steps in one small script. A shared idiom, not a copy. Budget pinned
windows/scripts/build/Export-LitertLmBridge.ps1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 14 | reviewed 2026-09-09, READ: the 5-line run is the copy-if-source-exists-and-destination-does-not guard -- `if ((Test-Path $src) -and -not (Test-Path $dst)) { Copy-Item $src $dst -Force; Write-Host .. }` -- shared between Invoke-LiteRtLmSupportGraft (:116) and Copy-CpythonPyConfigHeader (:404). Three lines of guard around a Copy-Item; a helper for it would be pure indirection. A shared idiom, not a copy. Budget pinned
windows/scripts/diagnostics/Test-Arm64Prereqs.ps1 | windows/scripts/host/Install-Vs.ps1 | 14 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 2 line(s) and both are the Kataglyphis/SPDX copyright header the license gate requires in every file. The 14 shingles are the Visual Studio / MSVC discovery vocabulary -- vswhere paths, the ARM64 component ids, MSVC tools-root shapes -- named by a probe that ASKS whether the arm64 prerequisites are present and by the installer that PUTS them there. Those are opposite jobs: the probe must report a missing component as a finding, the installer must install it, and the shared owner for the read half already exists (Get-VisualStudioInstallPath / Get-MsvcToolsRoots in WindowsScripts.Shared.psm1) without either file being obliged to use it for its own question. A shared idiom over one subject, not a copied block. Budget pinned so growth trips the gate.
windows/scripts/diagnostics/Test-OpencvCudaCmdshape.ps1 | windows/scripts/diagnostics/Test-SccachePatchVerify.ps1 | 14 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- `#requires`, the mandated copyright pair and the `<#`/`.SYNOPSIS` opener. The rest is the in-container probe preamble plus the `C:\bkmnt\patch\*.patch` apply idiom, and Dockerfile.probe:16-21 records that this mount was removed with windows/upstream/sccache-nvcc-quote-fix by #137, naming Test-SccachePatchVerify one of three readers 'dead by construction -- backlog #159's subject'. Refactoring dead code into a shared owner would freeze the shape #159 exists to change. Budget pinned; revisit when #159 lands.
windows/scripts/diagnostics/Test-Sccache2726Repro.ps1 | windows/scripts/diagnostics/Test-SccachePatchVerify.ps1 | 14 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- the `#requires` line, the license-gate copyright pair and the comment-based-help opener. Beyond that both carry the git checkout + `C:\bkmnt\patch\*.patch` apply block, and they DIFFER where it counts: the repro globs every patch sorted and throws on an empty dir ('No subject is not a pass', :48-49), the verify takes `Select-Object -First 1` -- the very subsetting Test-SccachePrHygiene.ps1:38-41 documents as a defect that once graded a build green with 0002/0003 never applied. Both files are among the three Dockerfile.probe:16-21 marks 'dead by construction' since the mount was deleted, so the divergence is recorded here rather than resolved by a shared owner; backlog #159 owns whether these scripts are repaired or deleted. Budget pinned.
windows/scripts/host/Install-ScoopTools.ps1 | windows/scripts/host/Install-ScoopTools.ps1 | 14 | reviewed 2026-09-09, READ: longest shared run 5 line(s) -- the fetch-to-TEMP arm of the two aarch64 side-artifact installs (compiler-rt builtins mined out of the LLVM tarball at :267, OpenSSL-arm64 from the slproweb installer at :355): `$url = '...'` / `$tmp = Join-Path $env:TEMP '...'` / `try {` / `Write-Host '...'` / `Invoke-DownloadWithRetry -Url $url -DestinationPath $tmp`. The download itself already has one owner; below that line the two bodies diverge completely -- one extracts a single .lib from a tar.xz into the host clang lib directory, the other runs an .exe installer into C:\opt\openssl-arm64. Each is preceded by ~15 lines of comment recording the measured link failure it exists to prevent (`undefined symbol: __udivti3`; `machine type x64 conflicts with arm64`); a common owner would separate those notes from the code they explain. Budget pinned
windows/scripts/host/Invoke-BkWarm.ps1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 14 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) -- `$code = if (Test-Path Variable:\LASTEXITCODE) { $LASTEXITCODE } else { 0 }` and `if ($code) { throw '...' }`. That is the language-mandated way to read an exit code that may not exist yet under Set-StrictMode; WindowsSourceBuild.Common alone contains three separate instances of it (:508, :1381, :1564), which is why the shingles land three different ways. Idiom, not a copy. Budget pinned
windows/scripts/modules/WindowsSlang.Common.psm1 | windows/scripts/modules/WindowsTvm.Common.psm1 | 14 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) - the shortest of any pair reviewed in this pass. 14 shingles scattered across two files with no two consecutive identical lines anywhere beyond `return $V`. This is the normaliser matching PowerShell's vocabulary, not a copy. Budget pinned
windows/scripts/modules/WindowsSourceBuild.Common.psm1 | windows/scripts/modules/WindowsTvm.Common.psm1 | 14 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 5 line(s), 2 substantive, and those two are `if ($X) { $cmakeArgs += "-D...=$X" }` - the CMake option ladder, where the value of each line is the flag it names. Same finding as the WindowsSourceBuild.Common self-pair. Budget pinned
windows/scripts/modules/WindowsSourceBuild.Patches.psm1 | windows/scripts/patches/opencv/Get-Ffmpeg9AvcodecConfig.ps1 | 14 | reviewed 2026-09-09, READ: longest shared run 3 line(s) - one Join-Path to a directory inside a vendored source tree, then two more to the specific headers under it. Both files are source-tree patchers and both must locate exactly two headers before patching, but the trees (onnxruntime DirectMLHelpers against OpenCV modules/videoio/src), the filenames and the patches share nothing. Three Join-Path lines. Budget pinned
windows/scripts/python/Invoke-CiPackaging.ps1 | windows/scripts/python/Invoke-CiStaticAnalysis.ps1 | 14 | reviewed 2026-09-09, READ: this pair is the RESIDUE of an extraction that already happened, not a copy waiting for one. Longest identical run 2 lines: `. (Join-Path $PSScriptRoot '..\modules\Initialize-CiEnvironment.ps1')` and the `Initialize-CiEnvironment -ScriptRoot $PSScriptRoot -Modules @('WindowsBuild.Common','WindowsUv.Common') -EnterRepoRoot -RepoRoot $RepoRoot` call (Invoke-CiPackaging.ps1:42-46, Invoke-CiStaticAnalysis.ps1:46-50). The other counted blocks are the single long `New-UvProjectEnvironment -Workspace ... -CommandRunner ... -LogInfo ... -LogWarning ... / Out-Null` line, which is 10 shingles from one call, and `} finally { Remove-UvProjectEnvironment -EnvPath ... }`. Every one of those is a CALL to an owner that exists -- New-CiSession and the uv delegate triple from #141, the env lifecycle from WindowsUv.Common -- and both files carry the comment saying so. Deduplicating a shared helper's call sites is deduplicating the wrong end. Budget pinned.
windows/scripts/tests/Driver.ClosureScope.Tests.ps1 | windows/scripts/tests/Driver.ClosureScope.Tests.ps1 | 14 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s) — `} finally { Remove-Item $tmp -Force -ErrorAction SilentlyContinue } }` plus the `$tmp = Join-Path ([IO.Path]::GetTempPath()) ("closure-probe-" + [guid]::NewGuid().ToString('N') + '.ps1')` line. Two cases write a probe script to a temp file and clean it up; the probe bodies (a here-string per case) are the content and share nothing. This suite writes a .ps1 rather than a directory, so Invoke-InTestDir is not a drop-in — and four lines is below any threshold worth a helper. Budget pinned
windows/scripts/tests/ScriptAssetRoot.Parity.Tests.ps1 | windows/scripts/tests/ScriptAssetRoot.Parity.Tests.ps1 | 14 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s) and it is structural only — two closing braces, `Assert-True ($drifted.Count -eq 0) ("drifted resolver copies:`n" + ($drifted -join "`n"))`, and one more brace. That is the shape of every collect-then-assert case in this tree; the collection loops above it differ. Budget pinned so a real copy still trips it
.claude/hooks/guard-destructive-deletes.ps1 | windows/scripts/build/Build-LitertLmFromSource.ps1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 0 line(s) -- not one normalised line matches between the two units, the only such pair in this review. The 13 shingles are the token sequence of two consecutive `[regex]::Replace($x, "...", "...")` statements: guard-destructive-deletes.ps1:135-136 blanks single- then double-quoted spans out of a candidate command before scanning it for destructive verbs, and Build-LitertLmFromSource.ps1:529-533 rewrites two CMake link-spec conditions inside an Edit-SourceFile transform. A Claude Code safety hook and a LiteRT-LM CMake patcher share the .NET regex API and nothing else. Budget pinned.
shared/agentic-loop/templates/Invoke-AgenticLoop.ps1 | windows/scripts/build/Build-IreeFromSource.ps1 | 13 | reviewed 2026-09-09, READ: longest identical run 1 line (`}`). The shared shingles are the two-candidate probe `foreach ($x in @((Join-Path $a '...'), (Join-Path $b '...'))) { if (Test-Path $x) { ... } }`. In Invoke-AgenticLoop.ps1:30-34 that loop is the BOOTSTRAP that locates WindowsAgenticLoop.Common (third_party\ContainerHub first, vendored fallback second) -- it cannot call a helper out of the module it is still looking for, and the file's own header declares it a TEMPLATE to be copied into a consumer repo, so it must run standalone where none of ContainerHub's modules exist. Same bootstrap-paradox shape the gstreamer-env/libcamera-env row is kept for. In Build-IreeFromSource.ps1:369-378 the same two-element loop neutralises IREE's CleanEggInfo rmtree in compiler\setup.py and runtime\setup.py, and it visits BOTH hits rather than taking the first. One idiom, two unrelated jobs. Budget pinned.
windows/Build-Buildkit.ps1 | windows/scripts/host/Start-GeniexServers.ps1 | 17 | reviewed 2026-09-09, READ (this replaces the 'still not reviewed' half of the re-measurement note; the merge history it records stays true): longest identical run 3 line(s) and those lines are structural only (`}` / `}` / `}`). The units are Build-Buildkit.ps1:533, the media-stage branch loop that fans out one BuildKit solve per media branch, and Start-GeniexServers.ps1:252, the Start-Lane function that assembles a `geniex serve` argument list and launches one inference server per compute lane. The shingles are the conditional argument-append gesture `if ($x) { $args += @('--flag', $x) }` plus nested closing braces, which is how every PowerShell script that builds a native command line looks. Nothing of substance is shared, which is also why upstream's unrelated edits to Start-GeniexServers could move this count at all. Budget pinned.
windows/scripts/Invoke-Lint.ps1 | windows/scripts/tests/TestHarness.psm1 | 13 | reviewed 2026-09-09, READ: longest identical run 1 line (`}`). Shared is the mandatory AST-parse call and its error check -- Invoke-Lint.ps1:90 and TestHarness.psm1:194 -- with opposite policies: Invoke-Lint records the parse error because emitting parse errors is its product, Get-ScriptFunctionDefinition throws because it cannot lift a function out of a file that will not parse. TestHarness is itself the OWNER this boilerplate already got: its header (line 158-172) records that 9 fixture suites had grown the same ~12 lines of Parser::ParseFile boilerplate and now call Get-ScriptFunctionDefinition instead. Invoke-Lint is deliberately not one of those consumers -- it wants the errors, not a function body -- so it is the one legitimate remaining caller of the raw API. Budget pinned.
windows/scripts/build/Build-FfmpegFromSource.ps1 | windows/scripts/build/Build-GstreamerFromSource.ps1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- no contiguous block, despite both being media source builds. A shared idiom, not a copy. Budget pinned
windows/scripts/build/Build-FfmpegFromSource.ps1 | windows/scripts/build/Build-OnnxFromSource.ps1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only. Two source-build siblings; the flag ladders they share fold to "S" and hold no common entry. Budget pinned
windows/scripts/build/Build-FfmpegFromSource.ps1 | windows/scripts/diagnostics/Measure-WarningStream.ps1 | 13 | reviewed 2026-09-09, READ: a NORMALISATION ARTEFACT, the largest one in this scope -- an 8-line run that is entirely `[ordered]@{` plus seven folded `'S'='S'` entries plus the closing brace. Build-FfmpegFromSource.ps1:507 lists inline patch targets GENERATED by configure; Measure-WarningStream.ps1:38 lists warning classes with their rationale. Two unrelated tables that happen to have the same arity. There is no block to own. Budget pinned
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/modules/WindowsBuildKit.Common.psm1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- no contiguous block. A shared idiom, not a copy. Budget pinned
windows/scripts/build/Build-IreeFromSource.ps1 | windows/scripts/build/Build-OpencvFromSource.ps1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- no contiguous block. A shared idiom, not a copy. Budget pinned
windows/scripts/build/Build-IreeFromSource.ps1 | windows/scripts/diagnostics/Measure-WarningStream.ps1 | 13 | reviewed 2026-09-09, READ: a NORMALISATION ARTEFACT. The 6-line run is `[ordered]@{ 'S'='S'; ... }`; the gate folds every string to "S", so two unrelated tables of the same arity measure identical. Build-IreeFromSource.ps1:269 holds per-TU arm_64 ukernel flags; Measure-WarningStream.ps1:38 holds 'the classes worth acting on, with WHY' for compiler-warning triage. Nothing is shared but the literal's shape. Budget pinned
windows/scripts/build/Build-LitertLmFromSource.ps1 | windows/scripts/build/Build-TvmFromSource.ps1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Two source-build siblings whose shared scaffolding already has an owner in WindowsSourceBuild.Common. A shared idiom, not a copy. Budget pinned
windows/scripts/build/Build-LitertLmFromSource.ps1 | windows/scripts/build/Export-LitertLmBridge.ps1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only. Both belong to the LiteRT-LM patch/graft stack the allowlist header names as a protected twin class, and they run in different stages -- the patcher inside the source build, the bridge exporter after it. Budget pinned
windows/scripts/build/Build-OpencvFromSource.ps1 | windows/scripts/tests/TargetArch.Common.Tests.ps1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. A Pester suite restating the arch vocabulary it asserts over -- the test oracle, deliberately independent of the script. Budget pinned
windows/scripts/build/Copy-TargetPythonDeps.ps1 | windows/scripts/modules/WindowsCMake.Common.psm1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- no contiguous block. A shared idiom, not a copy. Budget pinned
windows/scripts/diagnostics/Test-Arm64Prereqs.ps1 | windows/scripts/diagnostics/Test-OpencvVideoBackends.ps1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 2 line(s) (Test-Arm64Prereqs.ps1:4-5 / Test-OpencvVideoBackends.ps1:2-3) and both are the Kataglyphis/SPDX copyright header. The 13 shingles are the probe skeleton -- `[CmdletBinding()]`, a `param([string]$Nonce = '')` that Dockerfile.probe passes to every probe by contract, `$ErrorActionPreference`, and a `=== <name> (nonce=$Nonce) ===` banner. The $Nonce parameter is mandated by the mount contract at Dockerfile.probe:45 (`-Nonce $env:PROBE_NONCE` is passed to whatever -ProbeScript names), so it cannot live anywhere but each script's own param block. Structural only. Budget pinned.
windows/scripts/diagnostics/Test-Arm64Prereqs.ps1 | windows/scripts/diagnostics/Test-SccacheWrite.ps1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 3 line(s) (Test-Arm64Prereqs.ps1:38-40 / Test-SccacheWrite.ps1:33-35) and those lines are structural only -- `[CmdletBinding()]`, `param(` and the closing brace of the param block, which PowerShell requires in each script and which cannot be moved into a module. The remaining shingles are the per-check reporting idiom the two use for different subjects (Add-Finding over arm64 prerequisites vs Write-Result over sccache writes). That is the shape of a probe, not a copied block. Budget pinned so a real copy still trips it.
windows/scripts/host/Install-Cuda.ps1 | windows/scripts/host/Install-Tensorrt.ps1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) -- two adjacent `[Environment]::SetEnvironmentVariable('<ROOT>', $path, 'Process')` calls publishing each installer's root to the rest of the RUN step. Both files carry the same comment explaining that process scope is deliberate and the durable value comes from the Dockerfile ENV line. Two lines of a .NET call with different variable names; nothing to own. Budget pinned
windows/scripts/host/Install-NewHost.ps1 | windows/scripts/host/Install-NewHost.ps1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 5 line(s), and every one is structural (`}` / `if ($ReportOnly) {` / `Write-Step '...'` / `return` / `}`) -- the early-out that each of this orchestrator's per-concern steps carries so that -ReportOnly reaches all of them and none acts. That is the shape of sibling steps in one driver, not a copied block; collapsing it would mean the steps could no longer differ in what they report before returning. Budget pinned so a real copy still trips it
windows/scripts/host/Update-HostVhdx.ps1 | windows/scripts/host/Update-HostVhdx.ps1 | 13 | reviewed 2026-09-09, READ: longest shared run 4 line(s) -- the ROLLBACK arm (`} catch { Write-Step ('...' -f $_.Exception.Message) 'Red'` / `Write-Step '...' 'Yellow'` / `Dismount-DiskImage -ImagePath $p -ErrorAction SilentlyContinue / Out-Null` / then `foreach ($s in $stopped) { Start-Service $s -ErrorAction SilentlyContinue }` / `Save-Transcript` / `throw`), once around the mirror phase (:209) and once around the swap (:264). They unwind DIFFERENT amounts of work -- the mirror catch dismounts the staging image, the swap catch has to leave the original path usable -- and both are best-effort by construction, because a rollback that can itself throw is worse than one that leaks a mount. That is the same distinction WindowsHostMaintenance.Common.psm1's header records when it keeps the restore halves in the scripts: "best-effort mid-flow vs must-succeed final", and these two are the mid-flow ones. Budget pinned
windows/scripts/modules/WindowsBuildDriver.Common.psm1 | windows/scripts/modules/WindowsTesting.Common.psm1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s), 2 substantive: `} catch { Write-Verbose "S" }` - a best-effort swallow with a verbose note, in two unrelated places (a git ls-remote tag probe and a test-discovery probe). A shared idiom, not a copy. Budget pinned
windows/scripts/tests/BuildKit.ModuleClosure.Tests.ps1 | windows/scripts/tests/Modules.ScriptCallClosure.Tests.ps1 | 13 | reviewed 2026-09-09, READ: longest shared run 4 line(s) — the BeforeAll that computes `$repoRoot`, `$moduleDir` and `$buildDir`. The two suites are the two halves of the same guard on purpose: ModuleClosure asks whether a script's imports are MOUNTED into the container, ScriptCallClosure whether the functions a script CALLS are in its import list. They must be able to fail independently, and their shared preamble is three path variables. Get-RepoRoot could own the first line; the rest is each suite's own scope. Budget pinned
windows/scripts/tests/Driver.ClosureScope.Tests.ps1 | windows/scripts/tests/SourceBuild.StageTargetPythonDeps.Tests.ps1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — `Assert-Equal N $V.Count "S"` followed by two `Assert-True ($names -contains "S") "S"` lines. Once the expected names fold to "S" (here 'Docker'/'MediaCoreCpus'; there wheel requirement names), a count-then-membership assertion in any two suites matches. Structural only. Budget pinned so a real copy still trips it
windows/scripts/tests/Modules.Orchestrators.Tests.ps1 | windows/scripts/tests/Pins.CanonicalValues.Tests.ps1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for, and all 13 shingles fall against ONE unit in Pins.CanonicalValues.Tests.ps1 (line 49) from four different orchestrator units, which is the signature of a shared idiom rather than a shared block. A shared idiom, not a copy. Budget pinned
windows/scripts/tests/Shared.SccacheVs.Tests.ps1 | windows/scripts/tests/Shared.SccacheVs.Tests.ps1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. The 13 shingles are spread over four sibling cases with nothing contiguous between any two. A shared idiom, not a copy. Budget pinned
windows/scripts/tests/SourceBuild.CmakeArgsConsumed.Tests.ps1 | windows/scripts/tests/SourceBuild.GstWrapProvisioning.Tests.ps1 | 13 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — one assertion line plus `} finally { Remove-Item $d -Recurse -Force -ErrorAction SilentlyContinue }` and a closing brace. Two suites that hand-roll a temp directory both end their cases the same way; that is the try/finally shape, not a shared block. Both would be better off calling Invoke-InTestDir (TestHarness.psm1 already owns it), which is a hygiene tidy worth doing and would retire nothing here. Budget pinned
shared/agentic-loop/templates/Invoke-AgenticLoop.ps1 | windows/scripts/build/Build-FfmpegFromSource.ps1 | 12 | reviewed 2026-09-09, READ: longest identical run 1 line (`}`), same two-candidate `foreach ($x in @((Join-Path ...), (Join-Path ...))) { if (Test-Path $x) { ... } }` idiom as the Build-IreeFromSource row, and the semantics are opposite. Invoke-AgenticLoop.ps1:30-34 takes the FIRST hit and breaks, because it is the standalone bootstrap that finds WindowsAgenticLoop.Common in a consumer repo that may have vendored it -- a TEMPLATE, per this file's own header, so it cannot depend on the module it is locating. Build-FfmpegFromSource.ps1:175-177 walks version_major.h then version.h per libX and CONCATENATES every hit, because n9.0 split the version macros across two headers. Budget pinned.
windows/scripts/build/Build-FfmpegFromSource.ps1 | windows/scripts/build/Build-LitertFromSource.ps1 | 12 | reviewed 2026-09-09, READ: a NORMALISATION ARTEFACT. The 7-line run is `[ordered]@{` plus six folded `'S'='S'` entries. Build-FfmpegFromSource.ps1:507 holds configure-generated patch targets; Build-LitertFromSource.ps1:242 holds per-TU flags for XNNPACK aarch64 microkernels. No entry is common -- only the literal's shape. Budget pinned
windows/scripts/build/Build-FfmpegFromSource.ps1 | windows/scripts/diagnostics/Test-Arm64Prereqs.ps1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. A hand-run diagnostic that must stay self-contained. Budget pinned
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/diagnostics/Test-Arm64Prereqs.ps1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 0 line(s) -- there is NO contiguous identical line anywhere between these two files. The 12 shingles are token-level coincidences across the arm64 prerequisite vocabulary after normalisation. Nothing exists to extract. Budget pinned
windows/scripts/build/Build-LitertLmFromSource.ps1 | windows/scripts/build/Test-Container.ps1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only. A builder and the smoke gate that verifies its output, in different image closures; naming the same artifacts is the contract between them. Budget pinned
windows/scripts/build/Build-OpencvFromSource.ps1 | windows/scripts/build/Build-OpencvGstreamerPlugin.ps1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only. Deliberately separate: Build-GstreamerFromSource.ps1:77 records that the standalone plugin route is 'a separate module ON PURPOSE: it is mounted by the merge builder only, so editing the contract cannot invalidate the six media compile RUNs'. The plugin builder is the STANDALONE route (#93) and must not take a dependency on the main OpenCV build. Budget pinned
windows/scripts/build/Build-OpencvFromSource.ps1 | windows/scripts/modules/WindowsBuild.Common.psm1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only. A shared idiom, not a copy. Budget pinned
windows/scripts/build/Build-TvmFromSource.ps1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- no contiguous block; a script calling the module that owns its scaffolding. Budget pinned
windows/scripts/build/Debug-LitertlmLink.ps1 | windows/scripts/modules/WindowsBuild.Common.psm1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Debug-LitertlmLink.ps1 is a standalone link diagnostic by design -- it must run in an image where the build modules may be exactly what is broken. Budget pinned
windows/scripts/host/Clear-DiskSpace.ps1 | windows/scripts/tests/Guard.DestructiveDeletes.Tests.ps1 | 12 | reviewed 2026-09-09, READ: longest shared run 0 line(s) -- the suite that guards Clear-DiskSpace builds its own rule hashtables in the SHAPE the script's Get-ReclaimRules produces (`@{ Root = $sandbox; Leaf = 'scratch-*'; Kind = 'Directory'; MinAgeDays = 7; What = 'fixture' }`) so it can drive Get-ReclaimPlan over a sandbox instead of over C:\ProgramData. That restatement is the test doing its job: importing the production rule table would make the suite assert against whatever the rules currently say, and a wrong rule would pass itself. Not one identical line survives the normaliser, which is the measurement that says so. Budget pinned
windows/scripts/host/Install-ScoopTools.ps1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- a bare `$path = Join-Path $root '<name>'`. The 12 shingles are spread across fourteen different unit pairs, none of which shares two consecutive lines; the widest single pairing is one Join-Path. A shared idiom, not a copy. Budget pinned
windows/scripts/host/Start-GeniexServers.ps1 | windows/scripts/host/Start-GeniexServers.ps1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only (`}` / `return $result` / `}`) -- the tails of sibling functions, which is the shape this file has, not a copied block. Every one of its nine self-overlaps is closing braces. Budget pinned so a real copy still trips it
windows/scripts/host/Update-HostVhdx.ps1 | windows/scripts/modules/WindowsBuild.Common.psm1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- a bare `$args = @(` opening an argument-array literal. Below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
windows/scripts/modules/WindowsBuildDriver.Common.psm1 | windows/scripts/modules/WindowsBuildKit.Common.psm1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s), 3 substantive, and those three are the tail of a multi-line `throw ("..." + "..." + "...")` - different messages, identical only after string folding. Both files raise long operator-facing diagnostics that name the fix (the sccache endpoint setup, the buildkit worker setup); that is house style, not a copy. Budget pinned
windows/scripts/modules/WindowsCMake.Common.psm1 | windows/scripts/modules/WindowsCodeQL.Common.psm1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 6 line(s), 4 substantive, and all four are consecutive `Write-BuildLog -Context $Context -Message "S"` lines with entirely different messages - the normaliser folds every string literal to "S". The same measurement artefact as the WindowsCMake.Common / New-MsixPackage.ps1 row. Budget pinned
windows/scripts/modules/WindowsFormatting.Common.psm1 | windows/scripts/modules/WindowsMsix.Signing.psm1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), 2 substantive, and those two are the module-load guard convention - `if (-not (Get-Module -Name "S")) { Import-Module (Join-Path $V "S") }` - which both files carry so a nested -Force re-import cannot move exports out of the global session state. Language shape, not a copy. Budget pinned
windows/scripts/modules/WindowsRepoHygiene.Common.psm1 | windows/scripts/modules/WindowsRepoHygiene.Common.psm1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s), 2 substantive, and one of those two is the `return [pscustomobject]@{ Head; Reachable; ContainingRef; Method }` result literal - the function's return SHAPE, emitted once per outcome branch with a different Method string. Identical field names are the contract every caller reads; merging the branches would hide which outcome produced which row. Budget pinned
windows/scripts/modules/WindowsSourceBuild.Common.psm1 | windows/scripts/tests/SourceBuild.BundleManifest.Tests.ps1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) and NEITHER of the two carries a statement. 12 scattered shingles between a module and the Pester suite that asserts against it - a suite naming the thing it tests is not duplication, and there is no contiguous block on either side to own. Budget pinned
windows/scripts/modules/WindowsWasmOpt.Common.psm1 | windows/scripts/modules/WindowsWasmOpt.Common.psm1 | 12 | reviewed 2026-09-09, READ: the 6-line run is the twin pin-resolution ladders in Get-BinaryenAsset - BINARYEN_VERSION and the per-arch SHA256 key, each 'environment, else versions.env, else throw'. They are not the same code: one reads a FIXED $env: name directly, the other a COMPUTED one through Test-Path Env:\$shaKey and Get-Item, and the two throws tell the reader different things to add and where. A shared resolver takes three parameters to serve two call sites inside one function. Budget pinned
windows/scripts/rust/New-MsixPackage.ps1 | windows/scripts/rust/New-MsixPackage.ps1 | 12 | reviewed 2026-09-09, READ: longest identical run 1 line. The two units are the signing branches at New-MsixPackage.ps1:293 (-CreateTestCertificate) and :301 (an explicit -CertificatePath), and the 12 shingles are one call made twice -- `Import-CertificateAndSign -CertificatePath ... -CertificatePassword ... -PackageFile ... -SigntoolExe ... -Context ...`, with `-CreatedAsTestCert` on the first. That helper IS the extraction; the twin is its two call sites differing by one switch, and collapsing them means hoisting the cert-CREATION branch into the signer, which is precisely what the two branches exist to keep apart. The smaller counted block is the same story: `$x = Resolve-WindowsSdkToolPath -ToolName ...` plus its IsNullOrWhiteSpace throw, at :200 for makeappx and :205 for signtool, two lines each, already owned by WindowsMsix.Common (the comment at line 70 records that migration). Budget pinned.
windows/scripts/tests/BuildDriver.MediaLane.Tests.ps1 | windows/scripts/tests/BuildDriver.MediaLane.Tests.ps1 | 12 | reviewed 2026-09-09, READ: longest shared run 5 line(s), and it is a PIN TABLE — `OPENCV_VERSION = 'cv-3'; FFMPEG_VERSION = 'ff-4'; PYAV_VERSION = 'av-5'; ...`, the fake versions.env two cases each declare. The names are aligned and the values are deliberately obvious fakes; folded to "S" they read as identical, but each case needs its own table because one of them removes a key (LITERT_VERSION) to prove the missing-pin failure names it. The other 5-line run is an Assert-Throws with a -MessagePattern, i.e. one assertion. Budget pinned
windows/scripts/tests/Dockerfile.ProbeShell.Tests.ps1 | windows/scripts/tests/Guard.DestructiveDeletes.Tests.ps1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — `Assert-True (Test-Path $x) "S"` / `$raw = Get-Content -Raw $x` / one Assert-Match. Both suites open a file under windows\ and assert about its text; the file, the pattern and the message all fold to "S". That is the source-text-assertion idiom this tree uses everywhere, not a copied block. Budget pinned
windows/scripts/tests/Driver.ClosureScope.Tests.ps1 | windows/scripts/tests/SourceBuild.MesonBuildSubprojectPatch.Tests.ps1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — a count assertion plus two `-contains` membership assertions, identical only after the expected names fold to "S". The two suites have nothing else in common (a closure-scope detector against a vendored meson patch check). Structural only. Budget pinned
windows/scripts/tests/SourceBuild.JobCount.Tests.ps1 | windows/scripts/tests/SourceBuild.JobCount.Tests.ps1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — `Invoke-WithEnv @{ BUILD_JOBS = "S"; MEMORY_LIMIT_GB = "S" } { / Assert-Equal N (Get-BuildJobCount -MemGBPerJob N) / }`. The env values and the expected count fold away, and they are the whole test: an empty BUILD_JOBS, a non-numeric one, and an explicit one all take a different branch. Three lines with two folded literals is not a block. Budget pinned
windows/scripts/tests/SourceBuild.MesonBuildSubprojectPatch.Tests.ps1 | windows/scripts/tests/SourceBuild.StageTargetPythonDeps.Tests.ps1 | 12 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — one `Assert-Equal N $V.Count "S"` plus two `Assert-True ($lines -contains "S") "S"` lines in one place, and `Assert-True $threw "S" / } / }` in another. Both are assertion tails whose content is entirely inside the folded strings (here the patched meson source lines, there a wheel METADATA fact). Structural only. Budget pinned
shared/agentic-loop/templates/Invoke-AgenticLoop.ps1 | windows/scripts/build/Build-LitertLmFromSource.ps1 | 11 | reviewed 2026-09-09, READ: longest identical run 1 line, third face of the same two-candidate `foreach ($x in @((Join-Path ...), (Join-Path ...)))` idiom. Invoke-AgenticLoop.ps1:30-34 is the standalone module bootstrap of a file its own header declares a TEMPLATE for copying into a consumer repo; the LitertLm counterpart at Build-LitertLmFromSource.ps1:850-851 walks `(Join-Path $vcpkgInstalledX64 'bin\z.dll'), (Join-Path $buildDir '..._deps\kissfft_lib-build\kissfft-float.dll')` and copies every hit into the staged bin directory. Selecting-the-first versus copying-them-all on the same loop shape. Budget pinned.
shared/config/Sync-SharedConfig.ps1 | shared/config/Sync-SharedConfig.ps1 | 11 | reviewed 2026-09-09, READ: longest identical run 3 line(s) and those lines are the function tail (`}` / `return $x` / `}`). The units are Get-AssetRegistry (line 84) and Get-DeclaredAsset (line 136). Their shared half ALREADY has an owner: both iterate Read-ManifestRow (line 69), the single parser for '#'-commented, field-trimmed manifest rows. What remains is each function's own row validation, and it is genuinely different on both axes -- the OWNER side takes 4 to 5 pipe-separated fields (id/canonical/default/mode[/knobs]) and rejects an unknown mode, the CONSUMER side takes 1 to 2 whitespace-separated fields and rejects an id ContainerHub does not own; they emit different objects (an ordered registry keyed by id vs a list of asset/local pairs). Merging them means a mode flag per caller. Budget pinned.
windows/Build-Buildkit.ps1 | windows/scripts/build/Copy-TargetPythonDeps.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT and READ: longest identical run 1 line (`$x = @{}`). The units are Build-Buildkit.ps1:533, the media-stage branch loop, and Copy-TargetPythonDeps.ps1:117-137, the split of first-touch requirements into bundle-satisfied and external. The shingles are the build-a-hashtable-then-filter-by-ContainsKey gesture followed by `if ($x.Count -gt 0) { Write-Host ... }` -- three lines of stock PowerShell reached for by two files that share no domain. The remaining counted blocks in this pair are param() declarations (`[string]$X = '...'` / `[int]$X = N`), which cannot live anywhere but the script. Budget pinned.
windows/Build-Buildkit.ps1 | windows/scripts/build/Test-Container.ps1 | 11 | reviewed 2026-09-09, READ: longest identical run 4 line(s) and all four are a language-mandated shape -- `param(` followed by three consecutive `[switch]$X,` declarations, at Build-Buildkit.ps1:45-48 (-Gpu/-PatchedLlvm/-StockLlvm) and Test-Container.ps1:18-21 (-SkipCudaTests/-ExpectGpu/-ExitOnFirstFailure). A top-level param() block cannot be moved into a module, and the parameters themselves have nothing in common. The rest of the pair is the `if ($x) { $args += @('--flag', $x) }` argument-assembly idiom shared with the Start-GeniexServers row. Budget pinned.
windows/Build-Buildkit.ps1 | windows/scripts/modules/WindowsUv.Common.psm1 | 11 | reviewed 2026-09-09, READ: longest identical run 2 line(s) on the top block (`}` / `}`), and the pair's longest run anywhere is 4 lines that are again `param(` plus three `[switch]$X,` -- Build-Buildkit.ps1:45-48 against Sync-UvProjectDependencies' parameter block at WindowsUv.Common.psm1:231-234 (-NoBuildIsolationPackageWxPython/-UseLocked/-RetryWithoutLocked). A build driver and the uv module share PowerShell's parameter-declaration syntax and nothing else; the remaining shingles are `[int]$X = N` followed by the closing `)`. Structural only. Budget pinned.
windows/scripts/Invoke-Lint.ps1 | windows/scripts/modules/WindowsBuildSweep.Common.psm1 | 11 | reviewed 2026-09-09, READ: longest identical run 1 line (`foreach ($x in $y) {`). Invoke-Lint.ps1:179-184 reports the files PSScriptAnalyzer crashed on; Write-SweepSummary (WindowsBuildSweep.Common.psm1:141) prints the per-step sweep summary and RETURNS the first non-zero exit code so the caller's `exit` carries a real build's code. Both filter a collection and print one line per member, and there the resemblance ends -- the sweep summary's product is an exit code, the lint report's is a coverage warning that feeds a distinct exit 2. Neither has anything the other could usefully call. Budget pinned.
windows/scripts/Invoke-Lint.ps1 | windows/scripts/tests/Native.ArgQuoting.Tests.ps1 | 11 | reviewed 2026-09-09, READ: longest identical run 1 line (`}`), and this row's boundary is already documented from the other side. Native.ArgQuoting.Tests.ps1:11-13 states that the REPO SWEEP moved into Invoke-Lint.ps1 on 2026-08-10 (backlog #21) because the lint gate already parses every file once and owns the \archive\ exclusion, so this suite now only proves the detectors themselves still detect. What is left in common is the AST-parse call and its error check -- and they are not even the same call: Invoke-Lint.ps1:90 uses ParseFile over the tree, Get-ArgQuotingAstFromText at Native.ArgQuoting.Tests.ps1:20 uses ParseInput on an inline positive-control string and throws if the fixture itself fails to parse. Budget pinned.
windows/scripts/build/Build-FfmpegFromSource.ps1 | windows/scripts/build/Build-LitertLmFromSource.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Budget pinned
windows/scripts/build/Build-FfmpegFromSource.ps1 | windows/scripts/build/Build-OnnxGenaiFromSource.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Budget pinned
windows/scripts/build/Build-FfmpegFromSource.ps1 | windows/scripts/build/Build-OpencvFromSource.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Budget pinned
windows/scripts/build/Build-FfmpegFromSource.ps1 | windows/scripts/build/Build-TvmFromSource.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Budget pinned
windows/scripts/build/Build-IreeFromSource.ps1 | windows/scripts/build/Build-LitertLmFromSource.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only -- the inline-patch call shape whose owner already exists. Budget pinned
windows/scripts/build/Build-IreeFromSource.ps1 | windows/scripts/build/Build-OnnxGenaiFromSource.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Budget pinned
windows/scripts/build/Build-IreeFromSource.ps1 | windows/scripts/build/Build-OpencvGstreamerPlugin.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- no contiguous block. Budget pinned
windows/scripts/build/Build-IreeFromSource.ps1 | windows/scripts/build/Export-LitertLmBridge.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Budget pinned
windows/scripts/build/Build-IreeFromSource.ps1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only -- a script calling the module that owns its scaffolding. Budget pinned
windows/scripts/build/Build-IreeFromSource.ps1 | windows/scripts/tests/SourceBuild.CrossHelpers.Tests.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- no contiguous block. Test-oracle restatement, deliberately independent of the script. Budget pinned
windows/scripts/build/Build-LitertFromSource.ps1 | windows/scripts/build/Build-OnnxGenaiFromSource.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Budget pinned
windows/scripts/build/Build-LitertFromSource.ps1 | windows/scripts/build/Build-OpencvFromSource.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Budget pinned
windows/scripts/build/Build-LitertFromSource.ps1 | windows/scripts/build/Build-OpencvGstreamerPlugin.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. The plugin builder is the merge-lane standalone route and takes no dependency on other source builds by design. Budget pinned
windows/scripts/build/Build-LitertFromSource.ps1 | windows/scripts/build/Build-TvmFromSource.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Budget pinned
windows/scripts/build/Build-LitertFromSource.ps1 | windows/scripts/tests/SourceBuild.CrossHelpers.Tests.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- no contiguous block. A Pester suite restating the cross-helper vocabulary it asserts over. Budget pinned
windows/scripts/build/Build-LitertLmFromSource.ps1 | windows/scripts/build/Build-OnnxFromSource.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- no contiguous block. Budget pinned
windows/scripts/build/Build-OnnxGenaiFromSource.ps1 | windows/scripts/build/Build-OpencvFromSource.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Budget pinned
windows/scripts/build/Build-OnnxGenaiFromSource.ps1 | windows/scripts/build/Build-TvmFromSource.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Budget pinned
windows/scripts/build/Build-OpencvFromSource.ps1 | windows/scripts/build/Build-TvmFromSource.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. Two source-build siblings on owned scaffolding. Budget pinned
windows/scripts/build/Build-OpencvFromSource.ps1 | windows/scripts/diagnostics/Test-OpencvVideoBackends.ps1 | 11 | reviewed 2026-09-09, READ: the 5-line run is a nested else-ladder of `} else { Write-Host "S" }` -- structural only, and the messages differ entirely. Test-OpencvVideoBackends.ps1:168 reads the opencv-configure.log AFTER the fact to explain what the build decided; Build-OpencvFromSource.ps1:516 is the provenance gate that makes the decision. Reader and writer of the same log, deliberately independent so the diagnostic still works when the build script is the suspect. Budget pinned
windows/scripts/build/Build-OpencvGstreamerPlugin.ps1 | windows/scripts/diagnostics/Test-Sccache2726Repro.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- no contiguous block. Test-Sccache2726Repro.ps1 is a minimal upstream-bug reproducer; its value is being standalone. Budget pinned
windows/scripts/build/Build-ResourceSampler.ps1 | windows/scripts/modules/WindowsScripts.Shared.psm1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- no contiguous block. A shared idiom, not a copy. Budget pinned
windows/scripts/build/Build-ToolchainAll.ps1 | windows/scripts/tests/SourceBuild.BundleManifest.Tests.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. The suite restates the manifest shape it asserts over -- the test oracle, deliberately independent. Budget pinned
windows/scripts/build/Build-TvmFromSource.ps1 | windows/scripts/diagnostics/Test-BuildCopy.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. A hand-run copy diagnostic, self-contained by design. Budget pinned
windows/scripts/build/Write-BundleManifest.ps1 | windows/scripts/diagnostics/Get-SiloProcesses.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s), and those lines are structural only -- the List/Add and string-formatting idiom, over two entirely unrelated outputs (a bundle manifest and a silo process listing). A shared idiom, not a copy. Budget pinned
windows/scripts/build/Write-BundleManifest.ps1 | windows/scripts/modules/WindowsSourceBuild.Common.psm1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and those lines are structural only. A shared idiom, not a copy. Budget pinned
windows/scripts/diagnostics/Find-LsmEventHolder.ps1 | windows/scripts/tests/BuildKit.TwinParity.Tests.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 2 line(s) and both are the Kataglyphis/SPDX copyright header the license gate requires in every file. The 11 shingles beyond it are the buildctl argument vocabulary -- `--addr npipe:////./pipe/buildkitd`, `--frontend dockerfile.v0`, `--local context=`, `--output type=image,name=` -- which the LSM probe spells out to start its bait solve and the twin-parity suite spells out to assert what a solve should look like. A test that asserts a command shape and the caller that emits it MUST name the same tokens; sharing a builder between them would make the suite assert against itself. Note the probe's half of this is moving behind Start-SiloBaitContainer in windows/scripts/modules/WindowsSiloProbe.Common.psm1 (see the Get-HostLsm / Get-SiloProcesses row), which will shrink this budget on its own; the row still stays, because the assertion must keep naming the tokens literally. Budget pinned.
windows/scripts/diagnostics/Test-OnnxTuReplay.ps1 | windows/scripts/modules/WindowsFormatting.Common.psm1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 3 line(s) (Test-OnnxTuReplay.ps1:249-251 / WindowsFormatting.Common.psm1:188-190) and those lines are structural only -- a `foreach` head, a `Write-Host` with an `-f` format string and a closing brace. The 11 shingles are the `("{0}: {1}" -f ...)` tabular-output idiom, which PowerShell has exactly one spelling for. Two files formatting aligned columns is not a copied block, and the module in question formats source code while the probe formats a define-set diff. Budget pinned so a real copy still trips it.
windows/scripts/diagnostics/Test-OnnxTuReplay.ps1 | windows/scripts/patches/opencv/Get-Ffmpeg9AvcodecConfig.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 3 line(s) (Test-OnnxTuReplay.ps1:23-25 / Get-Ffmpeg9AvcodecConfig.ps1:34-36) and those lines are structural only -- param entries and a closing brace. The 11 shingles are the `[regex]::Matches(...) / ForEach-Object { $_.Groups[1].Value } / Sort-Object -Unique` token-extraction idiom, applied to compiler define lists in one file and to an ffmpeg avcodec config in the other. That is the one idiomatic PowerShell spelling for 'capture group 1 of every match, deduplicated'; it is a language idiom, not a copy, and the two operate on unrelated grammars. Budget pinned.
windows/scripts/diagnostics/Test-OpencvCudaCmdshape.ps1 | windows/scripts/diagnostics/Test-SccacheNvccInstantiation.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 5 line(s) and it is lines 1-5 of BOTH files -- `#requires -Version 7.0`, the license-gate copyright pair and the `<#`/`.SYNOPSIS` opener. The rest is the in-container probe preamble, whose only logic is the Import-Module/Enter-VsDevCmdEnvironment call into an existing owner, plus the sccache server reset with per-probe ports (`43${i}0` vs 4235). Budget pinned.
windows/scripts/diagnostics/Test-OpencvCudaCmdshape.ps1 | windows/scripts/patches/opencv/Get-Ffmpeg9AvcodecConfig.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 3 line(s) (Test-OpencvCudaCmdshape.ps1:13-15 / Get-Ffmpeg9AvcodecConfig.ps1:34-36) and those lines are structural only -- param entries and a closing brace. The 11 shingles are the regex-capture-and-dedupe idiom plus the opencv build vocabulary both files name. One reads an ffmpeg avcodec configuration out of a source tree, the other measures how sccache classifies an opencv CUDA command line; they share a subject area and PowerShell's single spelling for token extraction, not a block. Budget pinned so a real copy still trips it.
windows/scripts/diagnostics/Test-SccacheNvccInstantiation.ps1 | windows/scripts/modules/WindowsFormatting.Common.psm1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest identical run 2 line(s) (Test-SccacheNvccInstantiation.ps1:4-5 / WindowsFormatting.Common.psm1:231-232) and they are the `<#` / `.SYNOPSIS` comment-based-help opener plus a copyright line -- not code. The 11 shingles are the `-f` format-string reporting idiom, which has one spelling in PowerShell. A probe that counts nvcc template instantiations and a module that formats source files share no subject; there is nothing here for an owner to own. Budget pinned so a real copy still trips it.
windows/scripts/host/Install-RustToolchain.ps1 | windows/scripts/host/Install-RustToolchain.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) -- `Invoke-RustProcessWithHeartbeat -Description '...' `` / `-FilePath (Join-Path $root '...') ``, at :234 and :246, i.e. two call sites of the helper that already owns the heartbeat, for two different rustup invocations. What repeats is the helper's argument list spread over continued lines. Budget pinned
windows/scripts/host/Install-ScoopTools.ps1 | windows/scripts/modules/WindowsRustToolchain.Common.psm1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) -- a single `$name = '<literal>'` assignment on one side and a lone `}` on the other. Below any threshold worth extracting a helper for. A shared idiom, not a copy. Budget pinned
windows/scripts/host/Publish-ShimPatch.ps1 | windows/scripts/host/Set-BuildkitdGcpolicy.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and both are closing braces. Structural only -- the tail of a guarded block, which every such script has. Budget pinned so a real copy still trips it
windows/scripts/modules/WindowsAgenticLoop.Common.psm1 | windows/scripts/modules/WindowsSourceBuild.Cuda.psm1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 12 line(s), all of it Export-ModuleMember name list against Export-ModuleMember name list, and exactly ONE substantive line matches anywhere in the pair (`return $V`). Not a dedup target. Budget pinned
windows/scripts/modules/WindowsCMake.Common.psm1 | windows/scripts/modules/WindowsTesting.Common.psm1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 7 line(s), 3 substantive - the tool-resolution guard `Get-Command "S" -ErrorAction SilentlyContinue; if (-not $V) { throw "S" }`, naming different tools (cmake against ctest) and throwing different instructions. Same finding as the WindowsClang.Common / WindowsFormatting.Common row. Budget pinned
windows/scripts/modules/WindowsScripts.Shared.psm1 | windows/scripts/modules/WindowsVulkanValidation.Common.psm1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 6 line(s), 2 substantive: `if (-not (Test-Path $V)) { throw "S" }`. A path precondition with a different message either side. Budget pinned
windows/scripts/tests/Shared.ParameterList.Tests.ps1 | windows/scripts/tests/Shared.ParameterList.Tests.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) — `Assert-Equal N $r.Count` plus one or two `Assert-Equal "S" $r[N]` lines. Every case here is three lines long: call ConvertTo-ParameterList with one input shape and assert the exact expansion. The inputs and the expected tokens fold to "S"/N, which is the entire content; there is nothing left to give an owner. Budget pinned
windows/scripts/tests/SourceBuild.BundleManifest.Tests.ps1 | windows/scripts/tests/SourceBuild.StageTargetPythonDeps.Tests.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. One unit pair, both sides being the BeforeAll temp-tree setup plus the mandated AfterAll teardown. A shared idiom, not a copy. Budget pinned
windows/scripts/tests/SourceBuild.BundleManifest.Tests.ps1 | windows/scripts/tests/SourceBuild.VerifyTargetArch.Tests.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. The single unit pair is each suite's BeforeAll/AfterAll temp-tree scaffolding, which Pester's block shapes mandate. A shared idiom, not a copy. Budget pinned
windows/scripts/tests/SourceBuild.GstreamerCompilerRt.Tests.ps1 | windows/scripts/tests/SourceBuild.VerifyTargetArch.Tests.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 4 line(s) — two `Assert-True ($text.Contains("S")) "S"` lines and two closing braces. Both suites end a source-text case by asserting a substring survives; the substrings ('__emit', 'arm64') and their messages are the content and fold away. Structural only. Budget pinned so a real copy still trips it
windows/scripts/tests/SourceBuild.NinjaRetry.Tests.ps1 | windows/scripts/tests/SourceBuild.SccacheSession.Tests.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 5 line(s) and it is the call-log assertion tail — `}.GetNewClosure()` / `$calls = @(Get-Content -LiteralPath $log)` / `Assert-Equal N $calls.Count "S"` / two `Assert-Match "S" $calls[N] "S"`. Both suites drive a fake tool that appends its arguments to a log and then assert about the log; the counts fold to N and the patterns to "S", so what is left is the shape of "read the log and check it". The `.GetNewClosure()` on the first line is a PowerShell requirement for a scriptblock handed to Invoke-WithEnv, not a choice. Budget pinned
windows/scripts/tests/SourceBuild.PinParity.Tests.ps1 | windows/scripts/tests/TestHarness.psm1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 1 line(s) — there is no shared block. All 11 shingles are against TestHarness.psm1's Get-ScriptFunctionDefinition (line 185) and are the `[Parser]::ParseFile` / `$ast.FindAll({ param($n) $n -is [...Ast] -and ... }, $true)` idiom that any AST scan in PowerShell must spell. PinParity scans call sites, the harness lifts function definitions; sharing the idiom is not sharing a block. Budget pinned
windows/scripts/tests/SourceBuild.StageTargetPythonDeps.Tests.ps1 | windows/scripts/tests/SourceBuild.VerifyTargetArch.Tests.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 3 line(s) and it is a language-mandated shape — `AfterAll { / if ($tmp -and (Test-Path $tmp)) { Remove-Item $tmp -Recurse -Force -ErrorAction SilentlyContinue } / }`. Pester's teardown block has one correct spelling and it cannot live anywhere else; every suite that creates a temp tree in BeforeAll ends with it. Structural only. Budget pinned
windows/scripts/tests/WindowsVulkanValidation.Common.Tests.ps1 | windows/scripts/tests/WindowsWasmOpt.Common.Tests.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s) — below any threshold worth extracting a helper for. These are two of the ten Windows*.Common.Tests.ps1 suites whose only real commonality is the one-line temp-dir spelling `New-Item -ItemType Directory -Path (Join-Path $env:TEMP ('<name>-' + (Get-Random))) -Force`, which the gate already suppresses as idiom at >6 owners (measured: 7 shingles across 10 files). Adopting TestHarness's New-TestDir across that family is a worthwhile tidy that would retire no allowlist row. A shared idiom, not a copy. Budget pinned
windows/scripts/host/Start-GeniexServers.ps1 | windows/scripts/tests/TestHarness.psm1 | 47 | re-measured 2026-09-08 after merging origin/main: upstream's edits to Start-GeniexServers.ps1 (525eb218 'gniex') grew the shared block. NOT a new copy written here -- the PowerShell lane's freeze was taken before that merge, so this is the same baseline moved to the merged tree. Still not reviewed.
windows/scripts/host/Start-GeniexServers.ps1 | windows/scripts/modules/WindowsAgenticLoop.Common.psm1 | 31 | re-measured 2026-09-08 after merging origin/main: upstream's edits to Start-GeniexServers.ps1 (525eb218 'gniex') grew the shared block. NOT a new copy written here -- the PowerShell lane's freeze was taken before that merge, so this is the same baseline moved to the merged tree. Still not reviewed.
linux/scripts/tests/test-consumer-inventory.sh | linux/scripts/tests/test-gate-registry.sh | 13 | reviewed 2026-09-08 by MEASUREMENT: longest identical run 0 line(s) -- not one line is shared. Both suites build a hub-shaped fixture tree and write a JSON document into it (an inventory for the consumer gate, a mutations.json for the registry gate); the shingles that match are `mkdir -p "${d}/linux/scripts/..."` and JSON punctuation, after the gate folds every string to S and every variable to $V. There is no block to hoist: a shared fixture helper would have to take both documents as parameters and would be longer than either. Budget pinned at the measurement so a real copy still trips it
windows/Build-Buildkit.ps1 | windows/scripts/host/Set-ContainerdConfig.ps1 | 11 | the shared block is a [CmdletBinding()] param() header - PowerShell entry-point boilerplate, not copied logic. It surfaced as a pair for the first time on 2026-09-08 because the deletion pass took the sixth holder of that shape out of the tree. Nothing to give an owner: a param block is the one thing that cannot live somewhere else.
windows/scripts/build/Build-GstreamerFromSource.ps1 | windows/scripts/host/Install-NewHost.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: same idiom and same verdict as the Build-LlvmFromSource row above - a temp-workdir plus Get-ChildItem shape shared between a media build stage and host provisioning. Budget at the measurement
windows/scripts/build/Build-LlvmFromSource.ps1 | windows/scripts/host/Install-NewHost.ps1 | 11 | reviewed 2026-09-09 by MEASUREMENT: longest shared run 2 line(s), and the two functions are unrelated - Install-TargetCompilerRt provisions compiler-rt for a cross target, Invoke-BuildPatchedShim builds a containerd shim. What they share is the Get-ChildItem-into-a-temp-workdir shape, a .NET/PowerShell idiom. Merging would couple the LLVM build to host provisioning. Budget at the measurement
windows/scripts/build/Build-MediaTvmAll.ps1 | windows/scripts/tests/SourceBuild.Chain.Tests.ps1 | 19 | reviewed 2026-09-09, READ: same as the Build-MediaCoreAll row above - the suite constructs the stage-list shape its subject consumes. Budget at the measurement
windows/scripts/build/Build-MediaCoreAll.ps1 | windows/scripts/tests/SourceBuild.Chain.Tests.ps1 | 19 | reviewed 2026-09-09, READ: a test mirroring its subject. SourceBuild.Chain.Tests.ps1:40 builds the same @{Name;Script;SourceDir} stage-list shape that Build-MediaCoreAll.ps1:43 passes to Invoke-SourceBuildChain - because that is what it is testing. A test that constructed the input differently from its callers would be testing something else. Budget at the measurement
windows/scripts/build/Build-MediaCoreAll.ps1 | windows/scripts/build/Build-MediaTvmAll.ps1 | 19 | reviewed 2026-09-09, READ: the owner already exists and both files name it. The shared block is the @{Name;Script;SourceDir} stage-list literal handed to Invoke-SourceBuildChain, and Build-MediaCoreAll.ps1:43 says so - "Invoke-SourceBuildChain owns the banner + native-exit check". What repeats is the CALL, not the logic; the stage contents share nothing (media codecs vs the two LLVM-heavy ML compilers). A data literal cannot be given an owner without inventing a registry neither caller wants. Budget at the measurement
linux/host-config/prefix-common.sh | linux/scripts/06-packaging/smoke-runtime-image.sh | 12 | reviewed 2026-09-08 by MEASUREMENT: longest shared run 2 line(s), and the two functions are unrelated -- nerdctl_host_prefix reads ExecStart out of the systemd USER units to find the rootless prefix, _rt_manifest_trees parses runtime-artifacts.manifest against Dockerfile.package. What collides is the normalised token shape of `name() { local a b; for/while ...; x="$(...)"; if [ -n ... ]`, i.e. bash itself. Extracting a helper here would join two things that share nothing but syntax. Arrived with 35a392ef. Budget pinned so a real copy still trips it
