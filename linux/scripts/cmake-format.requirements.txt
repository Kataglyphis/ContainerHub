# Bootstrap set for the preflight cmake-format gate, installed into
# .venv-cmake-format by check_cmake_format when cmake-format is not on PATH.
# pyyaml is load-bearing: cmake-format==0.6.13 does not depend on it and dies
# with ModuleNotFoundError the moment a YAML config (.cmake-format.yaml) is read.
cmake-format==0.6.13
pyyaml==6.0.3
